// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/grid_map.fbs".

#include "grid_map.hpp"

#include "../collection_adapter_builtins.hpp"

namespace rerun::archetypes {
    GridMap GridMap::clear_fields() {
        auto archetype = GridMap();
        archetype.data =
            ComponentBatch::empty<rerun::components::ImageBuffer>(Descriptor_data).value_or_throw();
        archetype.format = ComponentBatch::empty<rerun::components::ImageFormat>(Descriptor_format)
                               .value_or_throw();
        archetype.cell_size =
            ComponentBatch::empty<rerun::components::CellSize>(Descriptor_cell_size)
                .value_or_throw();
        archetype.translation =
            ComponentBatch::empty<rerun::components::Translation3D>(Descriptor_translation)
                .value_or_throw();
        archetype.rotation_axis_angle = ComponentBatch::empty<rerun::components::RotationAxisAngle>(
                                            Descriptor_rotation_axis_angle
        )
                                            .value_or_throw();
        archetype.quaternion =
            ComponentBatch::empty<rerun::components::RotationQuat>(Descriptor_quaternion)
                .value_or_throw();
        archetype.opacity =
            ComponentBatch::empty<rerun::components::Opacity>(Descriptor_opacity).value_or_throw();
        archetype.draw_order =
            ComponentBatch::empty<rerun::components::DrawOrder>(Descriptor_draw_order)
                .value_or_throw();
        archetype.colormap = ComponentBatch::empty<rerun::components::Colormap>(Descriptor_colormap)
                                 .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> GridMap::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(9);
        if (data.has_value()) {
            columns.push_back(data.value().partitioned(lengths_).value_or_throw());
        }
        if (format.has_value()) {
            columns.push_back(format.value().partitioned(lengths_).value_or_throw());
        }
        if (cell_size.has_value()) {
            columns.push_back(cell_size.value().partitioned(lengths_).value_or_throw());
        }
        if (translation.has_value()) {
            columns.push_back(translation.value().partitioned(lengths_).value_or_throw());
        }
        if (rotation_axis_angle.has_value()) {
            columns.push_back(rotation_axis_angle.value().partitioned(lengths_).value_or_throw());
        }
        if (quaternion.has_value()) {
            columns.push_back(quaternion.value().partitioned(lengths_).value_or_throw());
        }
        if (opacity.has_value()) {
            columns.push_back(opacity.value().partitioned(lengths_).value_or_throw());
        }
        if (draw_order.has_value()) {
            columns.push_back(draw_order.value().partitioned(lengths_).value_or_throw());
        }
        if (colormap.has_value()) {
            columns.push_back(colormap.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> GridMap::columns() {
        if (data.has_value()) {
            return columns(std::vector<uint32_t>(data.value().length(), 1));
        }
        if (format.has_value()) {
            return columns(std::vector<uint32_t>(format.value().length(), 1));
        }
        if (cell_size.has_value()) {
            return columns(std::vector<uint32_t>(cell_size.value().length(), 1));
        }
        if (translation.has_value()) {
            return columns(std::vector<uint32_t>(translation.value().length(), 1));
        }
        if (rotation_axis_angle.has_value()) {
            return columns(std::vector<uint32_t>(rotation_axis_angle.value().length(), 1));
        }
        if (quaternion.has_value()) {
            return columns(std::vector<uint32_t>(quaternion.value().length(), 1));
        }
        if (opacity.has_value()) {
            return columns(std::vector<uint32_t>(opacity.value().length(), 1));
        }
        if (draw_order.has_value()) {
            return columns(std::vector<uint32_t>(draw_order.value().length(), 1));
        }
        if (colormap.has_value()) {
            return columns(std::vector<uint32_t>(colormap.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<archetypes::GridMap>::as_batches(
        const archetypes::GridMap& archetype
    ) {
        using namespace archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(9);

        if (archetype.data.has_value()) {
            cells.push_back(archetype.data.value());
        }
        if (archetype.format.has_value()) {
            cells.push_back(archetype.format.value());
        }
        if (archetype.cell_size.has_value()) {
            cells.push_back(archetype.cell_size.value());
        }
        if (archetype.translation.has_value()) {
            cells.push_back(archetype.translation.value());
        }
        if (archetype.rotation_axis_angle.has_value()) {
            cells.push_back(archetype.rotation_axis_angle.value());
        }
        if (archetype.quaternion.has_value()) {
            cells.push_back(archetype.quaternion.value());
        }
        if (archetype.opacity.has_value()) {
            cells.push_back(archetype.opacity.value());
        }
        if (archetype.draw_order.has_value()) {
            cells.push_back(archetype.draw_order.value());
        }
        if (archetype.colormap.has_value()) {
            cells.push_back(archetype.colormap.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
