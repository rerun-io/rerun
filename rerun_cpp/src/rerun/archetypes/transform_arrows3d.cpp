// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_types/definitions/rerun/archetypes/transform_arrows_3d.fbs".

#include "transform_arrows3d.hpp"

#include "../collection_adapter_builtins.hpp"

namespace rerun::archetypes {
    TransformArrows3D TransformArrows3D::clear_fields() {
        auto archetype = TransformArrows3D();
        archetype.axis_length =
            ComponentBatch::empty<rerun::components::AxisLength>(Descriptor_axis_length)
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> TransformArrows3D::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(1);
        if (axis_length.has_value()) {
            columns.push_back(axis_length.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> TransformArrows3D::columns() {
        if (axis_length.has_value()) {
            return columns(std::vector<uint32_t>(axis_length.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<archetypes::TransformArrows3D>::as_batches(
        const archetypes::TransformArrows3D& archetype
    ) {
        using namespace archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(1);

        if (archetype.axis_length.has_value()) {
            cells.push_back(archetype.axis_length.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
