// NOTE: This file was autogenerated by re_types_builder; DO NOT EDIT.
// Based on "crates/re_types/definitions/rerun/archetypes/text_entry.fbs"

#include "text_entry.hpp"

#include "../components/body.hpp"
#include "../components/level.hpp"

#include <arrow/api.h>

namespace rerun {
    namespace archetypes {
        arrow::Result<std::vector<rerun::DataCell>> TextEntry::to_data_cells() const {
            std::vector<rerun::DataCell> cells;
            cells.reserve(2);

            {
                ARROW_ASSIGN_OR_RAISE(
                    const auto cell,
                    rerun::components::Body::to_data_cell(bodies.data(), bodies.size())
                );
                cells.push_back(cell);
            }
            if (levels.has_value()) {
                const auto& value = levels.value();
                ARROW_ASSIGN_OR_RAISE(
                    const auto cell,
                    rerun::components::Level::to_data_cell(value.data(), value.size())
                );
                cells.push_back(cell);
            }

            return cells;
        }
    } // namespace archetypes
} // namespace rerun
