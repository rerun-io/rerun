// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_annotation.fbs".

#include "audio_annotation.hpp"

#include "../collection_adapter_builtins.hpp"

namespace rerun::archetypes {
    AudioAnnotation AudioAnnotation::clear_fields() {
        auto archetype = AudioAnnotation();
        archetype.text =
            ComponentBatch::empty<rerun::components::Text>(Descriptor_text).value_or_throw();
        archetype.span =
            ComponentBatch::empty<rerun::components::Range1D>(Descriptor_span).value_or_throw();
        archetype.color =
            ComponentBatch::empty<rerun::components::Color>(Descriptor_color).value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> AudioAnnotation::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(3);
        if (text.has_value()) {
            columns.push_back(text.value().partitioned(lengths_).value_or_throw());
        }
        if (span.has_value()) {
            columns.push_back(span.value().partitioned(lengths_).value_or_throw());
        }
        if (color.has_value()) {
            columns.push_back(color.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> AudioAnnotation::columns() {
        if (text.has_value()) {
            return columns(std::vector<uint32_t>(text.value().length(), 1));
        }
        if (span.has_value()) {
            return columns(std::vector<uint32_t>(span.value().length(), 1));
        }
        if (color.has_value()) {
            return columns(std::vector<uint32_t>(color.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<archetypes::AudioAnnotation>::as_batches(
        const archetypes::AudioAnnotation& archetype
    ) {
        using namespace archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(3);

        if (archetype.text.has_value()) {
            cells.push_back(archetype.text.value());
        }
        if (archetype.span.has_value()) {
            cells.push_back(archetype.span.value());
        }
        if (archetype.color.has_value()) {
            cells.push_back(archetype.color.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
