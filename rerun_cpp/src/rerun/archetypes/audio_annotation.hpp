// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_annotation.fbs".

#pragma once

#include "../collection.hpp"
#include "../component_batch.hpp"
#include "../component_column.hpp"
#include "../components/color.hpp"
#include "../components/range1d.hpp"
#include "../components/text.hpp"
#include "../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::archetypes {
    /// **Archetype**: A text annotation for a time window in an audio clip.
    ///
    /// The `span` is measured in seconds relative to the row timestamp. For example,
    /// log an ASR word at the start of the utterance with `span=[0.0, 0.42]`, or log
    /// a complete transcript once at time zero with each word carrying its own
    /// `span=[word_start, word_end]`.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct AudioAnnotation {
        /// Annotation text, such as an ASR word or phrase.
        std::optional<ComponentBatch> text;

        /// Time span in seconds relative to the row timestamp.
        std::optional<ComponentBatch> span;

        /// Optional color to use for the annotation span.
        std::optional<ComponentBatch> color;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.archetypes.AudioAnnotation";

        /// `ComponentDescriptor` for the `text` field.
        static constexpr auto Descriptor_text = ComponentDescriptor(
            ArchetypeName, "AudioAnnotation:text", Loggable<rerun::components::Text>::ComponentType
        );
        /// `ComponentDescriptor` for the `span` field.
        static constexpr auto Descriptor_span = ComponentDescriptor(
            ArchetypeName, "AudioAnnotation:span",
            Loggable<rerun::components::Range1D>::ComponentType
        );
        /// `ComponentDescriptor` for the `color` field.
        static constexpr auto Descriptor_color = ComponentDescriptor(
            ArchetypeName, "AudioAnnotation:color",
            Loggable<rerun::components::Color>::ComponentType
        );

      public:
        AudioAnnotation() = default;
        AudioAnnotation(AudioAnnotation&& other) = default;
        AudioAnnotation(const AudioAnnotation& other) = default;
        AudioAnnotation& operator=(const AudioAnnotation& other) = default;
        AudioAnnotation& operator=(AudioAnnotation&& other) = default;

        explicit AudioAnnotation(rerun::components::Text _text, rerun::components::Range1D _span)
            : text(ComponentBatch::from_loggable(std::move(_text), Descriptor_text).value_or_throw()
              ),
              span(ComponentBatch::from_loggable(std::move(_span), Descriptor_span).value_or_throw()
              ) {}

        /// Update only some specific fields of a `AudioAnnotation`.
        static AudioAnnotation update_fields() {
            return AudioAnnotation();
        }

        /// Clear all the fields of a `AudioAnnotation`.
        static AudioAnnotation clear_fields();

        /// Annotation text, such as an ASR word or phrase.
        AudioAnnotation with_text(const rerun::components::Text& _text) && {
            text = ComponentBatch::from_loggable(_text, Descriptor_text).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `text` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_text` should
        /// be used when logging a single row's worth of data.
        AudioAnnotation with_many_text(const Collection<rerun::components::Text>& _text) && {
            text = ComponentBatch::from_loggable(_text, Descriptor_text).value_or_throw();
            return std::move(*this);
        }

        /// Time span in seconds relative to the row timestamp.
        AudioAnnotation with_span(const rerun::components::Range1D& _span) && {
            span = ComponentBatch::from_loggable(_span, Descriptor_span).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `span` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_span` should
        /// be used when logging a single row's worth of data.
        AudioAnnotation with_many_span(const Collection<rerun::components::Range1D>& _span) && {
            span = ComponentBatch::from_loggable(_span, Descriptor_span).value_or_throw();
            return std::move(*this);
        }

        /// Optional color to use for the annotation span.
        AudioAnnotation with_color(const rerun::components::Color& _color) && {
            color = ComponentBatch::from_loggable(_color, Descriptor_color).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `color` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_color` should
        /// be used when logging a single row's worth of data.
        AudioAnnotation with_many_color(const Collection<rerun::components::Color>& _color) && {
            color = ComponentBatch::from_loggable(_color, Descriptor_color).value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<archetypes::AudioAnnotation> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const archetypes::AudioAnnotation& archetype
        );
    };
} // namespace rerun
