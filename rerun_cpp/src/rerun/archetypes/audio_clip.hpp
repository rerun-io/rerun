// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_clip.fbs".

#pragma once

#include "../collection.hpp"
#include "../component_batch.hpp"
#include "../component_column.hpp"
#include "../components/audio_channel_name.hpp"
#include "../components/sample_rate.hpp"
#include "../components/tensor_data.hpp"
#include "../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::archetypes {
    /// **Archetype**: A chunk of PCM audio samples.
    ///
    /// The samples are stored as a numeric tensor. 1D tensors are interpreted as mono audio.
    /// 2D tensors are interpreted as `[sample, channel]` audio, and may include dimension
    /// names such as `"sample"` and `"channel"`.
    ///
    /// Log audio chunks on a time timeline at the time where the first sample starts. The
    /// `components::SampleRate` determines how sample indices map to time, allowing the
    /// viewer to display waveforms and keep audio-aligned views in sync with the global
    /// timeline.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct AudioClip {
        /// PCM audio samples.
        std::optional<ComponentBatch> samples;

        /// Sample rate in Hertz.
        std::optional<ComponentBatch> sample_rate;

        /// Optional display names for each audio channel.
        std::optional<ComponentBatch> channel_names;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.archetypes.AudioClip";

        /// `ComponentDescriptor` for the `samples` field.
        static constexpr auto Descriptor_samples = ComponentDescriptor(
            ArchetypeName, "AudioClip:samples",
            Loggable<rerun::components::TensorData>::ComponentType
        );
        /// `ComponentDescriptor` for the `sample_rate` field.
        static constexpr auto Descriptor_sample_rate = ComponentDescriptor(
            ArchetypeName, "AudioClip:sample_rate",
            Loggable<rerun::components::SampleRate>::ComponentType
        );
        /// `ComponentDescriptor` for the `channel_names` field.
        static constexpr auto Descriptor_channel_names = ComponentDescriptor(
            ArchetypeName, "AudioClip:channel_names",
            Loggable<rerun::components::AudioChannelName>::ComponentType
        );

      public:
        AudioClip() = default;
        AudioClip(AudioClip&& other) = default;
        AudioClip(const AudioClip& other) = default;
        AudioClip& operator=(const AudioClip& other) = default;
        AudioClip& operator=(AudioClip&& other) = default;

        explicit AudioClip(
            rerun::components::TensorData _samples, rerun::components::SampleRate _sample_rate
        )
            : samples(ComponentBatch::from_loggable(std::move(_samples), Descriptor_samples)
                          .value_or_throw()),
              sample_rate(
                  ComponentBatch::from_loggable(std::move(_sample_rate), Descriptor_sample_rate)
                      .value_or_throw()
              ) {}

        /// Update only some specific fields of a `AudioClip`.
        static AudioClip update_fields() {
            return AudioClip();
        }

        /// Clear all the fields of a `AudioClip`.
        static AudioClip clear_fields();

        /// PCM audio samples.
        AudioClip with_samples(const rerun::components::TensorData& _samples) && {
            samples = ComponentBatch::from_loggable(_samples, Descriptor_samples).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `samples` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_samples` should
        /// be used when logging a single row's worth of data.
        AudioClip with_many_samples(const Collection<rerun::components::TensorData>& _samples) && {
            samples = ComponentBatch::from_loggable(_samples, Descriptor_samples).value_or_throw();
            return std::move(*this);
        }

        /// Sample rate in Hertz.
        AudioClip with_sample_rate(const rerun::components::SampleRate& _sample_rate) && {
            sample_rate = ComponentBatch::from_loggable(_sample_rate, Descriptor_sample_rate)
                              .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `sample_rate` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_sample_rate` should
        /// be used when logging a single row's worth of data.
        AudioClip with_many_sample_rate(
            const Collection<rerun::components::SampleRate>& _sample_rate
        ) && {
            sample_rate = ComponentBatch::from_loggable(_sample_rate, Descriptor_sample_rate)
                              .value_or_throw();
            return std::move(*this);
        }

        /// Optional display names for each audio channel.
        AudioClip with_channel_names(
            const Collection<rerun::components::AudioChannelName>& _channel_names
        ) && {
            channel_names = ComponentBatch::from_loggable(_channel_names, Descriptor_channel_names)
                                .value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<archetypes::AudioClip> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(const archetypes::AudioClip& archetype
        );
    };
} // namespace rerun
