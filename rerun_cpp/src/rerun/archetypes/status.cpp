// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/status.fbs".

#include "status.hpp"

#include "../collection_adapter_builtins.hpp"

namespace rerun::archetypes {
    Status Status::clear_fields() {
        auto archetype = Status();
        archetype.status =
            ComponentBatch::empty<rerun::components::Text>(Descriptor_status).value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> Status::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(1);
        if (status.has_value()) {
            columns.push_back(status.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> Status::columns() {
        if (status.has_value()) {
            return columns(std::vector<uint32_t>(status.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<archetypes::Status>::as_batches(
        const archetypes::Status& archetype
    ) {
        using namespace archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(1);

        if (archetype.status.has_value()) {
            cells.push_back(archetype.status.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
