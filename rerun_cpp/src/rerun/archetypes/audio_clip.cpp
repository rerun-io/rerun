// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_clip.fbs".

#include "audio_clip.hpp"

#include "../collection_adapter_builtins.hpp"

namespace rerun::archetypes {
    AudioClip AudioClip::clear_fields() {
        auto archetype = AudioClip();
        archetype.samples = ComponentBatch::empty<rerun::components::TensorData>(Descriptor_samples)
                                .value_or_throw();
        archetype.sample_rate =
            ComponentBatch::empty<rerun::components::SampleRate>(Descriptor_sample_rate)
                .value_or_throw();
        archetype.channel_names =
            ComponentBatch::empty<rerun::components::AudioChannelName>(Descriptor_channel_names)
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> AudioClip::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(3);
        if (samples.has_value()) {
            columns.push_back(samples.value().partitioned(lengths_).value_or_throw());
        }
        if (sample_rate.has_value()) {
            columns.push_back(sample_rate.value().partitioned(lengths_).value_or_throw());
        }
        if (channel_names.has_value()) {
            columns.push_back(channel_names.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> AudioClip::columns() {
        if (samples.has_value()) {
            return columns(std::vector<uint32_t>(samples.value().length(), 1));
        }
        if (sample_rate.has_value()) {
            return columns(std::vector<uint32_t>(sample_rate.value().length(), 1));
        }
        if (channel_names.has_value()) {
            return columns(std::vector<uint32_t>(channel_names.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<archetypes::AudioClip>::as_batches(
        const archetypes::AudioClip& archetype
    ) {
        using namespace archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(3);

        if (archetype.samples.has_value()) {
            cells.push_back(archetype.samples.value());
        }
        if (archetype.sample_rate.has_value()) {
            cells.push_back(archetype.sample_rate.value());
        }
        if (archetype.channel_names.has_value()) {
            cells.push_back(archetype.channel_names.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
