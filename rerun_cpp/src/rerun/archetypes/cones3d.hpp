// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/cones3d.fbs".

#pragma once

#include "../collection.hpp"
#include "../component_batch.hpp"
#include "../component_column.hpp"
#include "../components/albedo_factor.hpp"
#include "../components/class_id.hpp"
#include "../components/color.hpp"
#include "../components/fill_mode.hpp"
#include "../components/image_buffer.hpp"
#include "../components/image_format.hpp"
#include "../components/length.hpp"
#include "../components/radius.hpp"
#include "../components/rotation_axis_angle.hpp"
#include "../components/rotation_quat.hpp"
#include "../components/show_labels.hpp"
#include "../components/text.hpp"
#include "../components/translation3d.hpp"
#include "../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::archetypes {
    /// **Archetype**: 3D cones with flat circular bases.
    ///
    /// This archetype is for cone primitives defined by their axial length and base radius.
    /// If no rotation is specified, cones align with the +Z axis of the local coordinate system,
    /// with their base centered at `-length / 2` and tip at `+length / 2`.
    ///
    /// Orienting and placing cones forms a separate transform that is applied prior to `archetypes::InstancePoses3D` and `archetypes::Transform3D`.
    struct Cones3D {
        /// The total axial length of the cone, measured from the center of the base to the tip.
        std::optional<ComponentBatch> lengths;

        /// Base radii of the cones.
        std::optional<ComponentBatch> radii;

        /// Optional centers of the cones.
        ///
        /// If not specified, each cone will be centered at (0, 0, 0).
        std::optional<ComponentBatch> centers;

        /// Rotations via axis + angle.
        ///
        /// If no rotation is specified, the cones align with the +Z axis of the local coordinate system.
        std::optional<ComponentBatch> rotation_axis_angles;

        /// Rotations via quaternion.
        ///
        /// If no rotation is specified, the cones align with the +Z axis of the local coordinate system.
        std::optional<ComponentBatch> quaternions;

        /// Optional colors for the cones.
        ///
        /// Alpha channel is used for transparency for solid fill-mode.
        std::optional<ComponentBatch> colors;

        /// Optional radii for the lines used when the cone is rendered as a wireframe.
        std::optional<ComponentBatch> line_radii;

        /// Optionally choose whether the cones are drawn with lines or solid.
        std::optional<ComponentBatch> fill_mode;

        /// Optional color multiplier for textured cones.
        ///
        /// If there is no texture, this acts as a color for the solid fill.
        std::optional<ComponentBatch> albedo_factor;

        /// Optional albedo texture buffer for solid cones.
        ///
        /// The texture is mapped procedurally around the cone: U wraps around the base and V runs from base to tip.
        /// The flat base uses a radial mapping into the same texture.
        std::optional<ComponentBatch> albedo_texture_buffer;

        /// Optional albedo texture format for solid cones.
        std::optional<ComponentBatch> albedo_texture_format;

        /// Optional text labels for the cones, which will be located at their centers.
        std::optional<ComponentBatch> labels;

        /// Whether the text labels should be shown.
        ///
        /// If not set, labels will automatically appear when there is exactly one label for this entity
        /// or the number of instances on this entity is under a certain threshold.
        std::optional<ComponentBatch> show_labels;

        /// Optional class ID for the cones.
        ///
        /// The class ID provides colors and labels if not specified explicitly.
        std::optional<ComponentBatch> class_ids;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.archetypes.Cones3D";

        /// `ComponentDescriptor` for the `lengths` field.
        static constexpr auto Descriptor_lengths = ComponentDescriptor(
            ArchetypeName, "Cones3D:lengths", Loggable<rerun::components::Length>::ComponentType
        );
        /// `ComponentDescriptor` for the `radii` field.
        static constexpr auto Descriptor_radii = ComponentDescriptor(
            ArchetypeName, "Cones3D:radii", Loggable<rerun::components::Radius>::ComponentType
        );
        /// `ComponentDescriptor` for the `centers` field.
        static constexpr auto Descriptor_centers = ComponentDescriptor(
            ArchetypeName, "Cones3D:centers",
            Loggable<rerun::components::Translation3D>::ComponentType
        );
        /// `ComponentDescriptor` for the `rotation_axis_angles` field.
        static constexpr auto Descriptor_rotation_axis_angles = ComponentDescriptor(
            ArchetypeName, "Cones3D:rotation_axis_angles",
            Loggable<rerun::components::RotationAxisAngle>::ComponentType
        );
        /// `ComponentDescriptor` for the `quaternions` field.
        static constexpr auto Descriptor_quaternions = ComponentDescriptor(
            ArchetypeName, "Cones3D:quaternions",
            Loggable<rerun::components::RotationQuat>::ComponentType
        );
        /// `ComponentDescriptor` for the `colors` field.
        static constexpr auto Descriptor_colors = ComponentDescriptor(
            ArchetypeName, "Cones3D:colors", Loggable<rerun::components::Color>::ComponentType
        );
        /// `ComponentDescriptor` for the `line_radii` field.
        static constexpr auto Descriptor_line_radii = ComponentDescriptor(
            ArchetypeName, "Cones3D:line_radii", Loggable<rerun::components::Radius>::ComponentType
        );
        /// `ComponentDescriptor` for the `fill_mode` field.
        static constexpr auto Descriptor_fill_mode = ComponentDescriptor(
            ArchetypeName, "Cones3D:fill_mode", Loggable<rerun::components::FillMode>::ComponentType
        );
        /// `ComponentDescriptor` for the `albedo_factor` field.
        static constexpr auto Descriptor_albedo_factor = ComponentDescriptor(
            ArchetypeName, "Cones3D:albedo_factor",
            Loggable<rerun::components::AlbedoFactor>::ComponentType
        );
        /// `ComponentDescriptor` for the `albedo_texture_buffer` field.
        static constexpr auto Descriptor_albedo_texture_buffer = ComponentDescriptor(
            ArchetypeName, "Cones3D:albedo_texture_buffer",
            Loggable<rerun::components::ImageBuffer>::ComponentType
        );
        /// `ComponentDescriptor` for the `albedo_texture_format` field.
        static constexpr auto Descriptor_albedo_texture_format = ComponentDescriptor(
            ArchetypeName, "Cones3D:albedo_texture_format",
            Loggable<rerun::components::ImageFormat>::ComponentType
        );
        /// `ComponentDescriptor` for the `labels` field.
        static constexpr auto Descriptor_labels = ComponentDescriptor(
            ArchetypeName, "Cones3D:labels", Loggable<rerun::components::Text>::ComponentType
        );
        /// `ComponentDescriptor` for the `show_labels` field.
        static constexpr auto Descriptor_show_labels = ComponentDescriptor(
            ArchetypeName, "Cones3D:show_labels",
            Loggable<rerun::components::ShowLabels>::ComponentType
        );
        /// `ComponentDescriptor` for the `class_ids` field.
        static constexpr auto Descriptor_class_ids = ComponentDescriptor(
            ArchetypeName, "Cones3D:class_ids", Loggable<rerun::components::ClassId>::ComponentType
        );

      public:
        Cones3D() = default;
        Cones3D(Cones3D&& other) = default;
        Cones3D(const Cones3D& other) = default;
        Cones3D& operator=(const Cones3D& other) = default;
        Cones3D& operator=(Cones3D&& other) = default;

        /// Update only some specific fields of a `Cones3D`.
        static Cones3D update_fields() {
            return Cones3D();
        }

        /// Clear all the fields of a `Cones3D`.
        static Cones3D clear_fields();

        /// The total axial length of the cone, measured from the center of the base to the tip.
        Cones3D with_lengths(const Collection<rerun::components::Length>& _lengths) && {
            lengths = ComponentBatch::from_loggable(_lengths, Descriptor_lengths).value_or_throw();
            return std::move(*this);
        }

        /// Base radii of the cones.
        Cones3D with_radii(const Collection<rerun::components::Radius>& _radii) && {
            radii = ComponentBatch::from_loggable(_radii, Descriptor_radii).value_or_throw();
            return std::move(*this);
        }

        /// Optional centers of the cones.
        ///
        /// If not specified, each cone will be centered at (0, 0, 0).
        Cones3D with_centers(const Collection<rerun::components::Translation3D>& _centers) && {
            centers = ComponentBatch::from_loggable(_centers, Descriptor_centers).value_or_throw();
            return std::move(*this);
        }

        /// Rotations via axis + angle.
        ///
        /// If no rotation is specified, the cones align with the +Z axis of the local coordinate system.
        Cones3D with_rotation_axis_angles(
            const Collection<rerun::components::RotationAxisAngle>& _rotation_axis_angles
        ) && {
            rotation_axis_angles = ComponentBatch::from_loggable(
                                       _rotation_axis_angles,
                                       Descriptor_rotation_axis_angles
            )
                                       .value_or_throw();
            return std::move(*this);
        }

        /// Rotations via quaternion.
        ///
        /// If no rotation is specified, the cones align with the +Z axis of the local coordinate system.
        Cones3D with_quaternions(const Collection<rerun::components::RotationQuat>& _quaternions
        ) && {
            quaternions = ComponentBatch::from_loggable(_quaternions, Descriptor_quaternions)
                              .value_or_throw();
            return std::move(*this);
        }

        /// Optional colors for the cones.
        ///
        /// Alpha channel is used for transparency for solid fill-mode.
        Cones3D with_colors(const Collection<rerun::components::Color>& _colors) && {
            colors = ComponentBatch::from_loggable(_colors, Descriptor_colors).value_or_throw();
            return std::move(*this);
        }

        /// Optional radii for the lines used when the cone is rendered as a wireframe.
        Cones3D with_line_radii(const Collection<rerun::components::Radius>& _line_radii) && {
            line_radii =
                ComponentBatch::from_loggable(_line_radii, Descriptor_line_radii).value_or_throw();
            return std::move(*this);
        }

        /// Optionally choose whether the cones are drawn with lines or solid.
        Cones3D with_fill_mode(const rerun::components::FillMode& _fill_mode) && {
            fill_mode =
                ComponentBatch::from_loggable(_fill_mode, Descriptor_fill_mode).value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `fill_mode` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_fill_mode` should
        /// be used when logging a single row's worth of data.
        Cones3D with_many_fill_mode(const Collection<rerun::components::FillMode>& _fill_mode) && {
            fill_mode =
                ComponentBatch::from_loggable(_fill_mode, Descriptor_fill_mode).value_or_throw();
            return std::move(*this);
        }

        /// Optional color multiplier for textured cones.
        ///
        /// If there is no texture, this acts as a color for the solid fill.
        Cones3D with_albedo_factor(const rerun::components::AlbedoFactor& _albedo_factor) && {
            albedo_factor = ComponentBatch::from_loggable(_albedo_factor, Descriptor_albedo_factor)
                                .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `albedo_factor` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_albedo_factor` should
        /// be used when logging a single row's worth of data.
        Cones3D with_many_albedo_factor(
            const Collection<rerun::components::AlbedoFactor>& _albedo_factor
        ) && {
            albedo_factor = ComponentBatch::from_loggable(_albedo_factor, Descriptor_albedo_factor)
                                .value_or_throw();
            return std::move(*this);
        }

        /// Optional albedo texture buffer for solid cones.
        ///
        /// The texture is mapped procedurally around the cone: U wraps around the base and V runs from base to tip.
        /// The flat base uses a radial mapping into the same texture.
        Cones3D with_albedo_texture_buffer(
            const rerun::components::ImageBuffer& _albedo_texture_buffer
        ) && {
            albedo_texture_buffer = ComponentBatch::from_loggable(
                                        _albedo_texture_buffer,
                                        Descriptor_albedo_texture_buffer
            )
                                        .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `albedo_texture_buffer` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_albedo_texture_buffer` should
        /// be used when logging a single row's worth of data.
        Cones3D with_many_albedo_texture_buffer(
            const Collection<rerun::components::ImageBuffer>& _albedo_texture_buffer
        ) && {
            albedo_texture_buffer = ComponentBatch::from_loggable(
                                        _albedo_texture_buffer,
                                        Descriptor_albedo_texture_buffer
            )
                                        .value_or_throw();
            return std::move(*this);
        }

        /// Optional albedo texture format for solid cones.
        Cones3D with_albedo_texture_format(
            const rerun::components::ImageFormat& _albedo_texture_format
        ) && {
            albedo_texture_format = ComponentBatch::from_loggable(
                                        _albedo_texture_format,
                                        Descriptor_albedo_texture_format
            )
                                        .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `albedo_texture_format` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_albedo_texture_format` should
        /// be used when logging a single row's worth of data.
        Cones3D with_many_albedo_texture_format(
            const Collection<rerun::components::ImageFormat>& _albedo_texture_format
        ) && {
            albedo_texture_format = ComponentBatch::from_loggable(
                                        _albedo_texture_format,
                                        Descriptor_albedo_texture_format
            )
                                        .value_or_throw();
            return std::move(*this);
        }

        /// Optional text labels for the cones, which will be located at their centers.
        Cones3D with_labels(const Collection<rerun::components::Text>& _labels) && {
            labels = ComponentBatch::from_loggable(_labels, Descriptor_labels).value_or_throw();
            return std::move(*this);
        }

        /// Whether the text labels should be shown.
        ///
        /// If not set, labels will automatically appear when there is exactly one label for this entity
        /// or the number of instances on this entity is under a certain threshold.
        Cones3D with_show_labels(const rerun::components::ShowLabels& _show_labels) && {
            show_labels = ComponentBatch::from_loggable(_show_labels, Descriptor_show_labels)
                              .value_or_throw();
            return std::move(*this);
        }

        /// This method makes it possible to pack multiple `show_labels` in a single component batch.
        ///
        /// This only makes sense when used in conjunction with `columns`. `with_show_labels` should
        /// be used when logging a single row's worth of data.
        Cones3D with_many_show_labels(const Collection<rerun::components::ShowLabels>& _show_labels
        ) && {
            show_labels = ComponentBatch::from_loggable(_show_labels, Descriptor_show_labels)
                              .value_or_throw();
            return std::move(*this);
        }

        /// Optional class ID for the cones.
        ///
        /// The class ID provides colors and labels if not specified explicitly.
        Cones3D with_class_ids(const Collection<rerun::components::ClassId>& _class_ids) && {
            class_ids =
                ComponentBatch::from_loggable(_class_ids, Descriptor_class_ids).value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<archetypes::Cones3D> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(const archetypes::Cones3D& archetype);
    };
} // namespace rerun
