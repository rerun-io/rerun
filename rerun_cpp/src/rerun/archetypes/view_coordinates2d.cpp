// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/view_coordinates2d.fbs".

#include "view_coordinates2d.hpp"

#include "../collection_adapter_builtins.hpp"

namespace rerun::archetypes {
    ViewCoordinates2D ViewCoordinates2D::clear_fields() {
        auto archetype = ViewCoordinates2D();
        archetype.xy = ComponentBatch::empty<rerun::components::ViewCoordinates2D>(Descriptor_xy)
                           .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> ViewCoordinates2D::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(1);
        if (xy.has_value()) {
            columns.push_back(xy.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> ViewCoordinates2D::columns() {
        if (xy.has_value()) {
            return columns(std::vector<uint32_t>(xy.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<archetypes::ViewCoordinates2D>::as_batches(
        const archetypes::ViewCoordinates2D& archetype
    ) {
        using namespace archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(1);

        if (archetype.xy.has_value()) {
            cells.push_back(archetype.xy.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
