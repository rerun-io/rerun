// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/rotation_axis_angle.def.rs".

#pragma once

#include "../result.hpp"
#include "angle.hpp"
#include "vec3d.hpp"

#include <cstdint>
#include <memory>

namespace arrow {
    class Array;
    class DataType;
    class StructBuilder;
} // namespace arrow

namespace rerun::encodings {
    /// **Encoding**: 3D rotation represented by a rotation around a given axis.
    struct RotationAxisAngle {
        /// Axis to rotate around.
        ///
        /// This is not required to be normalized.
        /// However, if normalization of the rotation axis fails (typically due to a zero vector)
        /// the rotation is treated as an invalid transform, unless the angle is zero in which case
        /// it is treated as an identity.
        rerun::encodings::Vec3D axis;

        /// How much to rotate around the axis.
        rerun::encodings::Angle angle;

      public: // START of extensions from rotation_axis_angle_ext.cpp:
        RotationAxisAngle(const Vec3D& _axis, const Angle& _angle) : axis(_axis), angle(_angle) {}

        // END of extensions from rotation_axis_angle_ext.cpp, start of generated code:

      public:
        RotationAxisAngle() = default;
    };
} // namespace rerun::encodings

namespace rerun {
    template <typename T>
    struct Loggable;

    /// \private
    template <>
    struct Loggable<encodings::RotationAxisAngle> {
        static constexpr std::string_view ComponentType = "rerun.encodings.RotationAxisAngle";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_datatype();

        /// Serializes an array of `rerun::encodings::RotationAxisAngle` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const encodings::RotationAxisAngle* instances, size_t num_instances
        );

        /// Fills an arrow array builder with an array of this type.
        static rerun::Error fill_arrow_array_builder(
            arrow::StructBuilder* builder, const encodings::RotationAxisAngle* elements,
            size_t num_elements
        );
    };
} // namespace rerun
