// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/card_layout.def.rs".

#include "card_layout.hpp"

#include "../../collection_adapter_builtins.hpp"

namespace rerun::blueprint::archetypes {
    CardLayout CardLayout::clear_fields() {
        auto archetype = CardLayout();
        archetype.title =
            ComponentBatch::empty<rerun::blueprint::components::ColumnName>(Descriptor_title)
                .value_or_throw();
        archetype.link =
            ComponentBatch::empty<rerun::blueprint::components::ColumnName>(Descriptor_link)
                .value_or_throw();
        archetype.field_order =
            ComponentBatch::empty<rerun::blueprint::components::ColumnName>(Descriptor_field_order)
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> CardLayout::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(3);
        if (title.has_value()) {
            columns.push_back(title.value().partitioned(lengths_).value_or_throw());
        }
        if (link.has_value()) {
            columns.push_back(link.value().partitioned(lengths_).value_or_throw());
        }
        if (field_order.has_value()) {
            columns.push_back(field_order.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> CardLayout::columns() {
        if (title.has_value()) {
            return columns(std::vector<uint32_t>(title.value().length(), 1));
        }
        if (link.has_value()) {
            return columns(std::vector<uint32_t>(link.value().length(), 1));
        }
        if (field_order.has_value()) {
            return columns(std::vector<uint32_t>(field_order.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::blueprint::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<blueprint::archetypes::CardLayout>::as_batches(
        const blueprint::archetypes::CardLayout& archetype
    ) {
        using namespace blueprint::archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(3);

        if (archetype.title.has_value()) {
            cells.push_back(archetype.title.value());
        }
        if (archetype.link.has_value()) {
            cells.push_back(archetype.link.value());
        }
        if (archetype.field_order.has_value()) {
            cells.push_back(archetype.field_order.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
