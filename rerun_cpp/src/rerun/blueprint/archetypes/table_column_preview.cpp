// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_column_preview.def.rs".

#include "table_column_preview.hpp"

#include "../../collection_adapter_builtins.hpp"

namespace rerun::blueprint::archetypes {
    TableColumnPreview TableColumnPreview::clear_fields() {
        auto archetype = TableColumnPreview();
        archetype.views =
            ComponentBatch::empty<rerun::blueprint::components::IncludedContent>(Descriptor_views)
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> TableColumnPreview::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(1);
        if (views.has_value()) {
            columns.push_back(views.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> TableColumnPreview::columns() {
        if (views.has_value()) {
            return columns(std::vector<uint32_t>(views.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::blueprint::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>>
        AsComponents<blueprint::archetypes::TableColumnPreview>::as_batches(
            const blueprint::archetypes::TableColumnPreview& archetype
        ) {
        using namespace blueprint::archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(1);

        if (archetype.views.has_value()) {
            cells.push_back(archetype.views.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
