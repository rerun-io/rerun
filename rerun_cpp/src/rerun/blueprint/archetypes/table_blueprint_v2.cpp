// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_blueprint.def.rs".

#include "table_blueprint_v2.hpp"

#include "../../collection_adapter_builtins.hpp"

namespace rerun::blueprint::archetypes {
    TableBlueprintV2 TableBlueprintV2::clear_fields() {
        auto archetype = TableBlueprintV2();
        archetype.default_layout =
            ComponentBatch::empty<rerun::blueprint::components::TableLayoutKind>(
                Descriptor_default_layout
            )
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> TableBlueprintV2::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(1);
        if (default_layout.has_value()) {
            columns.push_back(default_layout.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> TableBlueprintV2::columns() {
        if (default_layout.has_value()) {
            return columns(std::vector<uint32_t>(default_layout.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::blueprint::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>>
        AsComponents<blueprint::archetypes::TableBlueprintV2>::as_batches(
            const blueprint::archetypes::TableBlueprintV2& archetype
        ) {
        using namespace blueprint::archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(1);

        if (archetype.default_layout.has_value()) {
            cells.push_back(archetype.default_layout.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
