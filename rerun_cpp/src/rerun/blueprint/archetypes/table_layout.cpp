// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_layout.def.rs".

#include "table_layout.hpp"

#include "../../collection_adapter_builtins.hpp"

namespace rerun::blueprint::archetypes {
    TableLayout TableLayout::clear_fields() {
        auto archetype = TableLayout();
        archetype.column_order =
            ComponentBatch::empty<rerun::blueprint::components::ColumnName>(Descriptor_column_order)
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> TableLayout::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(1);
        if (column_order.has_value()) {
            columns.push_back(column_order.value().partitioned(lengths_).value_or_throw());
        }
        return columns;
    }

    Collection<ComponentColumn> TableLayout::columns() {
        if (column_order.has_value()) {
            return columns(std::vector<uint32_t>(column_order.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::blueprint::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>> AsComponents<blueprint::archetypes::TableLayout>::as_batches(
        const blueprint::archetypes::TableLayout& archetype
    ) {
        using namespace blueprint::archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(1);

        if (archetype.column_order.has_value()) {
            cells.push_back(archetype.column_order.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
