// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/previews_config.def.rs".

#pragma once

#include "../../blueprint/components/timeline_name.hpp"
#include "../../collection.hpp"
#include "../../component_batch.hpp"
#include "../../component_column.hpp"
#include "../../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::blueprint::archetypes {
    /// **Archetype**: Shared configuration for every preview cell in a table blueprint.
    ///
    /// This archetype is stored at the entity `/table` alongside `archetypes::TableBlueprint`.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct PreviewsConfig {
        /// The timeline used by every preview cell.
        ///
        /// If left empty a timeline is automatically picked, preferring custom over built-in.
        std::optional<ComponentBatch> timeline;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.blueprint.archetypes.PreviewsConfig";

        /// `ComponentDescriptor` for the `timeline` field.
        static constexpr auto Descriptor_timeline = ComponentDescriptor(
            ArchetypeName, "PreviewsConfig:timeline",
            Loggable<rerun::blueprint::components::TimelineName>::ComponentType
        );

      public:
        PreviewsConfig() = default;
        PreviewsConfig(PreviewsConfig&& other) = default;
        PreviewsConfig(const PreviewsConfig& other) = default;
        PreviewsConfig& operator=(const PreviewsConfig& other) = default;
        PreviewsConfig& operator=(PreviewsConfig&& other) = default;

        /// Update only some specific fields of a `PreviewsConfig`.
        static PreviewsConfig update_fields() {
            return PreviewsConfig();
        }

        /// Clear all the fields of a `PreviewsConfig`.
        static PreviewsConfig clear_fields();

        /// The timeline used by every preview cell.
        ///
        /// If left empty a timeline is automatically picked, preferring custom over built-in.
        PreviewsConfig with_timeline(const rerun::blueprint::components::TimelineName& _timeline
        ) && {
            timeline =
                ComponentBatch::from_loggable(_timeline, Descriptor_timeline).value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::blueprint::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<blueprint::archetypes::PreviewsConfig> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const blueprint::archetypes::PreviewsConfig& archetype
        );
    };
} // namespace rerun
