// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/blueprint/archetypes/web_page_view_config.fbs".

#pragma once

#include "../../blueprint/components/show_navigation_controls.hpp"
#include "../../blueprint/components/web_page_url.hpp"
#include "../../collection.hpp"
#include "../../component_batch.hpp"
#include "../../component_column.hpp"
#include "../../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::blueprint::archetypes {
    /// **Archetype**: Configuration for the Web Page View.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct WebPageViewConfig {
        /// The initial URL to load.
        std::optional<ComponentBatch> url;

        /// Whether browser navigation controls should be shown.
        ///
        /// Defaults to true.
        std::optional<ComponentBatch> show_navigation_controls;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] =
            "rerun.blueprint.archetypes.WebPageViewConfig";

        /// `ComponentDescriptor` for the `url` field.
        static constexpr auto Descriptor_url = ComponentDescriptor(
            ArchetypeName, "WebPageViewConfig:url",
            Loggable<rerun::blueprint::components::WebPageUrl>::ComponentType
        );
        /// `ComponentDescriptor` for the `show_navigation_controls` field.
        static constexpr auto Descriptor_show_navigation_controls = ComponentDescriptor(
            ArchetypeName, "WebPageViewConfig:show_navigation_controls",
            Loggable<rerun::blueprint::components::ShowNavigationControls>::ComponentType
        );

      public:
        WebPageViewConfig() = default;
        WebPageViewConfig(WebPageViewConfig&& other) = default;
        WebPageViewConfig(const WebPageViewConfig& other) = default;
        WebPageViewConfig& operator=(const WebPageViewConfig& other) = default;
        WebPageViewConfig& operator=(WebPageViewConfig&& other) = default;

        explicit WebPageViewConfig(rerun::blueprint::components::WebPageUrl _url)
            : url(ComponentBatch::from_loggable(std::move(_url), Descriptor_url).value_or_throw()) {
        }

        /// Update only some specific fields of a `WebPageViewConfig`.
        static WebPageViewConfig update_fields() {
            return WebPageViewConfig();
        }

        /// Clear all the fields of a `WebPageViewConfig`.
        static WebPageViewConfig clear_fields();

        /// The initial URL to load.
        WebPageViewConfig with_url(const rerun::blueprint::components::WebPageUrl& _url) && {
            url = ComponentBatch::from_loggable(_url, Descriptor_url).value_or_throw();
            return std::move(*this);
        }

        /// Whether browser navigation controls should be shown.
        ///
        /// Defaults to true.
        WebPageViewConfig with_show_navigation_controls(
            const rerun::blueprint::components::ShowNavigationControls& _show_navigation_controls
        ) && {
            show_navigation_controls = ComponentBatch::from_loggable(
                                           _show_navigation_controls,
                                           Descriptor_show_navigation_controls
            )
                                           .value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::blueprint::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<blueprint::archetypes::WebPageViewConfig> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const blueprint::archetypes::WebPageViewConfig& archetype
        );
    };
} // namespace rerun
