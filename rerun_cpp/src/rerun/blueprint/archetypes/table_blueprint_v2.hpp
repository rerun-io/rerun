// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_blueprint.def.rs".

#pragma once

#include "../../blueprint/components/table_layout_kind.hpp"
#include "../../collection.hpp"
#include "../../component_batch.hpp"
#include "../../component_column.hpp"
#include "../../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::blueprint::archetypes {
    /// **Archetype**: Blueprint for configuring the styling of a table.
    ///
    /// TODO(RR-4810): This is not yet in use!
    ///
    /// The table blueprint as a whole is distributed across these entity paths:
    /// * `/table` for this archetype and `archetypes::PreviewsConfig`.
    /// * `/table/layouts/table` for `archetypes::TableLayout`.
    /// * `/table/layouts/table/columns/{column_name}` for table `archetypes::TableColumn` archetypes and per-column options such as `archetypes::TableColumnPreview`.
    /// * `/table/layouts/cards` for `archetypes::CardLayout`.
    /// * `/table/layouts/cards/fields/{column_name}` for card `archetypes::TableColumn` archetypes and per-field options such as `archetypes::TableColumnPreview`.
    /// * `/view/{view_id}` for preview `archetypes::ViewBlueprint` definitions.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct TableBlueprintV2 {
        /// The layout shown when the table is first opened and no user selection has been persisted.
        ///
        /// If unset, defaults to card layout if available.
        /// `Cards` falls back to table layout when no `archetypes::CardLayout` is configured.
        std::optional<ComponentBatch> default_layout;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.blueprint.archetypes.TableBlueprintV2";

        /// `ComponentDescriptor` for the `default_layout` field.
        static constexpr auto Descriptor_default_layout = ComponentDescriptor(
            ArchetypeName, "TableBlueprintV2:default_layout",
            Loggable<rerun::blueprint::components::TableLayoutKind>::ComponentType
        );

      public:
        TableBlueprintV2() = default;
        TableBlueprintV2(TableBlueprintV2&& other) = default;
        TableBlueprintV2(const TableBlueprintV2& other) = default;
        TableBlueprintV2& operator=(const TableBlueprintV2& other) = default;
        TableBlueprintV2& operator=(TableBlueprintV2&& other) = default;

        /// Update only some specific fields of a `TableBlueprintV2`.
        static TableBlueprintV2 update_fields() {
            return TableBlueprintV2();
        }

        /// Clear all the fields of a `TableBlueprintV2`.
        static TableBlueprintV2 clear_fields();

        /// The layout shown when the table is first opened and no user selection has been persisted.
        ///
        /// If unset, defaults to card layout if available.
        /// `Cards` falls back to table layout when no `archetypes::CardLayout` is configured.
        TableBlueprintV2 with_default_layout(
            const rerun::blueprint::components::TableLayoutKind& _default_layout
        ) && {
            default_layout =
                ComponentBatch::from_loggable(_default_layout, Descriptor_default_layout)
                    .value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::blueprint::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<blueprint::archetypes::TableBlueprintV2> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const blueprint::archetypes::TableBlueprintV2& archetype
        );
    };
} // namespace rerun
