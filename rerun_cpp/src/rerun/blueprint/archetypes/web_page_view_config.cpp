// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/blueprint/archetypes/web_page_view_config.fbs".

#include "web_page_view_config.hpp"

#include "../../collection_adapter_builtins.hpp"

namespace rerun::blueprint::archetypes {
    WebPageViewConfig WebPageViewConfig::clear_fields() {
        auto archetype = WebPageViewConfig();
        archetype.url =
            ComponentBatch::empty<rerun::blueprint::components::WebPageUrl>(Descriptor_url)
                .value_or_throw();
        archetype.show_navigation_controls =
            ComponentBatch::empty<rerun::blueprint::components::ShowNavigationControls>(
                Descriptor_show_navigation_controls
            )
                .value_or_throw();
        return archetype;
    }

    Collection<ComponentColumn> WebPageViewConfig::columns(const Collection<uint32_t>& lengths_) {
        std::vector<ComponentColumn> columns;
        columns.reserve(2);
        if (url.has_value()) {
            columns.push_back(url.value().partitioned(lengths_).value_or_throw());
        }
        if (show_navigation_controls.has_value()) {
            columns.push_back(show_navigation_controls.value().partitioned(lengths_).value_or_throw(
            ));
        }
        return columns;
    }

    Collection<ComponentColumn> WebPageViewConfig::columns() {
        if (url.has_value()) {
            return columns(std::vector<uint32_t>(url.value().length(), 1));
        }
        if (show_navigation_controls.has_value()) {
            return columns(std::vector<uint32_t>(show_navigation_controls.value().length(), 1));
        }
        return Collection<ComponentColumn>();
    }
} // namespace rerun::blueprint::archetypes

namespace rerun {

    Result<Collection<ComponentBatch>>
        AsComponents<blueprint::archetypes::WebPageViewConfig>::as_batches(
            const blueprint::archetypes::WebPageViewConfig& archetype
        ) {
        using namespace blueprint::archetypes;
        std::vector<ComponentBatch> cells;
        cells.reserve(2);

        if (archetype.url.has_value()) {
            cells.push_back(archetype.url.value());
        }
        if (archetype.show_navigation_controls.has_value()) {
            cells.push_back(archetype.show_navigation_controls.value());
        }

        return rerun::take_ownership(std::move(cells));
    }
} // namespace rerun
