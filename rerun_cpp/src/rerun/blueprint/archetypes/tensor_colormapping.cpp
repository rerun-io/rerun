// DO NOT EDIT! This file was auto-generated by crates/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/re_types/definitions/rerun/blueprint/archetypes/tensor_colormapping.fbs".

#include "tensor_colormapping.hpp"

#include "../../collection_adapter_builtins.hpp"

namespace rerun::blueprint::archetypes {}

namespace rerun {

    Result<std::vector<DataCell>>
        AsComponents<blueprint::archetypes::TensorColormapping>::serialize(
            const blueprint::archetypes::TensorColormapping& archetype
        ) {
        using namespace blueprint::archetypes;
        std::vector<DataCell> cells;
        cells.reserve(3);

        if (archetype.colormap.has_value()) {
            auto result = DataCell::from_loggable(archetype.colormap.value());
            RR_RETURN_NOT_OK(result.error);
            cells.push_back(std::move(result.value));
        }
        if (archetype.gamma.has_value()) {
            auto result = DataCell::from_loggable(archetype.gamma.value());
            RR_RETURN_NOT_OK(result.error);
            cells.push_back(std::move(result.value));
        }
        {
            auto indicator = TensorColormapping::IndicatorComponent();
            auto result = DataCell::from_loggable(indicator);
            RR_RETURN_NOT_OK(result.error);
            cells.emplace_back(std::move(result.value));
        }

        return cells;
    }
} // namespace rerun
