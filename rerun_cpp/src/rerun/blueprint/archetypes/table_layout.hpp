// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_layout.def.rs".

#pragma once

#include "../../blueprint/components/column_name.hpp"
#include "../../collection.hpp"
#include "../../component_batch.hpp"
#include "../../component_column.hpp"
#include "../../result.hpp"

#include <cstdint>
#include <optional>
#include <utility>
#include <vector>

namespace rerun::blueprint::archetypes {
    /// **Archetype**: Blueprint for displaying table records as rows and columns.
    ///
    /// This archetype is stored at the entity `/table/layouts/table`.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct TableLayout {
        /// Source columns to show first, in display order.
        ///
        /// Unmentioned columns retain the viewer defaults and follow in default order.
        /// Each source column may appear at most once.
        std::optional<ComponentBatch> column_order;

      public:
        /// The name of the archetype as used in `ComponentDescriptor`s.
        static constexpr const char ArchetypeName[] = "rerun.blueprint.archetypes.TableLayout";

        /// `ComponentDescriptor` for the `column_order` field.
        static constexpr auto Descriptor_column_order = ComponentDescriptor(
            ArchetypeName, "TableLayout:column_order",
            Loggable<rerun::blueprint::components::ColumnName>::ComponentType
        );

      public:
        TableLayout() = default;
        TableLayout(TableLayout&& other) = default;
        TableLayout(const TableLayout& other) = default;
        TableLayout& operator=(const TableLayout& other) = default;
        TableLayout& operator=(TableLayout&& other) = default;

        /// Update only some specific fields of a `TableLayout`.
        static TableLayout update_fields() {
            return TableLayout();
        }

        /// Clear all the fields of a `TableLayout`.
        static TableLayout clear_fields();

        /// Source columns to show first, in display order.
        ///
        /// Unmentioned columns retain the viewer defaults and follow in default order.
        /// Each source column may appear at most once.
        TableLayout with_column_order(
            const Collection<rerun::blueprint::components::ColumnName>& _column_order
        ) && {
            column_order = ComponentBatch::from_loggable(_column_order, Descriptor_column_order)
                               .value_or_throw();
            return std::move(*this);
        }

        /// Partitions the component data into multiple sub-batches.
        ///
        /// Specifically, this transforms the existing `ComponentBatch` data into `ComponentColumn`s
        /// instead, via `ComponentBatch::partitioned`.
        ///
        /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
        ///
        /// The specified `lengths` must sum to the total length of the component batch.
        Collection<ComponentColumn> columns(const Collection<uint32_t>& lengths_);

        /// Partitions the component data into unit-length sub-batches.
        ///
        /// This is semantically similar to calling `columns` with `std::vector<uint32_t>(n, 1)`,
        /// where `n` is automatically guessed.
        Collection<ComponentColumn> columns();
    };

} // namespace rerun::blueprint::archetypes

namespace rerun {
    /// \private
    template <typename T>
    struct AsComponents;

    /// \private
    template <>
    struct AsComponents<blueprint::archetypes::TableLayout> {
        /// Serialize all set component batches.
        static Result<Collection<ComponentBatch>> as_batches(
            const blueprint::archetypes::TableLayout& archetype
        );
    };
} // namespace rerun
