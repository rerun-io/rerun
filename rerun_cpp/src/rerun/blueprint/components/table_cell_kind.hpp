// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/components/table_cell_kind.def.rs".

#pragma once

#include "../../result.hpp"

#include <cstdint>
#include <memory>

namespace arrow {
    /// \private
    template <typename T>
    class NumericBuilder;

    class Array;
    class DataType;
    class UInt8Type;
    using UInt8Builder = NumericBuilder<UInt8Type>;
} // namespace arrow

namespace rerun::blueprint::components {
    /// **Component**: How a table column value is rendered.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    enum class TableCellKind : uint8_t {

        /// Infer the renderer from the component or Arrow datatype.
        Auto = 1,

        /// Render a Rerun URI as an interactive link.
        Link = 2,

        /// Render an image blob as a thumbnail.
        Thumbnail = 3,

        /// Render a boolean as a flag.
        ///
        /// Card layouts place the first flag field in the special flag position and
        /// currently support at most one flag field.
        Flag = 4,

        /// Render a recording reference as embedded views.
        ///
        /// The column must also have `archetypes::TableColumnPreview` configuration with at least one view.
        /// All preview columns use the shared `archetypes::PreviewsConfig`.
        Preview = 5,

        /// Interpret scalar `i32` values as `rerun.cloud.v1alpha1.EntryKind` enum values and render their human-readable names.
        ///
        /// Unknown numeric values are rendered as `Unknown EntryKind`.
        EntryKind = 6,
    };
} // namespace rerun::blueprint::components

namespace rerun {
    template <typename T>
    struct Loggable;

    /// \private
    template <>
    struct Loggable<blueprint::components::TableCellKind> {
        static constexpr std::string_view ComponentType =
            "rerun.blueprint.components.TableCellKind";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_datatype();

        /// Serializes an array of `rerun::blueprint:: components::TableCellKind` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const blueprint::components::TableCellKind* instances, size_t num_instances
        );

        /// Fills an arrow array builder with an array of this type.
        static rerun::Error fill_arrow_array_builder(
            arrow::UInt8Builder* builder, const blueprint::components::TableCellKind* elements,
            size_t num_elements
        );
    };
} // namespace rerun
