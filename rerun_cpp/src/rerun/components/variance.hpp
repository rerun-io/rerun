// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/components/variance.def.rs".

#pragma once

#include "../encodings/float64.hpp"
#include "../result.hpp"

#include <cstdint>
#include <memory>

namespace rerun::components {
    /// **Component**: Variance of a scalar measurement, i.e. σ², in the units of the value squared.
    ///
    /// A value of `0` is a perfectly known value and draws no error band.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct Variance {
        rerun::encodings::Float64 value;

      public:
        Variance() = default;

        Variance(rerun::encodings::Float64 value_) : value(value_) {}

        Variance& operator=(rerun::encodings::Float64 value_) {
            value = value_;
            return *this;
        }

        Variance(double value_) : value(value_) {}

        Variance& operator=(double value_) {
            value = value_;
            return *this;
        }

        /// Cast to the underlying Float64 encoding
        operator rerun::encodings::Float64() const {
            return value;
        }
    };
} // namespace rerun::components

namespace rerun {
    static_assert(sizeof(rerun::encodings::Float64) == sizeof(components::Variance));

    /// \private
    template <>
    struct Loggable<components::Variance> {
        static constexpr std::string_view ComponentType = "rerun.components.Variance";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_data_type() {
            return Loggable<rerun::encodings::Float64>::arrow_data_type();
        }

        /// Serializes an array of `rerun::components::Variance` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const components::Variance* instances, size_t num_instances
        ) {
            if (num_instances == 0) {
                return Loggable<rerun::encodings::Float64>::to_arrow(nullptr, 0);
            } else if (instances == nullptr) {
                return rerun::Error(
                    ErrorCode::UnexpectedNullArgument,
                    "Passed array instances is null when num_elements> 0."
                );
            } else {
                return Loggable<rerun::encodings::Float64>::to_arrow(
                    &instances->value,
                    num_instances
                );
            }
        }
    };
} // namespace rerun
