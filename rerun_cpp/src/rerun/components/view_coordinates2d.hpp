// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/components/view_coordinates2d.fbs".

#pragma once

#include "../datatypes/view_coordinates2d.hpp"
#include "../result.hpp"

#include <cstdint>
#include <memory>

namespace rerun::components {
    /// **Component**: How we interpret the 2D coordinate system of an entity/space.
    ///
    /// For instance: Does the Y axis point up or down?
    ///
    /// The two coordinates are always ordered as [x, y].
    ///
    /// For example [Right, Down] means that the X axis points to the right, and the Y axis points
    /// down. This is the default 2D coordinate system and common image/screen convention.
    ///
    /// The following constants are used to represent the different directions:
    ///  * Up = 1
    ///  * Down = 2
    ///  * Right = 3
    ///  * Left = 4
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct ViewCoordinates2D {
        /// The directions of the [x, y] axes.
        rerun::datatypes::ViewCoordinates2D coordinates;

      public:
        ViewCoordinates2D() = default;

        /// Cast to the underlying ViewCoordinates2D datatype
        operator rerun::datatypes::ViewCoordinates2D() const {
            return coordinates;
        }
    };
} // namespace rerun::components

namespace rerun {
    static_assert(
        sizeof(rerun::datatypes::ViewCoordinates2D) == sizeof(components::ViewCoordinates2D)
    );

    /// \private
    template <>
    struct Loggable<components::ViewCoordinates2D> {
        static constexpr std::string_view ComponentType = "rerun.components.ViewCoordinates2D";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_datatype() {
            return Loggable<rerun::datatypes::ViewCoordinates2D>::arrow_datatype();
        }

        /// Serializes an array of `rerun::components::ViewCoordinates2D` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const components::ViewCoordinates2D* instances, size_t num_instances
        ) {
            if (num_instances == 0) {
                return Loggable<rerun::datatypes::ViewCoordinates2D>::to_arrow(nullptr, 0);
            } else if (instances == nullptr) {
                return rerun::Error(
                    ErrorCode::UnexpectedNullArgument,
                    "Passed array instances is null when num_elements> 0."
                );
            } else {
                return Loggable<rerun::datatypes::ViewCoordinates2D>::to_arrow(
                    &instances->coordinates,
                    num_instances
                );
            }
        }
    };
} // namespace rerun
