// NOTE: This file was autogenerated by re_types_builder; DO NOT EDIT.
// Based on "crates/re_types/definitions/rerun/components/tensor_meaning.fbs"

#include "tensor_meaning.hpp"

#include "../arrow.hpp"
#include "../datatypes/tensor_meaning.hpp"

#include <arrow/api.h>

namespace rerun {
    namespace components {
        const char *TensorMeaning::NAME = "rerun.components.TensorMeaning";

        const std::shared_ptr<arrow::DataType> &TensorMeaning::to_arrow_datatype() {
            static const auto datatype = rerun::datatypes::TensorMeaning::to_arrow_datatype();
            return datatype;
        }

        arrow::Result<std::shared_ptr<arrow::DenseUnionBuilder>>
            TensorMeaning::new_arrow_array_builder(arrow::MemoryPool *memory_pool) {
            if (!memory_pool) {
                return arrow::Status::Invalid("Memory pool is null.");
            }

            return arrow::Result(
                rerun::datatypes::TensorMeaning::new_arrow_array_builder(memory_pool).ValueOrDie()
            );
        }

        arrow::Status TensorMeaning::fill_arrow_array_builder(
            arrow::DenseUnionBuilder *builder, const TensorMeaning *elements, size_t num_elements
        ) {
            if (!builder) {
                return arrow::Status::Invalid("Passed array builder is null.");
            }
            if (!elements) {
                return arrow::Status::Invalid("Cannot serialize null pointer to arrow array.");
            }

            static_assert(sizeof(rerun::datatypes::TensorMeaning) == sizeof(TensorMeaning));
            ARROW_RETURN_NOT_OK(rerun::datatypes::TensorMeaning::fill_arrow_array_builder(
                builder,
                reinterpret_cast<const rerun::datatypes::TensorMeaning *>(elements),
                num_elements
            ));

            return arrow::Status::OK();
        }

        arrow::Result<rerun::DataCell> TensorMeaning::to_data_cell(
            const TensorMeaning *instances, size_t num_instances
        ) {
            // TODO(andreas): Allow configuring the memory pool.
            arrow::MemoryPool *pool = arrow::default_memory_pool();

            ARROW_ASSIGN_OR_RAISE(auto builder, TensorMeaning::new_arrow_array_builder(pool));
            if (instances && num_instances > 0) {
                ARROW_RETURN_NOT_OK(
                    TensorMeaning::fill_arrow_array_builder(builder.get(), instances, num_instances)
                );
            }
            std::shared_ptr<arrow::Array> array;
            ARROW_RETURN_NOT_OK(builder->Finish(&array));

            auto schema = arrow::schema(
                {arrow::field(TensorMeaning::NAME, TensorMeaning::to_arrow_datatype(), false)}
            );

            rerun::DataCell cell;
            cell.component_name = TensorMeaning::NAME;
            ARROW_ASSIGN_OR_RAISE(
                cell.buffer,
                rerun::ipc_from_table(*arrow::Table::Make(schema, {array}))
            );

            return cell;
        }
    } // namespace components
} // namespace rerun
