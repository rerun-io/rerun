// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/build/re_type_definitions/rerun/components/unit.def.rs".

#pragma once

#include "../encodings/utf8.hpp"
#include "../result.hpp"

#include <cstdint>
#include <memory>
#include <string>
#include <utility>

namespace rerun::components {
    /// **Component**: Unit of a measured value, e.g. `"Pa"`, `"lux"`, `"°C"`, `"m"`.
    ///
    /// Used for display only. It does not convert or scale the value.
    ///
    /// ⚠ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    ///
    struct Unit {
        rerun::encodings::Utf8 value;

      public: // START of extensions from unit_ext.cpp:
        /// Construct `Unit` from a null-terminated UTF8 string.
        Unit(const char* str) : value(str) {}

        const char* c_str() const {
            return value.c_str();
        }

        // END of extensions from unit_ext.cpp, start of generated code:

      public:
        Unit() = default;

        Unit(rerun::encodings::Utf8 value_) : value(std::move(value_)) {}

        Unit& operator=(rerun::encodings::Utf8 value_) {
            value = std::move(value_);
            return *this;
        }

        Unit(std::string value_) : value(std::move(value_)) {}

        Unit& operator=(std::string value_) {
            value = std::move(value_);
            return *this;
        }

        /// Cast to the underlying Utf8 encoding
        operator rerun::encodings::Utf8() const {
            return value;
        }
    };
} // namespace rerun::components

namespace rerun {
    static_assert(sizeof(rerun::encodings::Utf8) == sizeof(components::Unit));

    /// \private
    template <>
    struct Loggable<components::Unit> {
        static constexpr std::string_view ComponentType = "rerun.components.Unit";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_data_type() {
            return Loggable<rerun::encodings::Utf8>::arrow_data_type();
        }

        /// Serializes an array of `rerun::components::Unit` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const components::Unit* instances, size_t num_instances
        ) {
            if (num_instances == 0) {
                return Loggable<rerun::encodings::Utf8>::to_arrow(nullptr, 0);
            } else if (instances == nullptr) {
                return rerun::Error(
                    ErrorCode::UnexpectedNullArgument,
                    "Passed array instances is null when num_elements> 0."
                );
            } else {
                return Loggable<rerun::encodings::Utf8>::to_arrow(&instances->value, num_instances);
            }
        }
    };
} // namespace rerun
