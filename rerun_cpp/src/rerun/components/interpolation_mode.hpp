// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/cpp/mod.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/components/interpolation_mode.fbs".

#pragma once

#include "../result.hpp"

#include <cstdint>
#include <memory>

namespace arrow {
    /// \private
    template <typename T>
    class NumericBuilder;

    class Array;
    class DataType;
    class UInt8Type;
    using UInt8Builder = NumericBuilder<UInt8Type>;
} // namespace arrow

namespace rerun::components {
    /// **Component**: Specifies how values between data points are interpolated in a time series line chart.
    enum class InterpolationMode : uint8_t {

        /// Connect data points with straight line segments.
        Linear = 1,

        /// Hold the previous value until the next data point, then jump.
        ///
        /// The step occurs at the end of the interval.
        StepAfter = 2,

        /// Jump to the new value immediately, then hold until the next data point.
        ///
        /// The step occurs at the beginning of the interval.
        StepBefore = 3,

        /// Hold the previous value until the midpoint between data points, then jump to the new value.
        ///
        /// The step occurs at the midpoint of the interval.
        StepMid = 4,
    };
} // namespace rerun::components

namespace rerun {
    template <typename T>
    struct Loggable;

    /// \private
    template <>
    struct Loggable<components::InterpolationMode> {
        static constexpr std::string_view ComponentType = "rerun.components.InterpolationMode";

        /// Returns the arrow data type this type corresponds to.
        static const std::shared_ptr<arrow::DataType>& arrow_datatype();

        /// Serializes an array of `rerun::components::InterpolationMode` into an arrow array.
        static Result<std::shared_ptr<arrow::Array>> to_arrow(
            const components::InterpolationMode* instances, size_t num_instances
        );

        /// Fills an arrow array builder with an array of this type.
        static rerun::Error fill_arrow_array_builder(
            arrow::UInt8Builder* builder, const components::InterpolationMode* elements,
            size_t num_elements
        );
    };
} // namespace rerun
