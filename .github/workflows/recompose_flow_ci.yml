# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
#
# This workflow is generated by recompose. To modify:
#   1. Edit the source flow/automation definition
#   2. Run: ./run generate_gha
#   3. Commit the regenerated file
#
# Source: flow: ci
# ============================================================================
name: ci
on:
  workflow_dispatch: {}
  # TEMPORARY: push trigger for testing on feature branch - remove before merge
  push:
    branches: [jleibs/recompose]
    paths: [recompose/**, .github/workflows/recompose_flow_ci.yml]
jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: recompose
    steps:
    - name: 1_gha.checkout
      uses: actions/checkout@v4
    - name: 2_gha.setup_python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - name: 3_gha.setup_uv
      uses: astral-sh/setup-uv@v4
    - name: Setup workspace
      run: uv run python -m examples.app ci --setup --workspace .recompose
    - name: 4_lint
      run: uv run python -m examples.app ci --step 4_lint --workspace .recompose
    - name: 5_format_check
      run: uv run python -m examples.app ci --step 5_format_check --workspace .recompose
    - name: 6_test
      run: uv run python -m examples.app ci --step 6_test --workspace .recompose
    - name: 7_generate_gha
      run: uv run python -m examples.app ci --step 7_generate_gha --workspace .recompose
