# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
#
# This workflow is generated by recompose. To modify:
#   1. Edit the source flow/automation definition
#   2. Run: ./run generate_gha
#   3. Commit the regenerated file
#
# Source: flow: ci
# ============================================================================
name: ci
on:
  workflow_dispatch: {}
jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: recompose
    env:
      RECOMPOSE_WORKSPACE: .recompose
    steps:
    - name: step_1_gha.checkout
      uses: actions/checkout@v4
    - name: setup_workspace
      run: uv run python -m examples.app _setup --flow ci
    - name: step_2_lint
      run: uv run python -m examples.app _run-step --flow ci --step step_2_lint
    - name: step_3_format_check
      run: uv run python -m examples.app _run-step --flow ci --step 
        step_3_format_check
    - name: step_4_test
      run: uv run python -m examples.app _run-step --flow ci --step step_4_test
    - name: step_5_generate_gha
      run: uv run python -m examples.app _run-step --flow ci --step 
        step_5_generate_gha
