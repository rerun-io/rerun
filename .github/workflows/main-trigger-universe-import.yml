name: Main Trigger Universe Import

# Triggers sync-imports in universe when commits are merged to main.
# This sends a repository_dispatch event to rerun-io/universe.

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  trigger-import:
    runs-on: ubuntu-latest
    steps:
      - name: Create GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ vars.SYNC_APP_ID }}
          private-key: ${{ secrets.SYNC_APP_PRIVATE_KEY }}
          owner: rerun-io
          repositories: |
            universe

      - name: Trigger universe sync-imports workflow
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh api repos/rerun-io/universe/dispatches \
            -f event_type=sync-import \
            -f "client_payload[repo]=rerun"
