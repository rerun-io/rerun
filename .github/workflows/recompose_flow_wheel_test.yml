# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
#
# This workflow is generated by recompose. To modify:
#   1. Edit the source flow/automation definition
#   2. Run: ./run generate_gha
#   3. Commit the regenerated file
#
# Source: flow: wheel_test
# ============================================================================
name: wheel_test
on:
  workflow_dispatch:
    inputs:
      full_tests:
        description: 'Parameter: full_tests'
        required: false
        type: boolean
        default: false
jobs:
  wheel_test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: recompose
    env:
      RECOMPOSE_WORKSPACE: .recompose
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: step_1_setup_workspace
      run: uv run python -m examples.app wheel_test --setup --full_tests ${{ 
        inputs.full_tests }}
    - name: step_2_build_wheel
      run: uv run python -m examples.app wheel_test --step step_2_build_wheel
    - name: step_3_create_test_venv
      run: uv run python -m examples.app wheel_test --step 
        step_3_create_test_venv
    - name: step_4_install_wheel
      run: uv run python -m examples.app wheel_test --step step_4_install_wheel
    - name: step_5_smoke_test
      run: uv run python -m examples.app wheel_test --step step_5_smoke_test
    - name: run_if_1
      id: run_if_1
      run: "# [if: full_tests]\nuv run python -m examples.app wheel_test --step run_if_1"
    - name: step_7_test_installed
      if: ${{ steps.run_if_1.outputs.value == 'true' }}
      run: uv run python -m examples.app wheel_test --step step_7_test_installed
