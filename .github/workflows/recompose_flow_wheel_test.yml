# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
#
# This workflow is generated by recompose. To modify:
#   1. Edit the source flow/automation definition
#   2. Run: ./run generate_gha
#   3. Commit the regenerated file
#
# Source: flow: wheel_test
# ============================================================================
name: wheel_test
on:
  workflow_dispatch:
    inputs:
      full_tests:
        description: 'Parameter: full_tests'
        required: false
        type: boolean
        default: 'false'
jobs:
  wheel_test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: recompose
    env:
      RECOMPOSE_WORKSPACE: .recompose
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: 1_setup_workspace
      run: uv run python -m examples.app wheel_test --setup --full_tests ${{ 
        inputs.full_tests }}
    - name: 2_build_wheel
      run: uv run python -m examples.app wheel_test --step 2_build_wheel
    - name: 3_create_test_venv
      run: uv run python -m examples.app wheel_test --step 3_create_test_venv
    - name: 4_eval_condition
      id: 4_eval_condition
      run: uv run python -m examples.app wheel_test --step 4_eval_condition
    - name: 5_install_wheel
      run: uv run python -m examples.app wheel_test --step 5_install_wheel
    - name: 6_smoke_test
      run: uv run python -m examples.app wheel_test --step 6_smoke_test
    - name: 7_test_installed
      if: ${{ steps.4_eval_condition.outputs.value == 'true' }}
      run: uv run python -m examples.app wheel_test --step 7_test_installed
