# ============================================================================
# GENERATED FILE - DO NOT EDIT MANUALLY
#
# This workflow is generated by recompose. To modify:
#   1. Edit the source flow/automation definition
#   2. Run: ./run generate_gha
#   3. Commit the regenerated file
#
# Source: flow: wheel_test
# ============================================================================
name: wheel_test
on:
  workflow_dispatch:
    inputs:
      full_tests:
        description: 'Parameter: full_tests'
        required: false
        type: boolean
        default: false
jobs:
  wheel_test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: recompose
    env:
      RECOMPOSE_WORKSPACE: .recompose
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: setup_workspace
      run: uv run python -m examples.app _setup --flow wheel_test full_tests=${{
        inputs.full_tests }}
    - name: step_1_build_wheel
      run: uv run python -m examples.app _run-step --flow wheel_test --step 
        step_1_build_wheel
    - name: step_2_create_test_venv
      run: uv run python -m examples.app _run-step --flow wheel_test --step 
        step_2_create_test_venv
    - name: step_3_install_wheel
      run: uv run python -m examples.app _run-step --flow wheel_test --step 
        step_3_install_wheel
    - name: step_4_smoke_test
      run: uv run python -m examples.app _run-step --flow wheel_test --step 
        step_4_smoke_test
    - name: run_if_1
      id: run_if_1
      # [if: full_tests]
      run: uv run python -m examples.app _run-step --flow wheel_test --step 
        run_if_1
    - name: step_6_test_installed
      if: ${{ steps.run_if_1.outputs.value == 'true' }}
      run: uv run python -m examples.app _run-step --flow wheel_test --step 
        step_6_test_installed
