name: Reusable Deploy Landing Preview

# TODO:
# - add ability to wait for deploy to finish
# - create a temporary branch that is based off of `docs-latest`,
#   push it, use that as the `RELEASE_COMMIT` for the preview,
#   then delete the branch

on:
  workflow_call:
    inputs:
      CONCURRENCY:
        required: true
        type: string
      PR_NUMBER:
        required: true
        type: string

concurrency:
  group: ${{ inputs.CONCURRENCY }}-deploy-landing-preview
  cancel-in-progress: true

defaults:
  run:
    shell: bash

permissions:
  contents: "write"
  id-token: "write"
  pull-requests: "write"

jobs:
  deploy:
    name: Deploy

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ (github.event_name == 'pull_request' && github.event.pull_request.head.ref) || '' }}

      - name: Get sha
        id: get-sha
        run: |
          full_commit="${{ (github.event_name == 'pull_request' && github.event.pull_request.head.sha) || github.sha }}"
          echo "sha=$full_commit" >> "$GITHUB_OUTPUT"

      - name: Re-deploy rerun.io
        id: vercel
        uses: ./.github/actions/vercel
        continue-on-error: true
        with:
          command: "deploy"
          vercel_token: ${{ secrets.VERCEL_TOKEN }}
          vercel_team_name: ${{ vars.VERCEL_TEAM_NAME }}
          vercel_project_name: ${{ vars.VERCEL_PROJECT_NAME }}
          release_commit: ${{ steps.get-sha.outputs.sha }}
          target: "preview"

      - name: Create PR comment
        # https://github.com/mshick/add-pr-comment
        uses: mshick/add-pr-comment@v2.8.2
        if: success() && steps.vercel.outputs.vercel_preview_result == 'success'
        with:
          message-id: "vercel-preview"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          message: |
            | Result | Commit  | Link  |
            | ------ | ------- | ----- |
            | ✅ | ${{ steps.get-sha.outputs.sha }} | https://${{ steps.vercel.outputs.vercel_preview_url }}/docs |

            <sub><sup>Note: This comment is updated whenever you push a commit.</sup></sub>

      - name: Create PR comment
        # https://github.com/mshick/add-pr-comment
        uses: mshick/add-pr-comment@v2.8.2
        if: success() && steps.vercel.outputs.vercel_preview_result == 'failure'
        with:
          message-id: "vercel-preview"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          message: |
            Latest preview failed to deploy. See ${{ steps.vercel.outputs.vercel_preview_inspector_url }} for more information.

            | Result | Commit  | Link  |
            | ------ | ------- | ----- |
            | ❌ | ${{ steps.get-sha.outputs.sha }} | unavailable |

            <sub><sup>Note: This comment is updated whenever you push a commit.</sup></sub>

      - name: Create PR comment
        # https://github.com/mshick/add-pr-comment
        uses: mshick/add-pr-comment@v2.8.2
        if: failure()
        with:
          message-id: "vercel-preview"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          message: |
            Latest preview failed to deploy.

            | Result | Commit  | Link  |
            | ------ | ------- | ----- |
            | ❌ | ${{ steps.get-sha.outputs.sha }} | unavailable |

            <sub><sup>Note: This comment is updated whenever you push a commit.</sup></sub>
