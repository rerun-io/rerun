# Rerun Viewer

Rerun Viewer is the user-facing application for inspecting logged multimodal data through configurable views.

## Language

**Web Page View**:
A native-only Rerun view that displays a configured webpage inline as part of the viewer layout.
_Avoid_: WebView, link view, browser view
