# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/testing/components/enum_test.fbs".

# You can extend this class by creating a "WideEnumExt" class in "wide_enum_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa
from rerun._baseclasses import (
    BaseBatch,
)

__all__ = ["WideEnum", "WideEnumArrayLike", "WideEnumBatch", "WideEnumLike"]


from enum import Enum


class WideEnum(Enum):
    """**Datatype**: A test enum with values that require more than one byte."""

    Low = 0x1
    """Low value."""

    High = 0x10000
    """High value."""

    @classmethod
    def auto(cls, val: str | int | WideEnum) -> WideEnum:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, WideEnum):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


WideEnumLike = WideEnum | Literal["High", "Low", "high", "low"] | int
"""A type alias for any WideEnum-like object."""

WideEnumArrayLike = WideEnum | Literal["High", "Low", "high", "low"] | int | Sequence[WideEnumLike]
"""A type alias for any WideEnum-like array object."""


class WideEnumBatch(BaseBatch[WideEnumArrayLike]):
    _ARROW_DATATYPE = pa.uint32()

    @staticmethod
    def _native_to_pa_array(data: WideEnumArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (WideEnum, int, str)):
            data = [data]

        pa_data = [WideEnum.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]  # ty: ignore[not-iterable]

        return pa.array(pa_data, type=data_type)
