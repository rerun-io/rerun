# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/card_layout.def.rs".

# You can extend this class by creating a "CardLayoutExt" class in "card_layout_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

from attrs import define, field

from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ... import encodings

__all__ = ["CardLayout"]


@define(str=False, repr=False, init=False)
class CardLayout(Archetype):
    """
    **Archetype**: Blueprint for displaying table records as cards.

    This archetype is stored at the entity `/table/layouts/cards`.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.CardLayout"

    def __init__(
        self: Any,
        field_order: encodings.Utf8ArrayLike,
        *,
        title: encodings.Utf8Like | None = None,
        link: encodings.Utf8Like | None = None,
    ) -> None:
        """
        Create a new instance of the CardLayout archetype.

        Parameters
        ----------
        title:
            The source column used for card titles.

            If unset, the first visible string column is used as the title.
        link:
            The source column containing the target opened when a card is activated.

            If unset, the first configured preview field is used, then the first inferred URL column.
        field_order:
            Source columns visible by default in each card, in display order.

            Unlisted fields are hidden unless their [`archetypes.TableColumn`][rerun.blueprint.archetypes.TableColumn] visibility overrides the default.
            Each source column may appear at most once.

            Fields with `TableCellKind::Flag` are omitted from the labeled-field list and the first one is shown in the card header.
            Card layouts currently support at most one flag field.

        """

        # You can define your own __init__ function as a member of CardLayoutExt in card_layout_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(title=title, link=link, field_order=field_order)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            title=None,
            link=None,
            field_order=None,
        )

    @classmethod
    def _clear(cls) -> CardLayout:
        """Produce an empty CardLayout, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        title: encodings.Utf8Like | None = None,
        link: encodings.Utf8Like | None = None,
        field_order: encodings.Utf8ArrayLike | None = None,
    ) -> CardLayout:
        """
        Update only some specific fields of a `CardLayout`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        title:
            The source column used for card titles.

            If unset, the first visible string column is used as the title.
        link:
            The source column containing the target opened when a card is activated.

            If unset, the first configured preview field is used, then the first inferred URL column.
        field_order:
            Source columns visible by default in each card, in display order.

            Unlisted fields are hidden unless their [`archetypes.TableColumn`][rerun.blueprint.archetypes.TableColumn] visibility overrides the default.
            Each source column may appear at most once.

            Fields with `TableCellKind::Flag` are omitted from the labeled-field list and the first one is shown in the card header.
            Card layouts currently support at most one flag field.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "title": title,
                "link": link,
                "field_order": field_order,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> CardLayout:
        """Clear all the fields of a `CardLayout`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_title() -> ComponentDescriptor:
        return ComponentDescriptor(
            "CardLayout:title",
            archetype=CardLayout.NAME,
            component_type=blueprint_components.ColumnNameBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_link() -> ComponentDescriptor:
        return ComponentDescriptor(
            "CardLayout:link",
            archetype=CardLayout.NAME,
            component_type=blueprint_components.ColumnNameBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_field_order() -> ComponentDescriptor:
        return ComponentDescriptor(
            "CardLayout:field_order",
            archetype=CardLayout.NAME,
            component_type=blueprint_components.ColumnNameBatch._COMPONENT_TYPE,
        )

    field_order: blueprint_components.ColumnNameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.ColumnNameBatch._converter,  # type: ignore[misc]
    )
    # Source columns visible by default in each card, in display order.
    #
    # Unlisted fields are hidden unless their [`archetypes.TableColumn`][rerun.blueprint.archetypes.TableColumn] visibility overrides the default.
    # Each source column may appear at most once.
    #
    # Fields with `TableCellKind::Flag` are omitted from the labeled-field list and the first one is shown in the card header.
    # Card layouts currently support at most one flag field.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    title: blueprint_components.ColumnNameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.ColumnNameBatch._converter,  # type: ignore[misc]
    )
    # The source column used for card titles.
    #
    # If unset, the first visible string column is used as the title.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    link: blueprint_components.ColumnNameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.ColumnNameBatch._converter,  # type: ignore[misc]
    )
    # The source column containing the target opened when a card is activated.
    #
    # If unset, the first configured preview field is used, then the first inferred URL column.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
