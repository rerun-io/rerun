# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_layout.def.rs".

# You can extend this class by creating a "TableLayoutExt" class in "table_layout_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

from attrs import define, field

from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ... import encodings

__all__ = ["TableLayout"]


@define(str=False, repr=False, init=False)
class TableLayout(Archetype):
    """
    **Archetype**: Blueprint for displaying table records as rows and columns.

    This archetype is stored at the entity `/table/layouts/table`.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.TableLayout"

    def __init__(self: Any, *, column_order: encodings.Utf8ArrayLike | None = None) -> None:
        """
        Create a new instance of the TableLayout archetype.

        Parameters
        ----------
        column_order:
            Source columns to show first, in display order.

            Unmentioned columns retain the viewer defaults and follow in default order.
            Each source column may appear at most once.

        """

        # You can define your own __init__ function as a member of TableLayoutExt in table_layout_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(column_order=column_order)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            column_order=None,
        )

    @classmethod
    def _clear(cls) -> TableLayout:
        """Produce an empty TableLayout, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        column_order: encodings.Utf8ArrayLike | None = None,
    ) -> TableLayout:
        """
        Update only some specific fields of a `TableLayout`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        column_order:
            Source columns to show first, in display order.

            Unmentioned columns retain the viewer defaults and follow in default order.
            Each source column may appear at most once.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "column_order": column_order,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> TableLayout:
        """Clear all the fields of a `TableLayout`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_column_order() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TableLayout:column_order",
            archetype=TableLayout.NAME,
            component_type=blueprint_components.ColumnNameBatch._COMPONENT_TYPE,
        )

    column_order: blueprint_components.ColumnNameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.ColumnNameBatch._converter,  # type: ignore[misc]
    )
    # Source columns to show first, in display order.
    #
    # Unmentioned columns retain the viewer defaults and follow in default order.
    # Each source column may appear at most once.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
