# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_column.def.rs".

# You can extend this class by creating a "TableColumnExt" class in "table_column_ext.py".

from __future__ import annotations

from typing import Any, ClassVar

from attrs import define, field

from ... import components, encodings
from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

__all__ = ["TableColumn"]


@define(str=False, repr=False, init=False)
class TableColumn(Archetype):
    """
    **Archetype**: Blueprint for a column used by a table or card layout.

    This archetype is stored at the layout-specific path for its source column.
    The source is the final [`components.ColumnName`][rerun.blueprint.components.ColumnName] part of that path.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.TableColumn"

    def __init__(
        self: Any,
        *,
        name: encodings.Utf8Like | None = None,
        editable: encodings.BoolLike | None = None,
        visible: encodings.BoolLike | None = None,
        cell_kind: blueprint_components.TableCellKindLike | None = None,
    ) -> None:
        """
        Create a new instance of the TableColumn archetype.

        Parameters
        ----------
        name:
            The name shown for the column.

            If unset, the name is inferred from the source column.
        editable:
            Whether the column's values can be edited.

            If unset, editing is disabled.
            Edits requires a remote table with a column marked by `rerun:is_table_index` metadata and write permission.
            ⚠ Currently only boolean values are supported.
        visible:
            Whether the column is visible in this layout.

            If unset, the enclosing layout determines visibility.
            Table layouts use the viewer default for the source column.
            Card layouts show sources listed in `field_order` and hide unlisted sources.
        cell_kind:
            How to render the column's values.

            If unset or `Auto`, the viewer infers the renderer from the component or Arrow datatype.

        """

        # You can define your own __init__ function as a member of TableColumnExt in table_column_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(name=name, editable=editable, visible=visible, cell_kind=cell_kind)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            name=None,
            editable=None,
            visible=None,
            cell_kind=None,
        )

    @classmethod
    def _clear(cls) -> TableColumn:
        """Produce an empty TableColumn, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        name: encodings.Utf8Like | None = None,
        editable: encodings.BoolLike | None = None,
        visible: encodings.BoolLike | None = None,
        cell_kind: blueprint_components.TableCellKindLike | None = None,
    ) -> TableColumn:
        """
        Update only some specific fields of a `TableColumn`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        name:
            The name shown for the column.

            If unset, the name is inferred from the source column.
        editable:
            Whether the column's values can be edited.

            If unset, editing is disabled.
            Edits requires a remote table with a column marked by `rerun:is_table_index` metadata and write permission.
            ⚠ Currently only boolean values are supported.
        visible:
            Whether the column is visible in this layout.

            If unset, the enclosing layout determines visibility.
            Table layouts use the viewer default for the source column.
            Card layouts show sources listed in `field_order` and hide unlisted sources.
        cell_kind:
            How to render the column's values.

            If unset or `Auto`, the viewer infers the renderer from the component or Arrow datatype.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "name": name,
                "editable": editable,
                "visible": visible,
                "cell_kind": cell_kind,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> TableColumn:
        """Clear all the fields of a `TableColumn`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_name() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TableColumn:name",
            archetype=TableColumn.NAME,
            component_type=components.NameBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_editable() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TableColumn:editable",
            archetype=TableColumn.NAME,
            component_type=blueprint_components.EditableBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_visible() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TableColumn:visible",
            archetype=TableColumn.NAME,
            component_type=components.VisibleBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_cell_kind() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TableColumn:cell_kind",
            archetype=TableColumn.NAME,
            component_type=blueprint_components.TableCellKindBatch._COMPONENT_TYPE,
        )

    name: components.NameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.NameBatch._converter,  # type: ignore[misc]
    )
    # The name shown for the column.
    #
    # If unset, the name is inferred from the source column.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    editable: blueprint_components.EditableBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.EditableBatch._converter,  # type: ignore[misc]
    )
    # Whether the column's values can be edited.
    #
    # If unset, editing is disabled.
    # Edits requires a remote table with a column marked by `rerun:is_table_index` metadata and write permission.
    # ⚠ Currently only boolean values are supported.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    visible: components.VisibleBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VisibleBatch._converter,  # type: ignore[misc]
    )
    # Whether the column is visible in this layout.
    #
    # If unset, the enclosing layout determines visibility.
    # Table layouts use the viewer default for the source column.
    # Card layouts show sources listed in `field_order` and hide unlisted sources.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    cell_kind: blueprint_components.TableCellKindBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.TableCellKindBatch._converter,  # type: ignore[misc]
    )
    # How to render the column's values.
    #
    # If unset or `Auto`, the viewer infers the renderer from the component or Arrow datatype.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
