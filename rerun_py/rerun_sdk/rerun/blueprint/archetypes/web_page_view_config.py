# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/blueprint/archetypes/web_page_view_config.fbs".

# You can extend this class by creating a "WebPageViewConfigExt" class in "web_page_view_config_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

from attrs import define, field

from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ... import datatypes

__all__ = ["WebPageViewConfig"]


@define(str=False, repr=False, init=False)
class WebPageViewConfig(Archetype):
    """
    **Archetype**: Configuration for the Web Page View.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.WebPageViewConfig"

    def __init__(
        self: Any, url: datatypes.Utf8Like, *, show_navigation_controls: datatypes.BoolLike | None = None
    ) -> None:
        """
        Create a new instance of the WebPageViewConfig archetype.

        Parameters
        ----------
        url:
            The initial URL to load.
        show_navigation_controls:
            Whether browser navigation controls should be shown.

            Defaults to true.

        """

        # You can define your own __init__ function as a member of WebPageViewConfigExt in web_page_view_config_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(url=url, show_navigation_controls=show_navigation_controls)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            url=None,
            show_navigation_controls=None,
        )

    @classmethod
    def _clear(cls) -> WebPageViewConfig:
        """Produce an empty WebPageViewConfig, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        url: datatypes.Utf8Like | None = None,
        show_navigation_controls: datatypes.BoolLike | None = None,
    ) -> WebPageViewConfig:
        """
        Update only some specific fields of a `WebPageViewConfig`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        url:
            The initial URL to load.
        show_navigation_controls:
            Whether browser navigation controls should be shown.

            Defaults to true.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "url": url,
                "show_navigation_controls": show_navigation_controls,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> WebPageViewConfig:
        """Clear all the fields of a `WebPageViewConfig`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_url() -> ComponentDescriptor:
        return ComponentDescriptor(
            "WebPageViewConfig:url",
            archetype=WebPageViewConfig.NAME,
            component_type=blueprint_components.WebPageUrlBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_show_navigation_controls() -> ComponentDescriptor:
        return ComponentDescriptor(
            "WebPageViewConfig:show_navigation_controls",
            archetype=WebPageViewConfig.NAME,
            component_type=blueprint_components.ShowNavigationControlsBatch._COMPONENT_TYPE,
        )

    url: blueprint_components.WebPageUrlBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.WebPageUrlBatch._converter,  # type: ignore[misc]
    )
    # The initial URL to load.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    show_navigation_controls: blueprint_components.ShowNavigationControlsBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.ShowNavigationControlsBatch._converter,  # type: ignore[misc]
    )
    # Whether browser navigation controls should be shown.
    #
    # Defaults to true.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
