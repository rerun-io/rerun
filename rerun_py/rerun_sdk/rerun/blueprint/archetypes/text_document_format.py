# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/blueprint/archetypes/text_document_format.fbs".

# You can extend this class by creating a "TextDocumentFormatExt" class in "text_document_format_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

from attrs import define, field

from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ... import datatypes

__all__ = ["TextDocumentFormat"]


@define(str=False, repr=False, init=False)
class TextDocumentFormat(Archetype):
    """
    **Archetype**: Formatting options for the text document view.

    These options only apply to plain text documents and have no effect on Markdown documents.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.TextDocumentFormat"

    def __init__(
        self: Any, *, monospace: datatypes.BoolLike | None = None, word_wrap: datatypes.BoolLike | None = None
    ) -> None:
        """
        Create a new instance of the TextDocumentFormat archetype.

        Parameters
        ----------
        monospace:
            Whether to use a monospace font for the document body.

            Defaults to disabled.
        word_wrap:
            Whether to wrap long lines in the document body.

            Defaults to enabled.

        """

        # You can define your own __init__ function as a member of TextDocumentFormatExt in text_document_format_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(monospace=monospace, word_wrap=word_wrap)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            monospace=None,
            word_wrap=None,
        )

    @classmethod
    def _clear(cls) -> TextDocumentFormat:
        """Produce an empty TextDocumentFormat, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        monospace: datatypes.BoolLike | None = None,
        word_wrap: datatypes.BoolLike | None = None,
    ) -> TextDocumentFormat:
        """
        Update only some specific fields of a `TextDocumentFormat`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        monospace:
            Whether to use a monospace font for the document body.

            Defaults to disabled.
        word_wrap:
            Whether to wrap long lines in the document body.

            Defaults to enabled.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "monospace": monospace,
                "word_wrap": word_wrap,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> TextDocumentFormat:
        """Clear all the fields of a `TextDocumentFormat`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_monospace() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TextDocumentFormat:monospace",
            archetype=TextDocumentFormat.NAME,
            component_type=blueprint_components.EnabledBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_word_wrap() -> ComponentDescriptor:
        return ComponentDescriptor(
            "TextDocumentFormat:word_wrap",
            archetype=TextDocumentFormat.NAME,
            component_type=blueprint_components.EnabledBatch._COMPONENT_TYPE,
        )

    monospace: blueprint_components.EnabledBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.EnabledBatch._converter,  # type: ignore[misc]
    )
    # Whether to use a monospace font for the document body.
    #
    # Defaults to disabled.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    word_wrap: blueprint_components.EnabledBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.EnabledBatch._converter,  # type: ignore[misc]
    )
    # Whether to wrap long lines in the document body.
    #
    # Defaults to enabled.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
