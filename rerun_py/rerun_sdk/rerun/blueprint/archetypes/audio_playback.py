# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/audio_playback.def.rs".

# You can extend this class by creating a "AudioPlaybackExt" class in "audio_playback_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

from attrs import define, field

from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ... import encodings

__all__ = ["AudioPlayback"]


@define(str=False, repr=False, init=False)
class AudioPlayback(Archetype):
    """
    **Archetype**: Playback settings for an audio view.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.AudioPlayback"

    def __init__(self: Any, *, volume: encodings.Float32Like | None = None) -> None:
        """
        Create a new instance of the AudioPlayback archetype.

        Parameters
        ----------
        volume:
            Playback volume, from 0.0 (silent) to 1.0 (full).

            Defaults to 1.0.

        """

        # You can define your own __init__ function as a member of AudioPlaybackExt in audio_playback_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(volume=volume)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            volume=None,
        )

    @classmethod
    def _clear(cls) -> AudioPlayback:
        """Produce an empty AudioPlayback, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        volume: encodings.Float32Like | None = None,
    ) -> AudioPlayback:
        """
        Update only some specific fields of a `AudioPlayback`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        volume:
            Playback volume, from 0.0 (silent) to 1.0 (full).

            Defaults to 1.0.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "volume": volume,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> AudioPlayback:
        """Clear all the fields of a `AudioPlayback`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_volume() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioPlayback:volume",
            archetype=AudioPlayback.NAME,
            component_type=blueprint_components.VolumeBatch._COMPONENT_TYPE,
        )

    volume: blueprint_components.VolumeBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.VolumeBatch._converter,  # type: ignore[misc]
    )
    # Playback volume, from 0.0 (silent) to 1.0 (full).
    #
    # Defaults to 1.0.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
