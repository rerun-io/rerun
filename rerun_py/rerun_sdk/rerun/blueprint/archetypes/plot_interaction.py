# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/plot_interaction.def.rs".

# You can extend this class by creating a "PlotInteractionExt" class in "plot_interaction_ext.py".

from __future__ import annotations

from typing import Any, ClassVar

from attrs import define, field

from ..._baseclasses import (
    Archetype,
    ComponentDescriptor,
)
from ...blueprint import components as blueprint_components
from ...error_utils import catch_and_log_exceptions

__all__ = ["PlotInteraction"]


@define(str=False, repr=False, init=False)
class PlotInteraction(Archetype):
    """
    **Archetype**: Configuration for plot interaction behavior.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    NAME: ClassVar[str] = "rerun.blueprint.archetypes.PlotInteraction"

    def __init__(
        self: Any,
        *,
        tooltip_mode: blueprint_components.TooltipModeLike | None = None,
        points_display: blueprint_components.PointsDisplayLike | None = None,
    ) -> None:
        """
        Create a new instance of the PlotInteraction archetype.

        Parameters
        ----------
        tooltip_mode:
            How the tooltip behaves when hovering over the plot.
        points_display:
            When data point markers are displayed on line series.

        """

        # You can define your own __init__ function as a member of PlotInteractionExt in plot_interaction_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(tooltip_mode=tooltip_mode, points_display=points_display)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            tooltip_mode=None,
            points_display=None,
        )

    @classmethod
    def _clear(cls) -> PlotInteraction:
        """Produce an empty PlotInteraction, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        tooltip_mode: blueprint_components.TooltipModeLike | None = None,
        points_display: blueprint_components.PointsDisplayLike | None = None,
    ) -> PlotInteraction:
        """
        Update only some specific fields of a `PlotInteraction`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        tooltip_mode:
            How the tooltip behaves when hovering over the plot.
        points_display:
            When data point markers are displayed on line series.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "tooltip_mode": tooltip_mode,
                "points_display": points_display,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> PlotInteraction:
        """Clear all the fields of a `PlotInteraction`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_tooltip_mode() -> ComponentDescriptor:
        return ComponentDescriptor(
            "PlotInteraction:tooltip_mode",
            archetype=PlotInteraction.NAME,
            component_type=blueprint_components.TooltipModeBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_points_display() -> ComponentDescriptor:
        return ComponentDescriptor(
            "PlotInteraction:points_display",
            archetype=PlotInteraction.NAME,
            component_type=blueprint_components.PointsDisplayBatch._COMPONENT_TYPE,
        )

    tooltip_mode: blueprint_components.TooltipModeBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.TooltipModeBatch._converter,  # type: ignore[misc]
    )
    # How the tooltip behaves when hovering over the plot.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    points_display: blueprint_components.PointsDisplayBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=blueprint_components.PointsDisplayBatch._converter,  # type: ignore[misc]
    )
    # When data point markers are displayed on line series.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
