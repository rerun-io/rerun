# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/encodings/timeline_column.def.rs".

# You can extend this class by creating a "TimelineColumnExt" class in "timeline_column_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any

import pyarrow as pa
from attrs import define, field

from ... import encodings
from ..._baseclasses import (
    BaseBatch,
)
from .timeline_column_ext import TimelineColumnExt

__all__ = ["TimelineColumn", "TimelineColumnArrayLike", "TimelineColumnBatch", "TimelineColumnLike"]


def _timeline_column__visible__special_field_converter_override(x: encodings.BoolLike) -> encodings.Bool:
    if isinstance(x, encodings.Bool):
        return x
    else:
        return encodings.Bool(x)


def _timeline_column__timeline__special_field_converter_override(x: encodings.Utf8Like) -> encodings.Utf8:
    if isinstance(x, encodings.Utf8):
        return x
    else:
        return encodings.Utf8(x)


@define(init=False)
class TimelineColumn(TimelineColumnExt):
    """
    **Encoding**: A timeline column in a table.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    # __init__ can be found in timeline_column_ext.py

    visible: encodings.Bool = field(converter=_timeline_column__visible__special_field_converter_override)
    # Is this column visible?
    #
    # Defaults to true.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    timeline: encodings.Utf8 = field(converter=_timeline_column__timeline__special_field_converter_override)
    # Which timeline is this?
    #
    # (Docstring intentionally commented out to hide this field from the docs)


if TYPE_CHECKING:
    TimelineColumnLike = TimelineColumn | encodings.Utf8Like
    """A type alias for any TimelineColumn-like object."""
else:
    TimelineColumnLike = Any

TimelineColumnArrayLike = TimelineColumn | Sequence[TimelineColumnLike]
"""A type alias for any TimelineColumn-like array object."""


class TimelineColumnBatch(BaseBatch[TimelineColumnArrayLike]):
    _ARROW_DATATYPE = pa.struct([
        pa.field("visible", pa.bool_(), nullable=False, metadata={}),
        pa.field("timeline", pa.utf8(), nullable=False, metadata={}),
    ])

    @staticmethod
    def _native_to_pa_array(data: TimelineColumnArrayLike, data_type: pa.DataType) -> pa.Array:
        from rerun.encodings import BoolBatch, Utf8Batch

        typed_data: Sequence[TimelineColumn]

        if isinstance(data, TimelineColumn):
            typed_data = [data]
        else:
            typed_data = [x if isinstance(x, TimelineColumn) else TimelineColumn(x) for x in data]

        return pa.StructArray.from_arrays(
            [
                BoolBatch([x.visible for x in typed_data]).as_arrow_array(),  # type: ignore[misc, arg-type]
                Utf8Batch([x.timeline for x in typed_data]).as_arrow_array(),  # type: ignore[misc, arg-type]
            ],
            fields=list(data_type),
        )
