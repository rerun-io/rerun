# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/components/table_layout_kind.def.rs".

# You can extend this class by creating a "TableLayoutKindExt" class in "table_layout_kind_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa

from ..._baseclasses import (
    BaseBatch,
    ComponentBatchMixin,
)

__all__ = ["TableLayoutKind", "TableLayoutKindArrayLike", "TableLayoutKindBatch", "TableLayoutKindLike"]


from enum import Enum


class TableLayoutKind(Enum):
    """
    **Component**: How table records are presented.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    Table = 1
    """Display records using [`archetypes.TableLayout`][rerun.blueprint.archetypes.TableLayout]."""

    Cards = 2
    """Display records using [`archetypes.CardLayout`][rerun.blueprint.archetypes.CardLayout]."""

    @classmethod
    def auto(cls, val: str | int | TableLayoutKind) -> TableLayoutKind:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, TableLayoutKind):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


TableLayoutKindLike = TableLayoutKind | Literal["Cards", "Table", "cards", "table"] | int
"""A type alias for any TableLayoutKind-like object."""

TableLayoutKindArrayLike = (
    TableLayoutKind | Literal["Cards", "Table", "cards", "table"] | int | Sequence[TableLayoutKindLike]
)
"""A type alias for any TableLayoutKind-like array object."""


class TableLayoutKindBatch(BaseBatch[TableLayoutKindArrayLike], ComponentBatchMixin):
    _ARROW_DATATYPE = pa.uint8()
    _COMPONENT_TYPE: str = "rerun.blueprint.components.TableLayoutKind"

    @staticmethod
    def _native_to_pa_array(data: TableLayoutKindArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (TableLayoutKind, int, str)):
            data = [data]

        pa_data = [TableLayoutKind.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]  # ty: ignore[not-iterable]

        return pa.array(pa_data, type=data_type)
