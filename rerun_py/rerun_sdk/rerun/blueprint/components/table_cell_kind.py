# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/components/table_cell_kind.def.rs".

# You can extend this class by creating a "TableCellKindExt" class in "table_cell_kind_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa

from ..._baseclasses import (
    BaseBatch,
    ComponentBatchMixin,
)

__all__ = ["TableCellKind", "TableCellKindArrayLike", "TableCellKindBatch", "TableCellKindLike"]


from enum import Enum


class TableCellKind(Enum):
    """
    **Component**: How a table column value is rendered.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    Auto = 1
    """Infer the renderer from the component or Arrow datatype."""

    Link = 2
    """Render a Rerun URI as an interactive link."""

    Thumbnail = 3
    """Render an image blob as a thumbnail."""

    Flag = 4
    """
    Render a boolean as a flag.

    Card layouts place the first flag field in the special flag position and
    currently support at most one flag field.
    """

    Preview = 5
    """
    Render a recording reference as embedded views.

    The column must also have [`archetypes.TableColumnPreview`][rerun.blueprint.archetypes.TableColumnPreview] configuration with at least one view.
    All preview columns use the shared [`archetypes.PreviewsConfig`][rerun.blueprint.archetypes.PreviewsConfig].
    """

    EntryKind = 6
    """
    Interpret scalar `i32` values as `rerun.cloud.v1alpha1.EntryKind` enum values and render their human-readable names.

    Unknown numeric values are rendered as `Unknown EntryKind`.
    """

    @classmethod
    def auto(cls, val: str | int | TableCellKind) -> TableCellKind:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, TableCellKind):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


TableCellKindLike = (
    TableCellKind
    | Literal[
        "Auto",
        "EntryKind",
        "Flag",
        "Link",
        "Preview",
        "Thumbnail",
        "auto",
        "entrykind",
        "flag",
        "link",
        "preview",
        "thumbnail",
    ]
    | int
)
"""A type alias for any TableCellKind-like object."""

TableCellKindArrayLike = (
    TableCellKind
    | Literal[
        "Auto",
        "EntryKind",
        "Flag",
        "Link",
        "Preview",
        "Thumbnail",
        "auto",
        "entrykind",
        "flag",
        "link",
        "preview",
        "thumbnail",
    ]
    | int
    | Sequence[TableCellKindLike]
)
"""A type alias for any TableCellKind-like array object."""


class TableCellKindBatch(BaseBatch[TableCellKindArrayLike], ComponentBatchMixin):
    _ARROW_DATATYPE = pa.uint8()
    _COMPONENT_TYPE: str = "rerun.blueprint.components.TableCellKind"

    @staticmethod
    def _native_to_pa_array(data: TableCellKindArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (TableCellKind, int, str)):
            data = [data]

        pa_data = [TableCellKind.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]  # ty: ignore[not-iterable]

        return pa.array(pa_data, type=data_type)
