# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/components/tooltip_mode.def.rs".

# You can extend this class by creating a "TooltipModeExt" class in "tooltip_mode_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa

from ..._baseclasses import (
    BaseBatch,
    ComponentBatchMixin,
)

__all__ = ["TooltipMode", "TooltipModeArrayLike", "TooltipModeBatch", "TooltipModeLike"]


from enum import Enum


class TooltipMode(Enum):
    """
    **Component**: Controls how the plot tooltip behaves when hovering over data.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    Nearest = 1
    """Show only the nearest data point to the cursor."""

    All = 2
    """Show values of all visible series at the hovered time."""

    @classmethod
    def auto(cls, val: str | int | TooltipMode) -> TooltipMode:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, TooltipMode):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


TooltipModeLike = TooltipMode | Literal["All", "Nearest", "all", "nearest"] | int
"""A type alias for any TooltipMode-like object."""

TooltipModeArrayLike = TooltipMode | Literal["All", "Nearest", "all", "nearest"] | int | Sequence[TooltipModeLike]
"""A type alias for any TooltipMode-like array object."""


class TooltipModeBatch(BaseBatch[TooltipModeArrayLike], ComponentBatchMixin):
    _ARROW_DATATYPE = pa.uint8()
    _COMPONENT_TYPE: str = "rerun.blueprint.components.TooltipMode"

    @staticmethod
    def _native_to_pa_array(data: TooltipModeArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (TooltipMode, int, str)):
            data = [data]

        pa_data = [TooltipMode.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]  # ty: ignore[not-iterable]

        return pa.array(pa_data, type=data_type)
