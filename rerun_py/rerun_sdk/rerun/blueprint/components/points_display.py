# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/blueprint/components/points_display.def.rs".

# You can extend this class by creating a "PointsDisplayExt" class in "points_display_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa

from ..._baseclasses import (
    BaseBatch,
    ComponentBatchMixin,
)

__all__ = ["PointsDisplay", "PointsDisplayArrayLike", "PointsDisplayBatch", "PointsDisplayLike"]


from enum import Enum


class PointsDisplay(Enum):
    """
    **Component**: Controls when data point markers are displayed on line series.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    OnHover = 1
    """Show data point markers only when hovering near them."""

    Always = 2
    """Always show data point markers on non-aggregated line series."""

    @classmethod
    def auto(cls, val: str | int | PointsDisplay) -> PointsDisplay:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, PointsDisplay):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


PointsDisplayLike = PointsDisplay | Literal["Always", "OnHover", "always", "onhover"] | int
"""A type alias for any PointsDisplay-like object."""

PointsDisplayArrayLike = (
    PointsDisplay | Literal["Always", "OnHover", "always", "onhover"] | int | Sequence[PointsDisplayLike]
)
"""A type alias for any PointsDisplay-like array object."""


class PointsDisplayBatch(BaseBatch[PointsDisplayArrayLike], ComponentBatchMixin):
    _ARROW_DATATYPE = pa.uint8()
    _COMPONENT_TYPE: str = "rerun.blueprint.components.PointsDisplay"

    @staticmethod
    def _native_to_pa_array(data: PointsDisplayArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (PointsDisplay, int, str)):
            data = [data]

        pa_data = [PointsDisplay.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]  # ty: ignore[not-iterable]

        return pa.array(pa_data, type=data_type)
