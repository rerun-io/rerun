# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_types/definitions/rerun/blueprint/datatypes/force_position_y.fbs".

# You can extend this class by creating a "ForcePositionYExt" class in "force_position_y_ext.py".

from __future__ import annotations

from typing import Any, Sequence, Union

import numpy as np
import pyarrow as pa
from attrs import define, field

from ..._baseclasses import (
    BaseBatch,
)

__all__ = ["ForcePositionY", "ForcePositionYArrayLike", "ForcePositionYBatch", "ForcePositionYLike"]


@define(init=False)
class ForcePositionY:
    """**Datatype**: Defines a force that pulls nodes towards an `y` position."""

    def __init__(self: Any, enabled: bool, y: float, strength: float):
        """
        Create a new instance of the ForcePositionY datatype.

        Parameters
        ----------
        enabled:
            Whether the force is enabled.
        y:
            The `y` position to pull nodes towards.
        strength:
            The strength of the force.

        """

        # You can define your own __init__ function as a member of ForcePositionYExt in force_position_y_ext.py
        self.__attrs_init__(enabled=enabled, y=y, strength=strength)

    enabled: bool = field(converter=bool)
    # Whether the force is enabled.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    y: float = field(converter=float)
    # The `y` position to pull nodes towards.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    strength: float = field(converter=float)
    # The strength of the force.
    #
    # (Docstring intentionally commented out to hide this field from the docs)


ForcePositionYLike = ForcePositionY
ForcePositionYArrayLike = Union[
    ForcePositionY,
    Sequence[ForcePositionYLike],
]


class ForcePositionYBatch(BaseBatch[ForcePositionYArrayLike]):
    _ARROW_DATATYPE = pa.struct([
        pa.field("enabled", pa.bool_(), nullable=False, metadata={}),
        pa.field("y", pa.float64(), nullable=False, metadata={}),
        pa.field("strength", pa.float64(), nullable=False, metadata={}),
    ])

    @staticmethod
    def _native_to_pa_array(data: ForcePositionYArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, ForcePositionY):
            data = [data]

        return pa.StructArray.from_arrays(
            [
                pa.array(np.asarray([x.enabled for x in data], dtype=np.bool_)),
                pa.array(np.asarray([x.y for x in data], dtype=np.float64)),
                pa.array(np.asarray([x.strength for x in data], dtype=np.float64)),
            ],
            fields=list(data_type),
        )
