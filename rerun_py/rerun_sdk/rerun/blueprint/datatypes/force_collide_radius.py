# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_types/definitions/rerun/blueprint/datatypes/force_collide_radius.fbs".

# You can extend this class by creating a "ForceCollideRadiusExt" class in "force_collide_radius_ext.py".

from __future__ import annotations

from typing import Any, Sequence, Union

import numpy as np
import pyarrow as pa
from attrs import define, field

from ..._baseclasses import (
    BaseBatch,
)

__all__ = ["ForceCollideRadius", "ForceCollideRadiusArrayLike", "ForceCollideRadiusBatch", "ForceCollideRadiusLike"]


@define(init=False)
class ForceCollideRadius:
    """**Datatype**: Defines a force that resolves collisions between the radii of `GraphNodes`."""

    def __init__(self: Any, enabled: bool, iterations: int, strength: float, padding: float):
        """
        Create a new instance of the ForceCollideRadius datatype.

        Parameters
        ----------
        enabled:
            Whether the force is enabled.
        iterations:
            The number of iterations to resolve collisions.
        strength:
            The strength of the force.
        padding:
            An additional padding to apply to each node radius.

        """

        # You can define your own __init__ function as a member of ForceCollideRadiusExt in force_collide_radius_ext.py
        self.__attrs_init__(enabled=enabled, iterations=iterations, strength=strength, padding=padding)

    enabled: bool = field(converter=bool)
    # Whether the force is enabled.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    iterations: int = field(converter=int)
    # The number of iterations to resolve collisions.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    strength: float = field(converter=float)
    # The strength of the force.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    padding: float = field(converter=float)
    # An additional padding to apply to each node radius.
    #
    # (Docstring intentionally commented out to hide this field from the docs)


ForceCollideRadiusLike = ForceCollideRadius
ForceCollideRadiusArrayLike = Union[
    ForceCollideRadius,
    Sequence[ForceCollideRadiusLike],
]


class ForceCollideRadiusBatch(BaseBatch[ForceCollideRadiusArrayLike]):
    _ARROW_DATATYPE = pa.struct([
        pa.field("enabled", pa.bool_(), nullable=False, metadata={}),
        pa.field("iterations", pa.uint32(), nullable=False, metadata={}),
        pa.field("strength", pa.float64(), nullable=False, metadata={}),
        pa.field("padding", pa.float64(), nullable=False, metadata={}),
    ])

    @staticmethod
    def _native_to_pa_array(data: ForceCollideRadiusArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, ForceCollideRadius):
            data = [data]

        return pa.StructArray.from_arrays(
            [
                pa.array(np.asarray([x.enabled for x in data], dtype=np.bool_)),
                pa.array(np.asarray([x.iterations for x in data], dtype=np.uint32)),
                pa.array(np.asarray([x.strength for x in data], dtype=np.float64)),
                pa.array(np.asarray([x.padding for x in data], dtype=np.float64)),
            ],
            fields=list(data_type),
        )
