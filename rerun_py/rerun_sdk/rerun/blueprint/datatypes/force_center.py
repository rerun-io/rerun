# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_types/definitions/rerun/blueprint/datatypes/force_center.fbs".

# You can extend this class by creating a "ForceCenterExt" class in "force_center_ext.py".

from __future__ import annotations

from typing import Any, Sequence, Union

import numpy as np
import pyarrow as pa
from attrs import define, field

from ..._baseclasses import (
    BaseBatch,
)

__all__ = ["ForceCenter", "ForceCenterArrayLike", "ForceCenterBatch", "ForceCenterLike"]


@define(init=False)
class ForceCenter:
    """**Datatype**: Defines a force that globally centers a graph by moving its center of mass towards a given position."""

    def __init__(self: Any, enabled: bool, x: float, y: float, strength: float):
        """
        Create a new instance of the ForceCenter datatype.

        Parameters
        ----------
        enabled:
            Whether the force is enabled.
        x:
            The `x` position to pull nodes towards.
        y:
            The `y` position to pull nodes towards.
        strength:
            The strength of the force.

        """

        # You can define your own __init__ function as a member of ForceCenterExt in force_center_ext.py
        self.__attrs_init__(enabled=enabled, x=x, y=y, strength=strength)

    enabled: bool = field(converter=bool)
    # Whether the force is enabled.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    x: float = field(converter=float)
    # The `x` position to pull nodes towards.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    y: float = field(converter=float)
    # The `y` position to pull nodes towards.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    strength: float = field(converter=float)
    # The strength of the force.
    #
    # (Docstring intentionally commented out to hide this field from the docs)


ForceCenterLike = ForceCenter
ForceCenterArrayLike = Union[
    ForceCenter,
    Sequence[ForceCenterLike],
]


class ForceCenterBatch(BaseBatch[ForceCenterArrayLike]):
    _ARROW_DATATYPE = pa.struct([
        pa.field("enabled", pa.bool_(), nullable=False, metadata={}),
        pa.field("x", pa.float64(), nullable=False, metadata={}),
        pa.field("y", pa.float64(), nullable=False, metadata={}),
        pa.field("strength", pa.float64(), nullable=False, metadata={}),
    ])

    @staticmethod
    def _native_to_pa_array(data: ForceCenterArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, ForceCenter):
            data = [data]

        return pa.StructArray.from_arrays(
            [
                pa.array(np.asarray([x.enabled for x in data], dtype=np.bool_)),
                pa.array(np.asarray([x.x for x in data], dtype=np.float64)),
                pa.array(np.asarray([x.y for x in data], dtype=np.float64)),
                pa.array(np.asarray([x.strength for x in data], dtype=np.float64)),
            ],
            fields=list(data_type),
        )
