# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/blueprint/datatypes/visualizer_component_mapping.fbs".

# You can extend this class by creating a "ComponentSourceKindExt" class in "component_source_kind_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa

from ..._baseclasses import (
    BaseBatch,
)

__all__ = ["ComponentSourceKind", "ComponentSourceKindArrayLike", "ComponentSourceKindBatch", "ComponentSourceKindLike"]


from enum import Enum


class ComponentSourceKind(Enum):
    """**Datatype**: What kind of source to use for a visualizer component mapping."""

    SourceComponent = 1
    """
    Use an explicit selection defined by `source_component`.

    May or may not make use of a selector string.
    """

    Override = 2
    """
    Use a timeless override value that is defined in the blueprint.

    The override value is stored on the same entity as the visualizer instruction
    and uses the `target` as its component name.
    """

    Default = 3
    """Default as specified on the view's blueprint."""

    Fallback = 4
    """Make use of the viewer's fallback logic to produce a value."""

    @classmethod
    def auto(cls, val: str | int | ComponentSourceKind) -> ComponentSourceKind:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, ComponentSourceKind):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


ComponentSourceKindLike = (
    ComponentSourceKind
    | Literal[
        "Default", "Fallback", "Override", "SourceComponent", "default", "fallback", "override", "sourcecomponent"
    ]
    | int
)
"""A type alias for any ComponentSourceKind-like object."""

ComponentSourceKindArrayLike = (
    ComponentSourceKind
    | Literal[
        "Default", "Fallback", "Override", "SourceComponent", "default", "fallback", "override", "sourcecomponent"
    ]
    | int
    | Sequence[ComponentSourceKindLike]
)
"""A type alias for any ComponentSourceKind-like array object."""


class ComponentSourceKindBatch(BaseBatch[ComponentSourceKindArrayLike]):
    _ARROW_DATATYPE = pa.uint8()

    @staticmethod
    def _native_to_pa_array(data: ComponentSourceKindArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (ComponentSourceKind, int, str)):
            data = [data]

        pa_data = [ComponentSourceKind.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]

        return pa.array(pa_data, type=data_type)
