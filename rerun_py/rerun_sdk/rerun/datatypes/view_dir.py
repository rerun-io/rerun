# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/datatypes/view_dir.fbs".

# You can extend this class by creating a "ViewDirExt" class in "view_dir_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import Literal

import pyarrow as pa

from .._baseclasses import (
    BaseBatch,
)

__all__ = ["ViewDir", "ViewDirArrayLike", "ViewDirBatch", "ViewDirLike"]


from enum import Enum


class ViewDir(Enum):
    """
    **Datatype**: The six cardinal directions for 3D view-space.

    Note that these are abstract directions and do on their own not correspond to any specific
    coordinate system or numerical values.
    """

    Up = 1
    """Up."""

    Down = 2
    """Down."""

    Right = 3
    """Right."""

    Left = 4
    """Left."""

    Forward = 5
    """Forward."""

    Back = 6
    """Back."""

    @classmethod
    def auto(cls, val: str | int | ViewDir) -> ViewDir:
        """Best-effort converter, including a case-insensitive string matcher."""
        if isinstance(val, ViewDir):
            return val
        if isinstance(val, int):
            return cls(val)
        try:
            return cls[val]
        except KeyError:
            val_lower = val.lower()
            for variant in cls:
                if variant.name.lower() == val_lower:
                    return variant
        raise ValueError(f"Cannot convert {val} to {cls.__name__}")

    def __str__(self) -> str:
        """Returns the variant name."""
        return self.name


ViewDirLike = (
    ViewDir
    | Literal["Back", "Down", "Forward", "Left", "Right", "Up", "back", "down", "forward", "left", "right", "up"]
    | int
)
"""A type alias for any ViewDir-like object."""

ViewDirArrayLike = (
    ViewDir
    | Literal["Back", "Down", "Forward", "Left", "Right", "Up", "back", "down", "forward", "left", "right", "up"]
    | int
    | Sequence[ViewDirLike]
)
"""A type alias for any ViewDir-like array object."""


class ViewDirBatch(BaseBatch[ViewDirArrayLike]):
    _ARROW_DATATYPE = pa.uint8()

    @staticmethod
    def _native_to_pa_array(data: ViewDirArrayLike, data_type: pa.DataType) -> pa.Array:
        if isinstance(data, (ViewDir, int, str)):
            data = [data]

        pa_data = [ViewDir.auto(v).value if v is not None else None for v in data]  # type: ignore[redundant-expr]  # ty: ignore[not-iterable]

        return pa.array(pa_data, type=data_type)
