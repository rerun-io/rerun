# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/datatypes/view_coordinates2d.fbs".

# You can extend this class by creating a "ViewCoordinates2DExt" class in "view_coordinates2d_ext.py".

from __future__ import annotations

from collections.abc import Sequence
from typing import TYPE_CHECKING, Any

import numpy as np
import numpy.typing as npt
import pyarrow as pa
from attrs import define, field

from .._baseclasses import (
    BaseBatch,
)
from .._converters import (
    to_np_uint8,
)
from .._numpy_compatibility import asarray
from .view_coordinates2d_ext import ViewCoordinates2DExt

__all__ = ["ViewCoordinates2D", "ViewCoordinates2DArrayLike", "ViewCoordinates2DBatch", "ViewCoordinates2DLike"]


@define(init=False)
class ViewCoordinates2D(ViewCoordinates2DExt):
    """
    **Datatype**: How we interpret the 2D coordinate system of an entity/space.

    For instance: Does the Y axis point up or down?

    The two coordinates are always ordered as [x, y].

    For example [Right, Down] means that the X axis points to the right, and the Y axis points
    down. This is the default 2D coordinate system and common image/screen convention.

    The following constants are used to represent the different directions:
     * Up = 1
     * Down = 2
     * Right = 3
     * Left = 4

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    def __init__(self: Any, coordinates: ViewCoordinates2DLike) -> None:
        """
        Create a new instance of the ViewCoordinates2D datatype.

        Parameters
        ----------
        coordinates:
            The directions of the [x, y] axes.

        """

        # You can define your own __init__ function as a member of ViewCoordinates2DExt in view_coordinates2d_ext.py
        self.__attrs_init__(coordinates=coordinates)

    coordinates: npt.NDArray[np.uint8] = field(converter=to_np_uint8)
    # The directions of the [x, y] axes.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    def __array__(self, dtype: npt.DTypeLike = None, copy: bool | None = None) -> npt.NDArray[Any]:
        # You can define your own __array__ function as a member of ViewCoordinates2DExt in view_coordinates2d_ext.py
        return asarray(self.coordinates, dtype=dtype, copy=copy)

    def __len__(self) -> int:
        # You can define your own __len__ function as a member of ViewCoordinates2DExt in view_coordinates2d_ext.py
        return len(self.coordinates)


if TYPE_CHECKING:
    ViewCoordinates2DLike = ViewCoordinates2D | npt.ArrayLike
    """A type alias for any ViewCoordinates2D-like object."""
else:
    ViewCoordinates2DLike = Any

ViewCoordinates2DArrayLike = ViewCoordinates2D | Sequence[ViewCoordinates2DLike] | npt.ArrayLike
"""A type alias for any ViewCoordinates2D-like array object."""


class ViewCoordinates2DBatch(BaseBatch[ViewCoordinates2DArrayLike]):
    _ARROW_DATATYPE = pa.list_(pa.field("item", pa.uint8(), nullable=False, metadata={}), 2)

    @staticmethod
    def _native_to_pa_array(data: ViewCoordinates2DArrayLike, data_type: pa.DataType) -> pa.Array:
        return ViewCoordinates2DExt.native_to_pa_array_override(data, data_type)
