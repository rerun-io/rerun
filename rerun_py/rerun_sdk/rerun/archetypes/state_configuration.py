# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/state_configuration.fbs".

# You can extend this class by creating a "StateConfigurationExt" class in "state_configuration_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
    ComponentDescriptor,
)
from ..blueprint import VisualizableArchetype, Visualizer
from ..error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ..blueprint.datatypes import VisualizerComponentMappingLike

__all__ = ["StateConfiguration"]


@define(str=False, repr=False, init=False)
class StateConfiguration(Archetype, VisualizableArchetype):
    """
    **Archetype**: Define the style and mapping for state values in a state timeline view.

    This archetype provides configuration for how state values are displayed.
    It maps raw state values to display labels, colors, and visibility.

    `values`, `labels`, `colors`, and `visible` are parallel arrays: the entry
    at index `i` of each describes the same state value, and only the
    per-index pairing is meaningful. The four arrays should have matching
    length; any secondary array (`labels`, `colors`, `visible`) that is shorter
    than `values` falls back to defaults for the missing entries.

    It's generally recommended to log this type as static.

    The underlying data needs to be logged to the same entity path using [`archetypes.StateChange`][rerun.archetypes.StateChange].

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**

    Example
    -------
    ### State changes with a custom style:
    ```python
    # Log a `StateChange` together with a `StateConfiguration` that customizes its display.

    import rerun as rr

    rr.init("rerun_example_state_configuration", spawn=True)

    # Configure how each raw state value is displayed (label, color, visibility).
    rr.log(
        "door",
        rr.StateConfiguration(
            values=["open", "closed"],
            labels=["Open", "Closed"],
            colors=[0x4CAF50FF, 0xEF5350FF],
        ),
        static=True,
    )

    rr.set_time("step", sequence=0)
    rr.log("door", rr.StateChange(state="open"))

    rr.set_time("step", sequence=1)
    rr.log("door", rr.StateChange(state="closed"))

    rr.set_time("step", sequence=2)
    rr.log("door", rr.StateChange(state="open"))
    ```

    """

    NAME: ClassVar[str] = "rerun.archetypes.StateConfiguration"

    def __init__(
        self: Any,
        *,
        values: datatypes.Utf8ArrayLike | None = None,
        labels: datatypes.Utf8ArrayLike | None = None,
        colors: datatypes.Rgba32ArrayLike | None = None,
        visible: datatypes.BoolArrayLike | None = None,
    ) -> None:
        """
        Create a new instance of the StateConfiguration archetype.

        Parameters
        ----------
        values:
            The raw state values that this configuration applies to.

            Each entry defines a known state value. The order determines the mapping to
            `labels`, `colors`, and `visible` (by index).
        labels:
            Display labels for each state value.

            If provided, the label at index `i` is shown instead of the raw value at index `i`.
            If not provided or shorter than `values`, the raw value is used as the label.
        colors:
            Colors for each state value.

            If provided, the color at index `i` is used for the state at index `i`.
            If not provided, colors are assigned automatically from a built-in palette.
        visible:
            Visibility for each state value.

            If provided, the visibility at index `i` controls whether the state at index `i` is shown.
            If not provided, all state values are visible.

        """

        # You can define your own __init__ function as a member of StateConfigurationExt in state_configuration_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(values=values, labels=labels, colors=colors, visible=visible)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            values=None,
            labels=None,
            colors=None,
            visible=None,
        )

    @classmethod
    def _clear(cls) -> StateConfiguration:
        """Produce an empty StateConfiguration, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        values: datatypes.Utf8ArrayLike | None = None,
        labels: datatypes.Utf8ArrayLike | None = None,
        colors: datatypes.Rgba32ArrayLike | None = None,
        visible: datatypes.BoolArrayLike | None = None,
    ) -> StateConfiguration:
        """
        Update only some specific fields of a `StateConfiguration`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        values:
            The raw state values that this configuration applies to.

            Each entry defines a known state value. The order determines the mapping to
            `labels`, `colors`, and `visible` (by index).
        labels:
            Display labels for each state value.

            If provided, the label at index `i` is shown instead of the raw value at index `i`.
            If not provided or shorter than `values`, the raw value is used as the label.
        colors:
            Colors for each state value.

            If provided, the color at index `i` is used for the state at index `i`.
            If not provided, colors are assigned automatically from a built-in palette.
        visible:
            Visibility for each state value.

            If provided, the visibility at index `i` controls whether the state at index `i` is shown.
            If not provided, all state values are visible.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "values": values,
                "labels": labels,
                "colors": colors,
                "visible": visible,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> StateConfiguration:
        """Clear all the fields of a `StateConfiguration`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_values() -> ComponentDescriptor:
        return ComponentDescriptor(
            "StateConfiguration:values",
            archetype=StateConfiguration.NAME,
            component_type=components.TextBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_labels() -> ComponentDescriptor:
        return ComponentDescriptor(
            "StateConfiguration:labels",
            archetype=StateConfiguration.NAME,
            component_type=components.TextBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_colors() -> ComponentDescriptor:
        return ComponentDescriptor(
            "StateConfiguration:colors",
            archetype=StateConfiguration.NAME,
            component_type=components.ColorBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_visible() -> ComponentDescriptor:
        return ComponentDescriptor(
            "StateConfiguration:visible",
            archetype=StateConfiguration.NAME,
            component_type=components.VisibleBatch._COMPONENT_TYPE,
        )

    @classmethod
    def columns(
        cls,
        *,
        values: datatypes.Utf8ArrayLike | None = None,
        labels: datatypes.Utf8ArrayLike | None = None,
        colors: datatypes.Rgba32ArrayLike | None = None,
        visible: datatypes.BoolArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        values:
            The raw state values that this configuration applies to.

            Each entry defines a known state value. The order determines the mapping to
            `labels`, `colors`, and `visible` (by index).
        labels:
            Display labels for each state value.

            If provided, the label at index `i` is shown instead of the raw value at index `i`.
            If not provided or shorter than `values`, the raw value is used as the label.
        colors:
            Colors for each state value.

            If provided, the color at index `i` is used for the state at index `i`.
            If not provided, colors are assigned automatically from a built-in palette.
        visible:
            Visibility for each state value.

            If provided, the visibility at index `i` controls whether the state at index `i` is shown.
            If not provided, all state values are visible.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                values=values,
                labels=labels,
                colors=colors,
                visible=visible,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "StateConfiguration:values": values,
            "StateConfiguration:labels": labels,
            "StateConfiguration:colors": colors,
            "StateConfiguration:visible": visible,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type):
                    elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                    if arrow_array.type.list_size == elem_flat_len:
                        # The product of the last dimensions of the shape are equal to the size of the fixed size list array,
                        # so we have `num_rows` single element batches (each element is a fixed sized list).
                        batch_length = 1
                    else:
                        batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                else:
                    # For primitive types, derive batch_length from the actual arrow array length
                    # since the input shape can be misleading (e.g. colors [R,G,B] -> single uint32).
                    batch_length = len(arrow_array) // num_rows if num_rows > 0 else 1

                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    values: components.TextBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TextBatch._converter,  # type: ignore[misc]
    )
    # The raw state values that this configuration applies to.
    #
    # Each entry defines a known state value. The order determines the mapping to
    # `labels`, `colors`, and `visible` (by index).
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    labels: components.TextBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TextBatch._converter,  # type: ignore[misc]
    )
    # Display labels for each state value.
    #
    # If provided, the label at index `i` is shown instead of the raw value at index `i`.
    # If not provided or shorter than `values`, the raw value is used as the label.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    colors: components.ColorBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ColorBatch._converter,  # type: ignore[misc]
    )
    # Colors for each state value.
    #
    # If provided, the color at index `i` is used for the state at index `i`.
    # If not provided, colors are assigned automatically from a built-in palette.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    visible: components.VisibleBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VisibleBatch._converter,  # type: ignore[misc]
    )
    # Visibility for each state value.
    #
    # If provided, the visibility at index `i` controls whether the state at index `i` is shown.
    # If not provided, all state values are visible.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]

    def visualizer(self, *, mappings: list[VisualizerComponentMappingLike] | None = None) -> Visualizer:
        """
        Creates a visualizer for this archetype, using all currently set values as overrides.

        Parameters
        ----------
        mappings:
            Optional component mappings to control how the visualizer sources its data.

            ⚠️ **Experimental**: Component mappings are an experimental feature and may change.
            See https://github.com/rerun-io/rerun/issues/10631 for more information.

        """
        return Visualizer("StateVisualizer", overrides=self.as_component_batches(), mappings=mappings)
