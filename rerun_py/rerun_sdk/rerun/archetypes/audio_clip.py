# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_clip.fbs".

# You can extend this class by creating a "AudioClipExt" class in "audio_clip_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
    ComponentDescriptor,
)
from ..blueprint import VisualizableArchetype, Visualizer
from ..error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ..blueprint.datatypes import VisualizerComponentMappingLike

__all__ = ["AudioClip"]


@define(str=False, repr=False, init=False)
class AudioClip(Archetype, VisualizableArchetype):
    """
    **Archetype**: A chunk of PCM audio samples.

    The samples are stored as a numeric tensor. 1D tensors are interpreted as mono audio.
    2D tensors are interpreted as `[sample, channel]` audio, and may include dimension
    names such as `"sample"` and `"channel"`.

    Log audio chunks on a time timeline at the time where the first sample starts. The
    [`components.SampleRate`][rerun.components.SampleRate] determines how sample indices map to time, allowing the
    viewer to display waveforms and keep audio-aligned views in sync with the global
    timeline.

    The audio samples are not decoded or transformed by Rerun. If you want to show
    spectrograms, mel spectrograms, MFCCs, or filtered versions, log those derived
    tensors next to the audio clip and place them in a `SignalHeatmapView`.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**

    Example
    -------
    ### Log a multi-channel audio clip.:
    ```python
    import numpy as np

    import rerun as rr

    rr.init("rerun_example_audio_clip", spawn=True)

    sample_rate = 16_000
    seconds = 2.0
    t = np.arange(int(sample_rate * seconds), dtype=np.float32) / sample_rate

    left = 0.4 * np.sin(2.0 * np.pi * 220.0 * t)
    right = 0.4 * np.sin(2.0 * np.pi * 330.0 * t)
    samples = np.stack([left, right], axis=1).astype(np.float32)

    rr.set_time("time", duration=0.0)
    rr.log(
        "audio",
        rr.AudioClip(
            samples,
            sample_rate=sample_rate,
            channel_names=["left", "right"],
        ),
    )
    ```

    """

    NAME: ClassVar[str] = "rerun.archetypes.AudioClip"

    def __init__(
        self: Any,
        samples: datatypes.TensorDataLike,
        sample_rate: datatypes.Float64Like,
        *,
        channel_names: datatypes.Utf8ArrayLike | None = None,
    ) -> None:
        """
        Create a new instance of the AudioClip archetype.

        Parameters
        ----------
        samples:
            PCM audio samples.
        sample_rate:
            Sample rate in Hertz.
        channel_names:
            Optional display names for each audio channel.

        """

        # You can define your own __init__ function as a member of AudioClipExt in audio_clip_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(samples=samples, sample_rate=sample_rate, channel_names=channel_names)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            samples=None,
            sample_rate=None,
            channel_names=None,
        )

    @classmethod
    def _clear(cls) -> AudioClip:
        """Produce an empty AudioClip, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        samples: datatypes.TensorDataLike | None = None,
        sample_rate: datatypes.Float64Like | None = None,
        channel_names: datatypes.Utf8ArrayLike | None = None,
    ) -> AudioClip:
        """
        Update only some specific fields of a `AudioClip`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        samples:
            PCM audio samples.
        sample_rate:
            Sample rate in Hertz.
        channel_names:
            Optional display names for each audio channel.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "samples": samples,
                "sample_rate": sample_rate,
                "channel_names": channel_names,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> AudioClip:
        """Clear all the fields of a `AudioClip`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_samples() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioClip:samples",
            archetype=AudioClip.NAME,
            component_type=components.TensorDataBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_sample_rate() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioClip:sample_rate",
            archetype=AudioClip.NAME,
            component_type=components.SampleRateBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_channel_names() -> ComponentDescriptor:
        return ComponentDescriptor(
            "AudioClip:channel_names",
            archetype=AudioClip.NAME,
            component_type=components.AudioChannelNameBatch._COMPONENT_TYPE,
        )

    @classmethod
    def columns(
        cls,
        *,
        samples: datatypes.TensorDataArrayLike | None = None,
        sample_rate: datatypes.Float64ArrayLike | None = None,
        channel_names: datatypes.Utf8ArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        samples:
            PCM audio samples.
        sample_rate:
            Sample rate in Hertz.
        channel_names:
            Optional display names for each audio channel.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                samples=samples,
                sample_rate=sample_rate,
                channel_names=channel_names,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "AudioClip:samples": samples,
            "AudioClip:sample_rate": sample_rate,
            "AudioClip:channel_names": channel_names,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type):
                    elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                    if arrow_array.type.list_size == elem_flat_len:
                        # The product of the last dimensions of the shape are equal to the size of the fixed size list array,
                        # so we have `num_rows` single element batches (each element is a fixed sized list).
                        batch_length = 1
                    else:
                        batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                else:
                    # For primitive types, derive batch_length from the actual arrow array length
                    # since the input shape can be misleading (e.g. colors [R,G,B] -> single uint32).
                    batch_length = len(arrow_array) // num_rows if num_rows > 0 else 1

                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    samples: components.TensorDataBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TensorDataBatch._converter,  # type: ignore[misc]
    )
    # PCM audio samples.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    sample_rate: components.SampleRateBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.SampleRateBatch._converter,  # type: ignore[misc]
    )
    # Sample rate in Hertz.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    channel_names: components.AudioChannelNameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.AudioChannelNameBatch._converter,  # type: ignore[misc]
    )
    # Optional display names for each audio channel.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]

    def visualizer(self, *, mappings: list[VisualizerComponentMappingLike] | None = None) -> Visualizer:
        """
        Creates a visualizer for this archetype, using all currently set values as overrides.

        Parameters
        ----------
        mappings:
            Optional component mappings to control how the visualizer sources its data.

            ⚠️ **Experimental**: Component mappings are an experimental feature and may change.
            See https://github.com/rerun-io/rerun/issues/10631 for more information.

        """
        return Visualizer("Audio", overrides=self.as_component_batches(), mappings=mappings)
