# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/view_coordinates2d.fbs".

# You can extend this class by creating a "ViewCoordinates2DExt" class in "view_coordinates2d_ext.py".

from __future__ import annotations

from typing import Any

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
)
from ..error_utils import catch_and_log_exceptions
from .view_coordinates2d_ext import ViewCoordinates2DExt

__all__ = ["ViewCoordinates2D"]


@define(str=False, repr=False, init=False)
class ViewCoordinates2D(ViewCoordinates2DExt, Archetype):
    """
    **Archetype**: How we interpret the 2D coordinate system of an entity/space.

    For instance: Does the Y axis point up or down?

    The two coordinates are always ordered as [x, y].

    For example [Right, Down] means that the X axis points to the right, and the Y axis points
    down. This is the default 2D coordinate system and common image/screen convention.

    Only the 4 axis-flip combinations are supported:
     * **RD** (Right, Down) - default, image/screen convention
     * **RU** (Right, Up) - math/plot convention
     * **LD** (Left, Down) - horizontally mirrored image
     * **LU** (Left, Up) - both axes flipped

    Make sure that this archetype is logged at or above the origin entity path of your 2D views.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**

    Example
    -------
    ### View coordinates for adjusting the 2D coordinate system:
    ```python
    import rerun as rr

    rr.init("rerun_example_view_coordinates2d", spawn=True)

    rr.log("world", rr.ViewCoordinates2D.RU, static=True)  # Set Y-Up

    rr.log(
        "world/points",
        rr.Points2D([(0, 0), (1, 1), (3, 2)]),
    )
    ```

    """

    def __init__(self: Any, xy: datatypes.ViewCoordinates2DLike) -> None:
        """
        Create a new instance of the ViewCoordinates2D archetype.

        Parameters
        ----------
        xy:
            The directions of the [x, y] axes.

        """

        # You can define your own __init__ function as a member of ViewCoordinates2DExt in view_coordinates2d_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(xy=xy)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            xy=None,
        )

    @classmethod
    def _clear(cls) -> ViewCoordinates2D:
        """Produce an empty ViewCoordinates2D, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        xy: datatypes.ViewCoordinates2DLike | None = None,
    ) -> ViewCoordinates2D:
        """
        Update only some specific fields of a `ViewCoordinates2D`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        xy:
            The directions of the [x, y] axes.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "xy": xy,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> ViewCoordinates2D:
        """Clear all the fields of a `ViewCoordinates2D`."""
        return cls.from_fields(clear_unset=True)

    @classmethod
    def columns(
        cls,
        *,
        xy: datatypes.ViewCoordinates2DArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        xy:
            The directions of the [x, y] axes.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                xy=xy,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {"ViewCoordinates2D:xy": xy}
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type) and arrow_array.type.list_size == elem_flat_len:
                    # If the product of the last dimensions of the shape are equal to the size of the fixed size list array,
                    # we have `num_rows` single element batches (each element is a fixed sized list).
                    # (This should have been already validated by conversion to the arrow_array)
                    batch_length = 1
                else:
                    batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]

                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]
                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    xy: components.ViewCoordinates2DBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ViewCoordinates2DBatch._converter,  # type: ignore[misc]
    )
    # The directions of the [x, y] axes.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]


ViewCoordinates2DExt.deferred_patch_class(ViewCoordinates2D)
