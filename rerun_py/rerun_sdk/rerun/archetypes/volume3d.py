# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/archetypes/volume_3d.def.rs".

# You can extend this class by creating a "Volume3DExt" class in "volume3d_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, encodings
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
    ComponentDescriptor,
)
from ..blueprint import VisualizableArchetype, Visualizer
from ..error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ..blueprint.encodings import VisualizerComponentMappingLike

__all__ = ["Volume3D"]


@define(str=False, repr=False, init=False)
class Volume3D(Archetype, VisualizableArchetype):
    """
    **Archetype**: A dense 3D scalar field, rendered by ray marching.

    This archetype is intended for volumetric scans and simulations, e.g. CT/MRI scans,
    signed distance fields, or occupancy probabilities sampled on a regular grid.

    The values are a 3D tensor with dimensions ordered `[z, y, x]`, i.e. the last dimension varies
    fastest and runs along the local X axis. This matches the row-major layout of
    [`archetypes.Image`][rerun.archetypes.Image], with slices stacked along the local Z axis.
    The tensor element at `[k, j, i]` is thus the voxel with grid index `[i, j, k]`.

    Voxels are positioned exactly like those of [`archetypes.VoxelGridMap`][rerun.archetypes.VoxelGridMap]:
    the minimum corner of the voxel with `[0, 0, 0]` index is located at the origin of the entity's
    coordinate frame and can have an additional offset from there through the optional translation
    and rotation fields, and a voxel center is at `(index + 0.5) * voxel_size` in local grid
    coordinates (i.e. relative to the minimum corner).
    This archetype and a [`archetypes.VoxelGridMap`][rerun.archetypes.VoxelGridMap] with the same `voxel_size` and pose
    therefore agree voxel for voxel, the dense volume covering indices `[0, 0, 0]` up to
    `[width - 1, height - 1, depth - 1]`.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**

    Example
    -------
    ### Simple volume:
    ```python
    import numpy as np

    import rerun as rr

    SIZE = 32
    RADIUS = np.float32(14.0)

    rr.init("rerun_example_volume3d_simple", spawn=True)

    # Squared distance from the center of the grid, per voxel, in voxel units.
    axis = np.arange(SIZE, dtype=np.float32) - np.float32(0.5 * (SIZE - 1))
    z, y, x = np.meshgrid(axis, axis, axis, indexing="ij")
    distance_sq = x * x + y * y + z * z

    # A soft sphere: 1 at the center, falling off to 0 at `RADIUS`.
    values = np.maximum(
        np.float32(0.0), np.float32(1.0) - distance_sq / (RADIUS * RADIUS)
    )

    # Dimensions are ordered `[z, y, x]`. Only `f16` values are supported for now.
    rr.log(
        "volume",
        rr.Volume3D(values.astype(np.float16), voxel_size=[0.1, 0.1, 0.1]),
    )
    ```

    """

    NAME: ClassVar[str] = "rerun.archetypes.Volume3D"

    def __init__(
        self: Any,
        values: encodings.TensorDataLike,
        *,
        voxel_size: encodings.Vec3DLike | None = None,
        translation: encodings.Vec3DLike | None = None,
        quaternion: encodings.QuaternionLike | None = None,
        value_range: encodings.Range1DLike | None = None,
        colormap: components.ColormapLike | None = None,
        opacity: encodings.Float32Like | None = None,
    ) -> None:
        """
        Create a new instance of the Volume3D archetype.

        Parameters
        ----------
        values:
            The scalar value of each voxel, as a 3D tensor with dimensions ordered `[z, y, x]`.

            Currently only `f16` are supported.
        voxel_size:
            The scene-unit dimensions of a single voxel cell.

            This defines the voxel size along the local grid X/Y/Z axes, and thus the total extent of the
            volume: `[width, height, depth] * voxel_size`.
            Anisotropic spacing (as is common for medical scans) is expressed here.
            Each dimension must be finite and positive.

            Defaults to `[1.0, 1.0, 1.0]`.
        translation:
            Translation of the minimum corner of voxel `[0, 0, 0]`.

            Together with [`components.RotationQuat`][rerun.components.RotationQuat], this defines the pose of the volume
            relative to the entity's coordinate frame.

            If not set, the minimum corner is placed at the origin of the entity's coordinate frame.
        quaternion:
            Rotation of the volume via quaternion.

            Together with [`components.Translation3D`][rerun.components.Translation3D], this defines the pose of the volume
            relative to the entity's coordinate frame.
            The rotation is around the minimum corner of voxel `[0, 0, 0]`, and is applied before the
            translation.
        value_range:
            How to map `values` to opacity and color.

            If not specified, the range is estimated from the data.
        colormap:
            Colormap applied to the values after mapping them through `value_range`.

            Defaults to Turbo.
        opacity:
            Overall opacity of the volume.

            The opacity of a single voxel is its value (normalized through `value_range`) scaled by
            this, i.e. a linear ramp: low values are transparent, high values are opaque.
            Lowering this makes the interior of the volume visible.

            Defaults to 1.0.

        """

        # You can define your own __init__ function as a member of Volume3DExt in volume3d_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(
                values=values,
                voxel_size=voxel_size,
                translation=translation,
                quaternion=quaternion,
                value_range=value_range,
                colormap=colormap,
                opacity=opacity,
            )
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            values=None,
            voxel_size=None,
            translation=None,
            quaternion=None,
            value_range=None,
            colormap=None,
            opacity=None,
        )

    @classmethod
    def _clear(cls) -> Volume3D:
        """Produce an empty Volume3D, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        values: encodings.TensorDataLike | None = None,
        voxel_size: encodings.Vec3DLike | None = None,
        translation: encodings.Vec3DLike | None = None,
        quaternion: encodings.QuaternionLike | None = None,
        value_range: encodings.Range1DLike | None = None,
        colormap: components.ColormapLike | None = None,
        opacity: encodings.Float32Like | None = None,
    ) -> Volume3D:
        """
        Update only some specific fields of a `Volume3D`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        values:
            The scalar value of each voxel, as a 3D tensor with dimensions ordered `[z, y, x]`.

            Currently only `f16` are supported.
        voxel_size:
            The scene-unit dimensions of a single voxel cell.

            This defines the voxel size along the local grid X/Y/Z axes, and thus the total extent of the
            volume: `[width, height, depth] * voxel_size`.
            Anisotropic spacing (as is common for medical scans) is expressed here.
            Each dimension must be finite and positive.

            Defaults to `[1.0, 1.0, 1.0]`.
        translation:
            Translation of the minimum corner of voxel `[0, 0, 0]`.

            Together with [`components.RotationQuat`][rerun.components.RotationQuat], this defines the pose of the volume
            relative to the entity's coordinate frame.

            If not set, the minimum corner is placed at the origin of the entity's coordinate frame.
        quaternion:
            Rotation of the volume via quaternion.

            Together with [`components.Translation3D`][rerun.components.Translation3D], this defines the pose of the volume
            relative to the entity's coordinate frame.
            The rotation is around the minimum corner of voxel `[0, 0, 0]`, and is applied before the
            translation.
        value_range:
            How to map `values` to opacity and color.

            If not specified, the range is estimated from the data.
        colormap:
            Colormap applied to the values after mapping them through `value_range`.

            Defaults to Turbo.
        opacity:
            Overall opacity of the volume.

            The opacity of a single voxel is its value (normalized through `value_range`) scaled by
            this, i.e. a linear ramp: low values are transparent, high values are opaque.
            Lowering this makes the interior of the volume visible.

            Defaults to 1.0.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "values": values,
                "voxel_size": voxel_size,
                "translation": translation,
                "quaternion": quaternion,
                "value_range": value_range,
                "colormap": colormap,
                "opacity": opacity,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> Volume3D:
        """Clear all the fields of a `Volume3D`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_values() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:values",
            archetype=Volume3D.NAME,
            component_type=components.TensorDataBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_voxel_size() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:voxel_size",
            archetype=Volume3D.NAME,
            component_type=components.VoxelSizeBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_translation() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:translation",
            archetype=Volume3D.NAME,
            component_type=components.Translation3DBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_quaternion() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:quaternion",
            archetype=Volume3D.NAME,
            component_type=components.RotationQuatBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_value_range() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:value_range",
            archetype=Volume3D.NAME,
            component_type=components.ValueRangeBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_colormap() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:colormap",
            archetype=Volume3D.NAME,
            component_type=components.ColormapBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_opacity() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Volume3D:opacity",
            archetype=Volume3D.NAME,
            component_type=components.OpacityBatch._COMPONENT_TYPE,
        )

    @classmethod
    def columns(
        cls,
        *,
        values: encodings.TensorDataArrayLike | None = None,
        voxel_size: encodings.Vec3DArrayLike | None = None,
        translation: encodings.Vec3DArrayLike | None = None,
        quaternion: encodings.QuaternionArrayLike | None = None,
        value_range: encodings.Range1DArrayLike | None = None,
        colormap: components.ColormapArrayLike | None = None,
        opacity: encodings.Float32ArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        values:
            The scalar value of each voxel, as a 3D tensor with dimensions ordered `[z, y, x]`.

            Currently only `f16` are supported.
        voxel_size:
            The scene-unit dimensions of a single voxel cell.

            This defines the voxel size along the local grid X/Y/Z axes, and thus the total extent of the
            volume: `[width, height, depth] * voxel_size`.
            Anisotropic spacing (as is common for medical scans) is expressed here.
            Each dimension must be finite and positive.

            Defaults to `[1.0, 1.0, 1.0]`.
        translation:
            Translation of the minimum corner of voxel `[0, 0, 0]`.

            Together with [`components.RotationQuat`][rerun.components.RotationQuat], this defines the pose of the volume
            relative to the entity's coordinate frame.

            If not set, the minimum corner is placed at the origin of the entity's coordinate frame.
        quaternion:
            Rotation of the volume via quaternion.

            Together with [`components.Translation3D`][rerun.components.Translation3D], this defines the pose of the volume
            relative to the entity's coordinate frame.
            The rotation is around the minimum corner of voxel `[0, 0, 0]`, and is applied before the
            translation.
        value_range:
            How to map `values` to opacity and color.

            If not specified, the range is estimated from the data.
        colormap:
            Colormap applied to the values after mapping them through `value_range`.

            Defaults to Turbo.
        opacity:
            Overall opacity of the volume.

            The opacity of a single voxel is its value (normalized through `value_range`) scaled by
            this, i.e. a linear ramp: low values are transparent, high values are opaque.
            Lowering this makes the interior of the volume visible.

            Defaults to 1.0.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                values=values,
                voxel_size=voxel_size,
                translation=translation,
                quaternion=quaternion,
                value_range=value_range,
                colormap=colormap,
                opacity=opacity,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "Volume3D:values": values,
            "Volume3D:voxel_size": voxel_size,
            "Volume3D:translation": translation,
            "Volume3D:quaternion": quaternion,
            "Volume3D:value_range": value_range,
            "Volume3D:colormap": colormap,
            "Volume3D:opacity": opacity,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type):
                    elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                    if arrow_array.type.list_size == elem_flat_len:
                        # The product of the last dimensions of the shape are equal to the size of the fixed size list array,
                        # so we have `num_rows` single element batches (each element is a fixed sized list).
                        batch_length = 1
                    else:
                        batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                else:
                    # For primitive types, derive batch_length from the actual arrow array length
                    # since the input shape can be misleading (e.g. colors [R,G,B] -> single uint32).
                    batch_length = len(arrow_array) // num_rows if num_rows > 0 else 1

                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    values: components.TensorDataBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TensorDataBatch._converter,  # type: ignore[misc]
    )
    # The scalar value of each voxel, as a 3D tensor with dimensions ordered `[z, y, x]`.
    #
    # Currently only `f16` are supported.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    voxel_size: components.VoxelSizeBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VoxelSizeBatch._converter,  # type: ignore[misc]
    )
    # The scene-unit dimensions of a single voxel cell.
    #
    # This defines the voxel size along the local grid X/Y/Z axes, and thus the total extent of the
    # volume: `[width, height, depth] * voxel_size`.
    # Anisotropic spacing (as is common for medical scans) is expressed here.
    # Each dimension must be finite and positive.
    #
    # Defaults to `[1.0, 1.0, 1.0]`.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    translation: components.Translation3DBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.Translation3DBatch._converter,  # type: ignore[misc]
    )
    # Translation of the minimum corner of voxel `[0, 0, 0]`.
    #
    # Together with [`components.RotationQuat`][rerun.components.RotationQuat], this defines the pose of the volume
    # relative to the entity's coordinate frame.
    #
    # If not set, the minimum corner is placed at the origin of the entity's coordinate frame.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    quaternion: components.RotationQuatBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.RotationQuatBatch._converter,  # type: ignore[misc]
    )
    # Rotation of the volume via quaternion.
    #
    # Together with [`components.Translation3D`][rerun.components.Translation3D], this defines the pose of the volume
    # relative to the entity's coordinate frame.
    # The rotation is around the minimum corner of voxel `[0, 0, 0]`, and is applied before the
    # translation.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    value_range: components.ValueRangeBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ValueRangeBatch._converter,  # type: ignore[misc]
    )
    # How to map `values` to opacity and color.
    #
    # If not specified, the range is estimated from the data.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    colormap: components.ColormapBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ColormapBatch._converter,  # type: ignore[misc]
    )
    # Colormap applied to the values after mapping them through `value_range`.
    #
    # Defaults to Turbo.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    opacity: components.OpacityBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.OpacityBatch._converter,  # type: ignore[misc]
    )
    # Overall opacity of the volume.
    #
    # The opacity of a single voxel is its value (normalized through `value_range`) scaled by
    # this, i.e. a linear ramp: low values are transparent, high values are opaque.
    # Lowering this makes the interior of the volume visible.
    #
    # Defaults to 1.0.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]

    def visualizer(self, *, mappings: list[VisualizerComponentMappingLike] | None = None) -> Visualizer:
        """
        Creates a visualizer for this archetype, using all currently set values as overrides.

        Parameters
        ----------
        mappings:
            Optional component mappings to control how the visualizer sources its data.

            ⚠️ **Experimental**: Component mappings are an experimental feature and may change.
            See https://github.com/rerun-io/rerun/issues/10631 for more information.

        """
        return Visualizer("Volume3D", overrides=self.as_component_batches(), mappings=mappings)
