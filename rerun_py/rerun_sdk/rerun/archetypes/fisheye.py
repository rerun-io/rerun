# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/fisheye.fbs".

# You can extend this class by creating a "FisheyeExt" class in "fisheye_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
)
from ..blueprint import VisualizableArchetype, Visualizer
from ..error_utils import catch_and_log_exceptions
from .fisheye_ext import FisheyeExt

if TYPE_CHECKING:
    from ..blueprint.datatypes import VisualizerComponentMappingLike

__all__ = ["Fisheye"]


@define(str=False, repr=False, init=False)
class Fisheye(FisheyeExt, Archetype, VisualizableArchetype):
    """
    **Archetype**: Camera fisheye projection using the equidistant (OpenCV) model.

    This archetype shares the same linear intrinsic matrix ([`components.PinholeProjection`][rerun.components.PinholeProjection]) as [`archetypes.Pinhole`][rerun.archetypes.Pinhole],
    plus adds fisheye radial distortion coefficients ([`components.FisheyeCoefficients`][rerun.components.FisheyeCoefficients]).

    The equidistant model computes the distorted angle as:
      theta_d = theta * (1 + k1*theta^2 + k2*theta^4 + k3*theta^6 + k4*theta^8)

    When all distortion coefficients are zero, the model reduces to the pure equidistant projection (r = f * theta).

    If [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is logged for the same child/parent relationship (e.g. for the camera extrinsics), it takes precedence over [`archetypes.Fisheye`][rerun.archetypes.Fisheye].

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    # __init__ can be found in fisheye_ext.py

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            image_from_camera=None,
            resolution=None,
            distortion_coefficients=None,
            camera_xyz=None,
            child_frame=None,
            parent_frame=None,
            image_plane_distance=None,
            color=None,
            line_width=None,
        )

    @classmethod
    def _clear(cls) -> Fisheye:
        """Produce an empty Fisheye, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        image_from_camera: datatypes.Mat3x3Like | None = None,
        resolution: datatypes.Vec2DLike | None = None,
        distortion_coefficients: datatypes.Vec4DLike | None = None,
        camera_xyz: datatypes.ViewCoordinatesLike | None = None,
        child_frame: datatypes.Utf8Like | None = None,
        parent_frame: datatypes.Utf8Like | None = None,
        image_plane_distance: datatypes.Float32Like | None = None,
        color: datatypes.Rgba32Like | None = None,
        line_width: datatypes.Float32Like | None = None,
    ) -> Fisheye:
        """
        Update only some specific fields of a `Fisheye`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        image_from_camera:
            Camera projection, from image coordinates to view coordinates (linear intrinsics).

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        resolution:
            Pixel resolution (usually integers) of child image space. Width and height.

            Example:
            ```text
            [1920.0, 1440.0]
            ```

            `image_from_camera` project onto the space spanned by `(0,0)` and `resolution - 1`.

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        distortion_coefficients:
            Fisheye radial distortion coefficients (k1, k2, k3, k4) for the equidistant model.

            Defaults to `[0, 0, 0, 0]` (no distortion beyond the linear equidistant projection).
        camera_xyz:
            Sets the view coordinates for the camera.

            All common values are available as constants on the [`components.ViewCoordinates`][rerun.components.ViewCoordinates] class.

            The default is `ViewCoordinates::RDF`, i.e. X=Right, Y=Down, Z=Forward, and this is also the recommended setting.
            This means that the camera frustum will point along the positive Z axis of the parent space,
            and the cameras "up" direction will be along the negative Y axis of the parent space.

            The camera frustum will point whichever axis is set to `F` (or the opposite of `B`).
            When logging a depth image under this entity, this is the direction the point cloud will be projected.
            With `RDF`, the default forward is +Z.

            The frustum's "up" direction will be whichever axis is set to `U` (or the opposite of `D`).
            This will match the negative Y direction of pixel space (all images are assumed to have xyz=RDF).
            With `RDF`, the default is up is -Y.

            The frustum's "right" direction will be whichever axis is set to `R` (or the opposite of `L`).
            This will match the positive X direction of pixel space (all images are assumed to have xyz=RDF).
            With `RDF`, the default right is +x.

            Other common formats are `RUB` (X=Right, Y=Up, Z=Back) and `FLU` (X=Forward, Y=Left, Z=Up).

            NOTE: setting this to something else than `RDF` (the default) will change the orientation of the camera frustum,
            and make the pinhole matrix not match up with the coordinate system of the fisheye entity.

            The pinhole matrix (the `image_from_camera` argument) always project along the third (Z) axis,
            but will be re-oriented to project along the forward axis of the `camera_xyz` argument.
        child_frame:
            The child frame this transform transforms from.

            The entity at which the transform relationship of any given child frame is specified mustn't change over time, but is allowed to be different for static time.
            E.g. if you specified the child frame `"robot_arm"` on an entity named `"my_transforms"`, you may not log transforms
            with the child frame `"robot_arm"` on any other entity than `"my_transforms"` unless one of them was logged with static time.

            If not specified, this is set to the implicit transform frame of the current entity path.
            This means that if a [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is set on an entity called `/my/entity/path` then this will default to `tf#/my/entity/path`.

            To set the frame an entity is part of see [`archetypes.CoordinateFrame`][rerun.archetypes.CoordinateFrame].

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        parent_frame:
            The parent frame this transform transforms into.

            If not specified, this is set to the implicit transform frame of the current entity path's parent.
            This means that if a [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is set on an entity called `/my/entity/path` then this will default to `tf#/my/entity`.

            To set the frame an entity is part of see [`archetypes.CoordinateFrame`][rerun.archetypes.CoordinateFrame].

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        image_plane_distance:
            The distance from the camera origin to the image plane when the projection is shown in a 3D viewer.

            This is only used for visualization purposes, and does not affect the projection itself.
        color:
            Color of the camera wireframe.
        line_width:
            Width of the camera wireframe lines.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "image_from_camera": image_from_camera,
                "resolution": resolution,
                "distortion_coefficients": distortion_coefficients,
                "camera_xyz": camera_xyz,
                "child_frame": child_frame,
                "parent_frame": parent_frame,
                "image_plane_distance": image_plane_distance,
                "color": color,
                "line_width": line_width,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> Fisheye:
        """Clear all the fields of a `Fisheye`."""
        return cls.from_fields(clear_unset=True)

    @classmethod
    def columns(
        cls,
        *,
        image_from_camera: datatypes.Mat3x3ArrayLike | None = None,
        resolution: datatypes.Vec2DArrayLike | None = None,
        distortion_coefficients: datatypes.Vec4DArrayLike | None = None,
        camera_xyz: datatypes.ViewCoordinatesArrayLike | None = None,
        child_frame: datatypes.Utf8ArrayLike | None = None,
        parent_frame: datatypes.Utf8ArrayLike | None = None,
        image_plane_distance: datatypes.Float32ArrayLike | None = None,
        color: datatypes.Rgba32ArrayLike | None = None,
        line_width: datatypes.Float32ArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        image_from_camera:
            Camera projection, from image coordinates to view coordinates (linear intrinsics).

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        resolution:
            Pixel resolution (usually integers) of child image space. Width and height.

            Example:
            ```text
            [1920.0, 1440.0]
            ```

            `image_from_camera` project onto the space spanned by `(0,0)` and `resolution - 1`.

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        distortion_coefficients:
            Fisheye radial distortion coefficients (k1, k2, k3, k4) for the equidistant model.

            Defaults to `[0, 0, 0, 0]` (no distortion beyond the linear equidistant projection).
        camera_xyz:
            Sets the view coordinates for the camera.

            All common values are available as constants on the [`components.ViewCoordinates`][rerun.components.ViewCoordinates] class.

            The default is `ViewCoordinates::RDF`, i.e. X=Right, Y=Down, Z=Forward, and this is also the recommended setting.
            This means that the camera frustum will point along the positive Z axis of the parent space,
            and the cameras "up" direction will be along the negative Y axis of the parent space.

            The camera frustum will point whichever axis is set to `F` (or the opposite of `B`).
            When logging a depth image under this entity, this is the direction the point cloud will be projected.
            With `RDF`, the default forward is +Z.

            The frustum's "up" direction will be whichever axis is set to `U` (or the opposite of `D`).
            This will match the negative Y direction of pixel space (all images are assumed to have xyz=RDF).
            With `RDF`, the default is up is -Y.

            The frustum's "right" direction will be whichever axis is set to `R` (or the opposite of `L`).
            This will match the positive X direction of pixel space (all images are assumed to have xyz=RDF).
            With `RDF`, the default right is +x.

            Other common formats are `RUB` (X=Right, Y=Up, Z=Back) and `FLU` (X=Forward, Y=Left, Z=Up).

            NOTE: setting this to something else than `RDF` (the default) will change the orientation of the camera frustum,
            and make the pinhole matrix not match up with the coordinate system of the fisheye entity.

            The pinhole matrix (the `image_from_camera` argument) always project along the third (Z) axis,
            but will be re-oriented to project along the forward axis of the `camera_xyz` argument.
        child_frame:
            The child frame this transform transforms from.

            The entity at which the transform relationship of any given child frame is specified mustn't change over time, but is allowed to be different for static time.
            E.g. if you specified the child frame `"robot_arm"` on an entity named `"my_transforms"`, you may not log transforms
            with the child frame `"robot_arm"` on any other entity than `"my_transforms"` unless one of them was logged with static time.

            If not specified, this is set to the implicit transform frame of the current entity path.
            This means that if a [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is set on an entity called `/my/entity/path` then this will default to `tf#/my/entity/path`.

            To set the frame an entity is part of see [`archetypes.CoordinateFrame`][rerun.archetypes.CoordinateFrame].

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        parent_frame:
            The parent frame this transform transforms into.

            If not specified, this is set to the implicit transform frame of the current entity path's parent.
            This means that if a [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is set on an entity called `/my/entity/path` then this will default to `tf#/my/entity`.

            To set the frame an entity is part of see [`archetypes.CoordinateFrame`][rerun.archetypes.CoordinateFrame].

            Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
        image_plane_distance:
            The distance from the camera origin to the image plane when the projection is shown in a 3D viewer.

            This is only used for visualization purposes, and does not affect the projection itself.
        color:
            Color of the camera wireframe.
        line_width:
            Width of the camera wireframe lines.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                image_from_camera=image_from_camera,
                resolution=resolution,
                distortion_coefficients=distortion_coefficients,
                camera_xyz=camera_xyz,
                child_frame=child_frame,
                parent_frame=parent_frame,
                image_plane_distance=image_plane_distance,
                color=color,
                line_width=line_width,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "Fisheye:image_from_camera": image_from_camera,
            "Fisheye:resolution": resolution,
            "Fisheye:distortion_coefficients": distortion_coefficients,
            "Fisheye:camera_xyz": camera_xyz,
            "Fisheye:child_frame": child_frame,
            "Fisheye:parent_frame": parent_frame,
            "Fisheye:image_plane_distance": image_plane_distance,
            "Fisheye:color": color,
            "Fisheye:line_width": line_width,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type) and arrow_array.type.list_size == elem_flat_len:
                    # If the product of the last dimensions of the shape are equal to the size of the fixed size list array,
                    # we have `num_rows` single element batches (each element is a fixed sized list).
                    # (This should have been already validated by conversion to the arrow_array)
                    batch_length = 1
                else:
                    batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]

                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]
                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    image_from_camera: components.PinholeProjectionBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.PinholeProjectionBatch._converter,  # type: ignore[misc]
    )
    # Camera projection, from image coordinates to view coordinates (linear intrinsics).
    #
    # Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    resolution: components.ResolutionBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ResolutionBatch._converter,  # type: ignore[misc]
    )
    # Pixel resolution (usually integers) of child image space. Width and height.
    #
    # Example:
    # ```text
    # [1920.0, 1440.0]
    # ```
    #
    # `image_from_camera` project onto the space spanned by `(0,0)` and `resolution - 1`.
    #
    # Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    distortion_coefficients: components.FisheyeCoefficientsBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.FisheyeCoefficientsBatch._converter,  # type: ignore[misc]
    )
    # Fisheye radial distortion coefficients (k1, k2, k3, k4) for the equidistant model.
    #
    # Defaults to `[0, 0, 0, 0]` (no distortion beyond the linear equidistant projection).
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    camera_xyz: components.ViewCoordinatesBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ViewCoordinatesBatch._converter,  # type: ignore[misc]
    )
    # Sets the view coordinates for the camera.
    #
    # All common values are available as constants on the [`components.ViewCoordinates`][rerun.components.ViewCoordinates] class.
    #
    # The default is `ViewCoordinates::RDF`, i.e. X=Right, Y=Down, Z=Forward, and this is also the recommended setting.
    # This means that the camera frustum will point along the positive Z axis of the parent space,
    # and the cameras "up" direction will be along the negative Y axis of the parent space.
    #
    # The camera frustum will point whichever axis is set to `F` (or the opposite of `B`).
    # When logging a depth image under this entity, this is the direction the point cloud will be projected.
    # With `RDF`, the default forward is +Z.
    #
    # The frustum's "up" direction will be whichever axis is set to `U` (or the opposite of `D`).
    # This will match the negative Y direction of pixel space (all images are assumed to have xyz=RDF).
    # With `RDF`, the default is up is -Y.
    #
    # The frustum's "right" direction will be whichever axis is set to `R` (or the opposite of `L`).
    # This will match the positive X direction of pixel space (all images are assumed to have xyz=RDF).
    # With `RDF`, the default right is +x.
    #
    # Other common formats are `RUB` (X=Right, Y=Up, Z=Back) and `FLU` (X=Forward, Y=Left, Z=Up).
    #
    # NOTE: setting this to something else than `RDF` (the default) will change the orientation of the camera frustum,
    # and make the pinhole matrix not match up with the coordinate system of the fisheye entity.
    #
    # The pinhole matrix (the `image_from_camera` argument) always project along the third (Z) axis,
    # but will be re-oriented to project along the forward axis of the `camera_xyz` argument.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    child_frame: components.TransformFrameIdBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TransformFrameIdBatch._converter,  # type: ignore[misc]
    )
    # The child frame this transform transforms from.
    #
    # The entity at which the transform relationship of any given child frame is specified mustn't change over time, but is allowed to be different for static time.
    # E.g. if you specified the child frame `"robot_arm"` on an entity named `"my_transforms"`, you may not log transforms
    # with the child frame `"robot_arm"` on any other entity than `"my_transforms"` unless one of them was logged with static time.
    #
    # If not specified, this is set to the implicit transform frame of the current entity path.
    # This means that if a [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is set on an entity called `/my/entity/path` then this will default to `tf#/my/entity/path`.
    #
    # To set the frame an entity is part of see [`archetypes.CoordinateFrame`][rerun.archetypes.CoordinateFrame].
    #
    # Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    parent_frame: components.TransformFrameIdBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TransformFrameIdBatch._converter,  # type: ignore[misc]
    )
    # The parent frame this transform transforms into.
    #
    # If not specified, this is set to the implicit transform frame of the current entity path's parent.
    # This means that if a [`archetypes.Transform3D`][rerun.archetypes.Transform3D] is set on an entity called `/my/entity/path` then this will default to `tf#/my/entity`.
    #
    # To set the frame an entity is part of see [`archetypes.CoordinateFrame`][rerun.archetypes.CoordinateFrame].
    #
    # Any update to this field will reset all other transform properties that aren't changed in the same log call or `send_columns` row.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    image_plane_distance: components.ImagePlaneDistanceBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ImagePlaneDistanceBatch._converter,  # type: ignore[misc]
    )
    # The distance from the camera origin to the image plane when the projection is shown in a 3D viewer.
    #
    # This is only used for visualization purposes, and does not affect the projection itself.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    color: components.ColorBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ColorBatch._converter,  # type: ignore[misc]
    )
    # Color of the camera wireframe.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    line_width: components.RadiusBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.RadiusBatch._converter,  # type: ignore[misc]
    )
    # Width of the camera wireframe lines.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]

    def visualizer(self, *, mappings: list[VisualizerComponentMappingLike] | None = None) -> Visualizer:
        """
        Creates a visualizer for this archetype, using all currently set values as overrides.

        Parameters
        ----------
        mappings:
            Optional component mappings to control how the visualizer sources its data.

            ⚠️ **Experimental**: Component mappings are an experimental feature and may change.
            See https://github.com/rerun-io/rerun/issues/10631 for more information.

        """
        return Visualizer("FisheyeCameras", overrides=self.as_component_batches(), mappings=mappings)
