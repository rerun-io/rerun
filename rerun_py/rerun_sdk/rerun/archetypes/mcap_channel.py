# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/mcap_channel.fbs".

# You can extend this class by creating a "McapChannelExt" class in "mcap_channel_ext.py".

from __future__ import annotations

from typing import Any

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
)
from ..error_utils import catch_and_log_exceptions

__all__ = ["McapChannel"]


@define(str=False, repr=False, init=False)
class McapChannel(Archetype):
    """
    **Archetype**: A channel within an MCAP file that defines how messages are structured and encoded.

    Channels in MCAP files group messages by topic and define their encoding format.
    Each channel has a unique identifier and specifies the message schema and encoding used
    for all messages published to that topic.

    See also [`archetypes.McapMessage`][rerun.archetypes.McapMessage] for individual messages within a channel,
    [`archetypes.McapSchema`][rerun.archetypes.McapSchema] for the data structure definitions, and the
    [MCAP specification](https://mcap.dev/) for complete format details.

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
    """

    def __init__(
        self: Any,
        *,
        id: datatypes.UInt16Like,
        topic: datatypes.Utf8Like,
        message_encoding: datatypes.Utf8Like,
        metadata: components.KeyValuePairsLike | None = None,
    ) -> None:
        """
        Create a new instance of the McapChannel archetype.

        Parameters
        ----------
        id:
            Unique identifier for this channel within the MCAP file.

            Channel IDs must be unique within a single MCAP file and are used to associate
            messages with their corresponding channel definition.
        topic:
            The topic name that this channel publishes to.

            Topics are hierarchical paths from the original robotics system (e.g., "/sensors/camera/image")
            that categorize and organize different data streams.
            Topics are separate from Rerun's entity paths, but they often can be mapped to them.
        message_encoding:
            The encoding format used for messages in this channel.

            Common encodings include:
            * `ros1` - ROS1 message format
            * `cdr` - Common Data Representation (CDR) message format, used by ROS2
            * `protobuf` - Protocol Buffers
            * `json` - JSON encoding
        metadata:
            Additional metadata for this channel stored as key-value pairs.

            This can include channel-specific configuration, description, units, coordinate frames,
            or any other contextual information that helps interpret the data in this channel.

        """

        # You can define your own __init__ function as a member of McapChannelExt in mcap_channel_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(id=id, topic=topic, message_encoding=message_encoding, metadata=metadata)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            id=None,
            topic=None,
            message_encoding=None,
            metadata=None,
        )

    @classmethod
    def _clear(cls) -> McapChannel:
        """Produce an empty McapChannel, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        id: datatypes.UInt16Like | None = None,
        topic: datatypes.Utf8Like | None = None,
        message_encoding: datatypes.Utf8Like | None = None,
        metadata: components.KeyValuePairsLike | None = None,
    ) -> McapChannel:
        """
        Update only some specific fields of a `McapChannel`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        id:
            Unique identifier for this channel within the MCAP file.

            Channel IDs must be unique within a single MCAP file and are used to associate
            messages with their corresponding channel definition.
        topic:
            The topic name that this channel publishes to.

            Topics are hierarchical paths from the original robotics system (e.g., "/sensors/camera/image")
            that categorize and organize different data streams.
            Topics are separate from Rerun's entity paths, but they often can be mapped to them.
        message_encoding:
            The encoding format used for messages in this channel.

            Common encodings include:
            * `ros1` - ROS1 message format
            * `cdr` - Common Data Representation (CDR) message format, used by ROS2
            * `protobuf` - Protocol Buffers
            * `json` - JSON encoding
        metadata:
            Additional metadata for this channel stored as key-value pairs.

            This can include channel-specific configuration, description, units, coordinate frames,
            or any other contextual information that helps interpret the data in this channel.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "id": id,
                "topic": topic,
                "message_encoding": message_encoding,
                "metadata": metadata,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> McapChannel:
        """Clear all the fields of a `McapChannel`."""
        return cls.from_fields(clear_unset=True)

    @classmethod
    def columns(
        cls,
        *,
        id: datatypes.UInt16ArrayLike | None = None,
        topic: datatypes.Utf8ArrayLike | None = None,
        message_encoding: datatypes.Utf8ArrayLike | None = None,
        metadata: components.KeyValuePairsArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        id:
            Unique identifier for this channel within the MCAP file.

            Channel IDs must be unique within a single MCAP file and are used to associate
            messages with their corresponding channel definition.
        topic:
            The topic name that this channel publishes to.

            Topics are hierarchical paths from the original robotics system (e.g., "/sensors/camera/image")
            that categorize and organize different data streams.
            Topics are separate from Rerun's entity paths, but they often can be mapped to them.
        message_encoding:
            The encoding format used for messages in this channel.

            Common encodings include:
            * `ros1` - ROS1 message format
            * `cdr` - Common Data Representation (CDR) message format, used by ROS2
            * `protobuf` - Protocol Buffers
            * `json` - JSON encoding
        metadata:
            Additional metadata for this channel stored as key-value pairs.

            This can include channel-specific configuration, description, units, coordinate frames,
            or any other contextual information that helps interpret the data in this channel.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                id=id,
                topic=topic,
                message_encoding=message_encoding,
                metadata=metadata,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "McapChannel:id": id,
            "McapChannel:topic": topic,
            "McapChannel:message_encoding": message_encoding,
            "McapChannel:metadata": metadata,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type) and arrow_array.type.list_size == elem_flat_len:
                    # If the product of the last dimensions of the shape are equal to the size of the fixed size list array,
                    # we have `num_rows` single element batches (each element is a fixed sized list).
                    # (This should have been already validated by conversion to the arrow_array)
                    batch_length = 1
                else:
                    batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]

                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]
                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    id: components.ChannelIdBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ChannelIdBatch._converter,  # type: ignore[misc]
    )
    # Unique identifier for this channel within the MCAP file.
    #
    # Channel IDs must be unique within a single MCAP file and are used to associate
    # messages with their corresponding channel definition.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    topic: components.TextBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TextBatch._converter,  # type: ignore[misc]
    )
    # The topic name that this channel publishes to.
    #
    # Topics are hierarchical paths from the original robotics system (e.g., "/sensors/camera/image")
    # that categorize and organize different data streams.
    # Topics are separate from Rerun's entity paths, but they often can be mapped to them.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    message_encoding: components.TextBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.TextBatch._converter,  # type: ignore[misc]
    )
    # The encoding format used for messages in this channel.
    #
    # Common encodings include:
    # * `ros1` - ROS1 message format
    # * `cdr` - Common Data Representation (CDR) message format, used by ROS2
    # * `protobuf` - Protocol Buffers
    # * `json` - JSON encoding
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    metadata: components.KeyValuePairsBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.KeyValuePairsBatch._converter,  # type: ignore[misc]
    )
    # Additional metadata for this channel stored as key-value pairs.
    #
    # This can include channel-specific configuration, description, units, coordinate frames,
    # or any other contextual information that helps interpret the data in this channel.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
