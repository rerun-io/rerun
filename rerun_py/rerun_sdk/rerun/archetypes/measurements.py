# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/build/re_type_definitions/rerun/archetypes/measurements.def.rs".

# You can extend this class by creating a "MeasurementsExt" class in "measurements_ext.py".

from __future__ import annotations

from typing import TYPE_CHECKING, Any, ClassVar

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, encodings
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
    ComponentDescriptor,
)
from ..blueprint import VisualizableArchetype, Visualizer
from ..error_utils import catch_and_log_exceptions

if TYPE_CHECKING:
    from ..blueprint.encodings import VisualizerComponentMappingLike

__all__ = ["Measurements"]


@define(str=False, repr=False, init=False)
class Measurements(Archetype, VisualizableArchetype):
    """
    **Archetype**: One or more scalar measurements, each with a variance and a unit.

    Use this for sensors that report a value together with its uncertainty:
    pressure, temperature, illuminance, relative humidity, range, and so on.

    In a [`views.TimeSeriesView`][rerun.blueprint.views.TimeSeriesView] each series is drawn as a line with a translucent
    band around it, one standard deviation (the square root of the variance) wide in each direction.
    Leave `variances` unset if the uncertainty is unknown.

    The current timeline value is used for the time/X-axis, so measurements should not be
    static. Number of values per timestamp is expected to be the same over time.

    Unlike [`archetypes.Scalars`][rerun.archetypes.Scalars], this archetype carries its own styling, so values and style
    are logged together.
    Changes over time are supported for most but not all styling fields (see respective fields for details).

    ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**

    Example
    -------
    ### Pressure with variance:
    ```python
    import math

    import rerun as rr

    rr.init("rerun_example_measurements_simple", spawn=True)

    # Two parallel pressure sensors (in Pa), each with slowly drifting variance.
    for step in range(64):
        rr.set_time("step", sequence=step)
        pressures = [
            101_325.0 + 50.0 * math.sin(step / 10.0),
            101_300.0 + 30.0 * math.cos(step / 8.0),
        ]
        variances = [
            100.0 + 25.0 * math.sin(step / 7.0),
            80.0 + 15.0 * math.cos(step / 11.0),
        ]
        rr.log(
            "pressure",
            rr.Measurements(values=pressures, variances=variances, units="Pa"),
        )
    ```
    <center>
    <picture>
      <source media="(max-width: 480px)" srcset="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/480w.png">
      <source media="(max-width: 768px)" srcset="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/768w.png">
      <source media="(max-width: 1024px)" srcset="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/1024w.png">
      <img src="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/full.png" width="640">
    </picture>
    </center>

    """

    NAME: ClassVar[str] = "rerun.archetypes.Measurements"

    def __init__(
        self: Any,
        values: encodings.Float64ArrayLike,
        *,
        variances: encodings.Float64ArrayLike | None = None,
        units: encodings.Utf8ArrayLike | None = None,
        colors: encodings.Rgba32ArrayLike | None = None,
        widths: encodings.Float32ArrayLike | None = None,
        names: encodings.Utf8ArrayLike | None = None,
        visible_series: encodings.BoolArrayLike | None = None,
        aggregation_policy: components.AggregationPolicyLike | None = None,
        interpolation_mode: components.InterpolationModeLike | None = None,
    ) -> None:
        """
        Create a new instance of the Measurements archetype.

        Parameters
        ----------
        values:
            The measured scalar values.
        variances:
            Variances of the measurements (σ², in the units of `values` squared).

            When set, length is expected to match `values`.
        units:
            Units of the measurements, shown in the legend and in tooltips.

            When set, length is expected to match `values`.
            Expected to be unchanging over time.
        colors:
            Color for the corresponding series.

            May change over time, but can cause discontinuities in the line.
        widths:
            Stroke width for the corresponding series.

            May change over time, but can cause discontinuities in the line.
        names:
            Display name of the series.

            Used in the legend. Expected to be unchanging over time.
        visible_series:
            Which series are visible.

            If not set, all series on this entity are visible.
            Unlike with the regular visibility property of the entire entity, any series that is hidden
            via this property will still be visible in the legend.

            May change over time, but can cause discontinuities in the line.
        aggregation_policy:
            Configures the zoom-dependent scalar aggregation.

            This is done only if steps on the X axis go below a single pixel,
            i.e. a single pixel covers more than one tick worth of data. It can greatly improve performance
            (and readability) in such situations as it prevents overdraw.

            Expected to be unchanging over time.
        interpolation_mode:
            Specifies how values between data points are interpolated.

            Defaults to linear interpolation. Use one of the `Step*` variants for a stepped (staircase) line.

            Expected to be unchanging over time.

        """

        # You can define your own __init__ function as a member of MeasurementsExt in measurements_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(
                values=values,
                variances=variances,
                units=units,
                colors=colors,
                widths=widths,
                names=names,
                visible_series=visible_series,
                aggregation_policy=aggregation_policy,
                interpolation_mode=interpolation_mode,
            )
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            values=None,
            variances=None,
            units=None,
            colors=None,
            widths=None,
            names=None,
            visible_series=None,
            aggregation_policy=None,
            interpolation_mode=None,
        )

    @classmethod
    def _clear(cls) -> Measurements:
        """Produce an empty Measurements, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        values: encodings.Float64ArrayLike | None = None,
        variances: encodings.Float64ArrayLike | None = None,
        units: encodings.Utf8ArrayLike | None = None,
        colors: encodings.Rgba32ArrayLike | None = None,
        widths: encodings.Float32ArrayLike | None = None,
        names: encodings.Utf8ArrayLike | None = None,
        visible_series: encodings.BoolArrayLike | None = None,
        aggregation_policy: components.AggregationPolicyLike | None = None,
        interpolation_mode: components.InterpolationModeLike | None = None,
    ) -> Measurements:
        """
        Update only some specific fields of a `Measurements`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        values:
            The measured scalar values.
        variances:
            Variances of the measurements (σ², in the units of `values` squared).

            When set, length is expected to match `values`.
        units:
            Units of the measurements, shown in the legend and in tooltips.

            When set, length is expected to match `values`.
            Expected to be unchanging over time.
        colors:
            Color for the corresponding series.

            May change over time, but can cause discontinuities in the line.
        widths:
            Stroke width for the corresponding series.

            May change over time, but can cause discontinuities in the line.
        names:
            Display name of the series.

            Used in the legend. Expected to be unchanging over time.
        visible_series:
            Which series are visible.

            If not set, all series on this entity are visible.
            Unlike with the regular visibility property of the entire entity, any series that is hidden
            via this property will still be visible in the legend.

            May change over time, but can cause discontinuities in the line.
        aggregation_policy:
            Configures the zoom-dependent scalar aggregation.

            This is done only if steps on the X axis go below a single pixel,
            i.e. a single pixel covers more than one tick worth of data. It can greatly improve performance
            (and readability) in such situations as it prevents overdraw.

            Expected to be unchanging over time.
        interpolation_mode:
            Specifies how values between data points are interpolated.

            Defaults to linear interpolation. Use one of the `Step*` variants for a stepped (staircase) line.

            Expected to be unchanging over time.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "values": values,
                "variances": variances,
                "units": units,
                "colors": colors,
                "widths": widths,
                "names": names,
                "visible_series": visible_series,
                "aggregation_policy": aggregation_policy,
                "interpolation_mode": interpolation_mode,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> Measurements:
        """Clear all the fields of a `Measurements`."""
        return cls.from_fields(clear_unset=True)

    @staticmethod
    def descriptor_values() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:values",
            archetype=Measurements.NAME,
            component_type=components.ScalarBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_variances() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:variances",
            archetype=Measurements.NAME,
            component_type=components.VarianceBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_units() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:units",
            archetype=Measurements.NAME,
            component_type=components.UnitBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_colors() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:colors",
            archetype=Measurements.NAME,
            component_type=components.ColorBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_widths() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:widths",
            archetype=Measurements.NAME,
            component_type=components.StrokeWidthBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_names() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:names",
            archetype=Measurements.NAME,
            component_type=components.NameBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_visible_series() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:visible_series",
            archetype=Measurements.NAME,
            component_type=components.VisibleBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_aggregation_policy() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:aggregation_policy",
            archetype=Measurements.NAME,
            component_type=components.AggregationPolicyBatch._COMPONENT_TYPE,
        )

    @staticmethod
    def descriptor_interpolation_mode() -> ComponentDescriptor:
        return ComponentDescriptor(
            "Measurements:interpolation_mode",
            archetype=Measurements.NAME,
            component_type=components.InterpolationModeBatch._COMPONENT_TYPE,
        )

    @classmethod
    def columns(
        cls,
        *,
        values: encodings.Float64ArrayLike | None = None,
        variances: encodings.Float64ArrayLike | None = None,
        units: encodings.Utf8ArrayLike | None = None,
        colors: encodings.Rgba32ArrayLike | None = None,
        widths: encodings.Float32ArrayLike | None = None,
        names: encodings.Utf8ArrayLike | None = None,
        visible_series: encodings.BoolArrayLike | None = None,
        aggregation_policy: components.AggregationPolicyArrayLike | None = None,
        interpolation_mode: components.InterpolationModeArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        values:
            The measured scalar values.
        variances:
            Variances of the measurements (σ², in the units of `values` squared).

            When set, length is expected to match `values`.
        units:
            Units of the measurements, shown in the legend and in tooltips.

            When set, length is expected to match `values`.
            Expected to be unchanging over time.
        colors:
            Color for the corresponding series.

            May change over time, but can cause discontinuities in the line.
        widths:
            Stroke width for the corresponding series.

            May change over time, but can cause discontinuities in the line.
        names:
            Display name of the series.

            Used in the legend. Expected to be unchanging over time.
        visible_series:
            Which series are visible.

            If not set, all series on this entity are visible.
            Unlike with the regular visibility property of the entire entity, any series that is hidden
            via this property will still be visible in the legend.

            May change over time, but can cause discontinuities in the line.
        aggregation_policy:
            Configures the zoom-dependent scalar aggregation.

            This is done only if steps on the X axis go below a single pixel,
            i.e. a single pixel covers more than one tick worth of data. It can greatly improve performance
            (and readability) in such situations as it prevents overdraw.

            Expected to be unchanging over time.
        interpolation_mode:
            Specifies how values between data points are interpolated.

            Defaults to linear interpolation. Use one of the `Step*` variants for a stepped (staircase) line.

            Expected to be unchanging over time.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                values=values,
                variances=variances,
                units=units,
                colors=colors,
                widths=widths,
                names=names,
                visible_series=visible_series,
                aggregation_policy=aggregation_policy,
                interpolation_mode=interpolation_mode,
            )

        batches = inst.as_component_batches()
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {
            "Measurements:values": values,
            "Measurements:variances": variances,
            "Measurements:units": units,
            "Measurements:colors": colors,
            "Measurements:widths": widths,
            "Measurements:names": names,
            "Measurements:visible_series": visible_series,
            "Measurements:aggregation_policy": aggregation_policy,
            "Measurements:interpolation_mode": interpolation_mode,
        }
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays and fixed size list arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type) or pa.types.is_fixed_size_list(arrow_array.type):
                param = kwargs[batch.component_descriptor().component]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]
                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]

                if pa.types.is_fixed_size_list(arrow_array.type):
                    elem_flat_len = int(np.prod(shape[1:])) if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                    if arrow_array.type.list_size == elem_flat_len:
                        # The product of the last dimensions of the shape are equal to the size of the fixed size list array,
                        # so we have `num_rows` single element batches (each element is a fixed sized list).
                        batch_length = 1
                    else:
                        batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                else:
                    # For primitive types, derive batch_length from the actual arrow array length
                    # since the input shape can be misleading (e.g. colors [R,G,B] -> single uint32).
                    batch_length = len(arrow_array) // num_rows if num_rows > 0 else 1

                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        return ComponentColumnList(columns)

    values: components.ScalarBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ScalarBatch._converter,  # type: ignore[misc]
    )
    # The measured scalar values.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    variances: components.VarianceBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VarianceBatch._converter,  # type: ignore[misc]
    )
    # Variances of the measurements (σ², in the units of `values` squared).
    #
    # When set, length is expected to match `values`.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    units: components.UnitBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.UnitBatch._converter,  # type: ignore[misc]
    )
    # Units of the measurements, shown in the legend and in tooltips.
    #
    # When set, length is expected to match `values`.
    # Expected to be unchanging over time.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    colors: components.ColorBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.ColorBatch._converter,  # type: ignore[misc]
    )
    # Color for the corresponding series.
    #
    # May change over time, but can cause discontinuities in the line.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    widths: components.StrokeWidthBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.StrokeWidthBatch._converter,  # type: ignore[misc]
    )
    # Stroke width for the corresponding series.
    #
    # May change over time, but can cause discontinuities in the line.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    names: components.NameBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.NameBatch._converter,  # type: ignore[misc]
    )
    # Display name of the series.
    #
    # Used in the legend. Expected to be unchanging over time.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    visible_series: components.VisibleBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VisibleBatch._converter,  # type: ignore[misc]
    )
    # Which series are visible.
    #
    # If not set, all series on this entity are visible.
    # Unlike with the regular visibility property of the entire entity, any series that is hidden
    # via this property will still be visible in the legend.
    #
    # May change over time, but can cause discontinuities in the line.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    aggregation_policy: components.AggregationPolicyBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.AggregationPolicyBatch._converter,  # type: ignore[misc]
    )
    # Configures the zoom-dependent scalar aggregation.
    #
    # This is done only if steps on the X axis go below a single pixel,
    # i.e. a single pixel covers more than one tick worth of data. It can greatly improve performance
    # (and readability) in such situations as it prevents overdraw.
    #
    # Expected to be unchanging over time.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    interpolation_mode: components.InterpolationModeBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.InterpolationModeBatch._converter,  # type: ignore[misc]
    )
    # Specifies how values between data points are interpolated.
    #
    # Defaults to linear interpolation. Use one of the `Step*` variants for a stepped (staircase) line.
    #
    # Expected to be unchanging over time.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]

    def visualizer(self, *, mappings: list[VisualizerComponentMappingLike] | None = None) -> Visualizer:
        """
        Creates a visualizer for this archetype, using all currently set values as overrides.

        Parameters
        ----------
        mappings:
            Optional component mappings to control how the visualizer sources its data.

            ⚠️ **Experimental**: Component mappings are an experimental feature and may change.
            See https://github.com/rerun-io/rerun/issues/10631 for more information.

        """
        return Visualizer("MeasurementsSeries", overrides=self.as_component_batches(), mappings=mappings)
