# DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/python/mod.rs
# Based on "crates/store/re_types/definitions/rerun/archetypes/entity_properties.fbs".

# You can extend this class by creating a "EntityPropertiesExt" class in "entity_properties_ext.py".

from __future__ import annotations

from typing import Any

import numpy as np
import pyarrow as pa
from attrs import define, field

from .. import components, datatypes
from .._baseclasses import (
    Archetype,
    ComponentColumnList,
)
from ..error_utils import catch_and_log_exceptions

__all__ = ["EntityProperties"]


@define(str=False, repr=False, init=False)
class EntityProperties(Archetype):
    """
    **Archetype**: General visualization properties of an entity.

    TODO(#6541): Fields of this archetype currently only have an effect when logged in the blueprint store.
    """

    def __init__(
        self: Any,
        *,
        interactive: datatypes.BoolLike | None = None,
        visible: datatypes.BoolLike | None = None,
        visible_recursive: datatypes.BoolLike | None = None,
    ) -> None:
        """
        Create a new instance of the EntityProperties archetype.

        Parameters
        ----------
        interactive:
            Whether the entity can be interacted with.

            Non interactive components may still be still visible, but mouse interactions in the view are disabled.

            Defaults to true.
        visible:
            Whether the entity is visible.

            If this is set, it will take precedence over the `visible_recursive` field
            and any `visible_recursive` setting further up in the hierarchy.

            Defaults to true.
        visible_recursive:
            Whether the entity and its children are visible.

            This property is propagated down the entity hierarchy until another child entity
            sets `visible_recursive` to a different value at which point propagation continues with that value instead.

            `visible_recursive` is ignored on any individual entity that has the `visible` field set.
            (But this does not affect tree propagation of the property)

            Defaults to true.

        """

        # You can define your own __init__ function as a member of EntityPropertiesExt in entity_properties_ext.py
        with catch_and_log_exceptions(context=self.__class__.__name__):
            self.__attrs_init__(interactive=interactive, visible=visible, visible_recursive=visible_recursive)
            return
        self.__attrs_clear__()

    def __attrs_clear__(self) -> None:
        """Convenience method for calling `__attrs_init__` with all `None`s."""
        self.__attrs_init__(
            interactive=None,
            visible=None,
            visible_recursive=None,
        )

    @classmethod
    def _clear(cls) -> EntityProperties:
        """Produce an empty EntityProperties, bypassing `__init__`."""
        inst = cls.__new__(cls)
        inst.__attrs_clear__()
        return inst

    @classmethod
    def from_fields(
        cls,
        *,
        clear_unset: bool = False,
        interactive: datatypes.BoolLike | None = None,
        visible: datatypes.BoolLike | None = None,
        visible_recursive: datatypes.BoolLike | None = None,
    ) -> EntityProperties:
        """
        Update only some specific fields of a `EntityProperties`.

        Parameters
        ----------
        clear_unset:
            If true, all unspecified fields will be explicitly cleared.
        interactive:
            Whether the entity can be interacted with.

            Non interactive components may still be still visible, but mouse interactions in the view are disabled.

            Defaults to true.
        visible:
            Whether the entity is visible.

            If this is set, it will take precedence over the `visible_recursive` field
            and any `visible_recursive` setting further up in the hierarchy.

            Defaults to true.
        visible_recursive:
            Whether the entity and its children are visible.

            This property is propagated down the entity hierarchy until another child entity
            sets `visible_recursive` to a different value at which point propagation continues with that value instead.

            `visible_recursive` is ignored on any individual entity that has the `visible` field set.
            (But this does not affect tree propagation of the property)

            Defaults to true.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            kwargs = {
                "interactive": interactive,
                "visible": visible,
                "visible_recursive": visible_recursive,
            }

            if clear_unset:
                kwargs = {k: v if v is not None else [] for k, v in kwargs.items()}  # type: ignore[misc]

            inst.__attrs_init__(**kwargs)
            return inst

        inst.__attrs_clear__()
        return inst

    @classmethod
    def cleared(cls) -> EntityProperties:
        """Clear all the fields of a `EntityProperties`."""
        return cls.from_fields(clear_unset=True)

    @classmethod
    def columns(
        cls,
        *,
        interactive: datatypes.BoolArrayLike | None = None,
        visible: datatypes.BoolArrayLike | None = None,
        visible_recursive: datatypes.BoolArrayLike | None = None,
    ) -> ComponentColumnList:
        """
        Construct a new column-oriented component bundle.

        This makes it possible to use `rr.send_columns` to send columnar data directly into Rerun.

        The returned columns will be partitioned into unit-length sub-batches by default.
        Use `ComponentColumnList.partition` to repartition the data as needed.

        Parameters
        ----------
        interactive:
            Whether the entity can be interacted with.

            Non interactive components may still be still visible, but mouse interactions in the view are disabled.

            Defaults to true.
        visible:
            Whether the entity is visible.

            If this is set, it will take precedence over the `visible_recursive` field
            and any `visible_recursive` setting further up in the hierarchy.

            Defaults to true.
        visible_recursive:
            Whether the entity and its children are visible.

            This property is propagated down the entity hierarchy until another child entity
            sets `visible_recursive` to a different value at which point propagation continues with that value instead.

            `visible_recursive` is ignored on any individual entity that has the `visible` field set.
            (But this does not affect tree propagation of the property)

            Defaults to true.

        """

        inst = cls.__new__(cls)
        with catch_and_log_exceptions(context=cls.__name__):
            inst.__attrs_init__(
                interactive=interactive,
                visible=visible,
                visible_recursive=visible_recursive,
            )

        batches = inst.as_component_batches(include_indicators=False)
        if len(batches) == 0:
            return ComponentColumnList([])

        kwargs = {"interactive": interactive, "visible": visible, "visible_recursive": visible_recursive}
        columns = []

        for batch in batches:
            arrow_array = batch.as_arrow_array()

            # For primitive arrays, we infer partition size from the input shape.
            if pa.types.is_primitive(arrow_array.type):
                param = kwargs[batch.component_descriptor().archetype_field_name]  # type: ignore[index]
                shape = np.shape(param)  # type: ignore[arg-type]

                batch_length = shape[1] if len(shape) > 1 else 1  # type: ignore[redundant-expr,misc]
                num_rows = shape[0] if len(shape) >= 1 else 1  # type: ignore[redundant-expr,misc]
                sizes = batch_length * np.ones(num_rows)
            else:
                # For non-primitive types, default to partitioning each element separately.
                sizes = np.ones(len(arrow_array))

            columns.append(batch.partition(sizes))

        indicator_column = cls.indicator().partition(np.zeros(len(sizes)))
        return ComponentColumnList([indicator_column] + columns)

    interactive: components.InteractiveBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.InteractiveBatch._converter,  # type: ignore[misc]
    )
    # Whether the entity can be interacted with.
    #
    # Non interactive components may still be still visible, but mouse interactions in the view are disabled.
    #
    # Defaults to true.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    visible: components.VisibleBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VisibleBatch._converter,  # type: ignore[misc]
    )
    # Whether the entity is visible.
    #
    # If this is set, it will take precedence over the `visible_recursive` field
    # and any `visible_recursive` setting further up in the hierarchy.
    #
    # Defaults to true.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    visible_recursive: components.VisibleRecursiveBatch | None = field(
        metadata={"component": True},
        default=None,
        converter=components.VisibleRecursiveBatch._converter,  # type: ignore[misc]
    )
    # Whether the entity and its children are visible.
    #
    # This property is propagated down the entity hierarchy until another child entity
    # sets `visible_recursive` to a different value at which point propagation continues with that value instead.
    #
    # `visible_recursive` is ignored on any individual entity that has the `visible` field set.
    # (But this does not affect tree propagation of the property)
    #
    # Defaults to true.
    #
    # (Docstring intentionally commented out to hide this field from the docs)

    __str__ = Archetype.__str__
    __repr__ = Archetype.__repr__  # type: ignore[assignment]
