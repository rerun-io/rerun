// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_types/definitions/rerun/archetypes/entity_properties.fbs".

#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]

use ::re_types_core::try_serialize_field;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch as _, SerializedComponentBatch};
use ::re_types_core::{ComponentDescriptor, ComponentName};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: General visualization properties of an entity.
///
/// TODO(#6541): Fields of this archetype currently only have an effect when logged in the blueprint store.
#[derive(Clone, Debug, Default)]
pub struct EntityProperties {
    /// Whether the entity can be interacted with.
    ///
    /// Non interactive components may still be still visible, but mouse interactions in the view are disabled.
    ///
    /// Defaults to true.
    pub interactive: Option<SerializedComponentBatch>,

    /// Whether the entity is visible.
    ///
    /// If this is set, it will take precedence over the `visible_recursive` field
    /// and any `visible_recursive` setting further up in the hierarchy.
    ///
    /// Defaults to true.
    pub visible: Option<SerializedComponentBatch>,

    /// Whether the entity and its children are visible.
    ///
    /// This property is propagated down the entity hierarchy until another child entity
    /// sets `visible_recursive` to a different value at which point propagation continues with that value instead.
    ///
    /// `visible_recursive` is ignored on any individual entity that has the `visible` field set.
    /// (But this does not affect tree propagation of the property)
    ///
    /// Defaults to true.
    pub visible_recursive: Option<SerializedComponentBatch>,
}

impl EntityProperties {
    /// Returns the [`ComponentDescriptor`] for [`Self::interactive`].
    #[inline]
    pub fn descriptor_interactive() -> ComponentDescriptor {
        ComponentDescriptor {
            archetype_name: Some("rerun.archetypes.EntityProperties".into()),
            component_name: "rerun.components.Interactive".into(),
            archetype_field_name: Some("interactive".into()),
        }
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::visible`].
    #[inline]
    pub fn descriptor_visible() -> ComponentDescriptor {
        ComponentDescriptor {
            archetype_name: Some("rerun.archetypes.EntityProperties".into()),
            component_name: "rerun.components.Visible".into(),
            archetype_field_name: Some("visible".into()),
        }
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::visible_recursive`].
    #[inline]
    pub fn descriptor_visible_recursive() -> ComponentDescriptor {
        ComponentDescriptor {
            archetype_name: Some("rerun.archetypes.EntityProperties".into()),
            component_name: "rerun.components.VisibleRecursive".into(),
            archetype_field_name: Some("visible_recursive".into()),
        }
    }

    /// Returns the [`ComponentDescriptor`] for the associated indicator component.
    #[inline]
    pub fn descriptor_indicator() -> ComponentDescriptor {
        ComponentDescriptor {
            archetype_name: Some("rerun.archetypes.EntityProperties".into()),
            component_name: "rerun.components.EntityPropertiesIndicator".into(),
            archetype_field_name: None,
        }
    }
}

static REQUIRED_COMPONENTS: once_cell::sync::Lazy<[ComponentDescriptor; 0usize]> =
    once_cell::sync::Lazy::new(|| []);

static RECOMMENDED_COMPONENTS: once_cell::sync::Lazy<[ComponentDescriptor; 1usize]> =
    once_cell::sync::Lazy::new(|| [EntityProperties::descriptor_indicator()]);

static OPTIONAL_COMPONENTS: once_cell::sync::Lazy<[ComponentDescriptor; 3usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            EntityProperties::descriptor_interactive(),
            EntityProperties::descriptor_visible(),
            EntityProperties::descriptor_visible_recursive(),
        ]
    });

static ALL_COMPONENTS: once_cell::sync::Lazy<[ComponentDescriptor; 4usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            EntityProperties::descriptor_indicator(),
            EntityProperties::descriptor_interactive(),
            EntityProperties::descriptor_visible(),
            EntityProperties::descriptor_visible_recursive(),
        ]
    });

impl EntityProperties {
    /// The total number of components in the archetype: 0 required, 1 recommended, 3 optional
    pub const NUM_COMPONENTS: usize = 4usize;
}

/// Indicator component for the [`EntityProperties`] [`::re_types_core::Archetype`]
pub type EntityPropertiesIndicator = ::re_types_core::GenericIndicatorComponent<EntityProperties>;

impl ::re_types_core::Archetype for EntityProperties {
    type Indicator = EntityPropertiesIndicator;

    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        "rerun.archetypes.EntityProperties".into()
    }

    #[inline]
    fn display_name() -> &'static str {
        "Entity properties"
    }

    #[inline]
    fn indicator() -> SerializedComponentBatch {
        #[allow(clippy::unwrap_used)]
        EntityPropertiesIndicator::DEFAULT.serialized().unwrap()
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, arrow::array::ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let interactive = arrays_by_descr
            .get(&Self::descriptor_interactive())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_interactive())
            });
        let visible = arrays_by_descr
            .get(&Self::descriptor_visible())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_visible()));
        let visible_recursive = arrays_by_descr
            .get(&Self::descriptor_visible_recursive())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_visible_recursive())
            });
        Ok(Self {
            interactive,
            visible,
            visible_recursive,
        })
    }
}

impl ::re_types_core::AsComponents for EntityProperties {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        [
            Some(Self::indicator()),
            self.interactive.clone(),
            self.visible.clone(),
            self.visible_recursive.clone(),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for EntityProperties {}

impl EntityProperties {
    /// Create a new `EntityProperties`.
    #[inline]
    pub fn new() -> Self {
        Self {
            interactive: None,
            visible: None,
            visible_recursive: None,
        }
    }

    /// Update only some specific fields of a `EntityProperties`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `EntityProperties`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::Loggable as _;
        Self {
            interactive: Some(SerializedComponentBatch::new(
                crate::components::Interactive::arrow_empty(),
                Self::descriptor_interactive(),
            )),
            visible: Some(SerializedComponentBatch::new(
                crate::components::Visible::arrow_empty(),
                Self::descriptor_visible(),
            )),
            visible_recursive: Some(SerializedComponentBatch::new(
                crate::components::VisibleRecursive::arrow_empty(),
                Self::descriptor_visible_recursive(),
            )),
        }
    }

    /// Partitions the component data into multiple sub-batches.
    ///
    /// Specifically, this transforms the existing [`SerializedComponentBatch`]es data into [`SerializedComponentColumn`]s
    /// instead, via [`SerializedComponentBatch::partitioned`].
    ///
    /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
    ///
    /// The specified `lengths` must sum to the total length of the component batch.
    ///
    /// [`SerializedComponentColumn`]: [::re_types_core::SerializedComponentColumn]
    #[inline]
    pub fn columns<I>(
        self,
        _lengths: I,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>>
    where
        I: IntoIterator<Item = usize> + Clone,
    {
        let columns = [
            self.interactive
                .map(|interactive| interactive.partitioned(_lengths.clone()))
                .transpose()?,
            self.visible
                .map(|visible| visible.partitioned(_lengths.clone()))
                .transpose()?,
            self.visible_recursive
                .map(|visible_recursive| visible_recursive.partitioned(_lengths.clone()))
                .transpose()?,
        ];
        Ok(columns
            .into_iter()
            .flatten()
            .chain([::re_types_core::indicator_column::<Self>(
                _lengths.into_iter().count(),
            )?]))
    }

    /// Helper to partition the component data into unit-length sub-batches.
    ///
    /// This is semantically similar to calling [`Self::columns`] with `std::iter::take(1).repeat(n)`,
    /// where `n` is automatically guessed.
    #[inline]
    pub fn columns_of_unit_batches(
        self,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>> {
        let len_interactive = self.interactive.as_ref().map(|b| b.array.len());
        let len_visible = self.visible.as_ref().map(|b| b.array.len());
        let len_visible_recursive = self.visible_recursive.as_ref().map(|b| b.array.len());
        let len = None
            .or(len_interactive)
            .or(len_visible)
            .or(len_visible_recursive)
            .unwrap_or(0);
        self.columns(std::iter::repeat(1).take(len))
    }

    /// Whether the entity can be interacted with.
    ///
    /// Non interactive components may still be still visible, but mouse interactions in the view are disabled.
    ///
    /// Defaults to true.
    #[inline]
    pub fn with_interactive(
        mut self,
        interactive: impl Into<crate::components::Interactive>,
    ) -> Self {
        self.interactive = try_serialize_field(Self::descriptor_interactive(), [interactive]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::Interactive`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_interactive`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_interactive(
        mut self,
        interactive: impl IntoIterator<Item = impl Into<crate::components::Interactive>>,
    ) -> Self {
        self.interactive = try_serialize_field(Self::descriptor_interactive(), interactive);
        self
    }

    /// Whether the entity is visible.
    ///
    /// If this is set, it will take precedence over the `visible_recursive` field
    /// and any `visible_recursive` setting further up in the hierarchy.
    ///
    /// Defaults to true.
    #[inline]
    pub fn with_visible(mut self, visible: impl Into<crate::components::Visible>) -> Self {
        self.visible = try_serialize_field(Self::descriptor_visible(), [visible]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::Visible`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_visible`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_visible(
        mut self,
        visible: impl IntoIterator<Item = impl Into<crate::components::Visible>>,
    ) -> Self {
        self.visible = try_serialize_field(Self::descriptor_visible(), visible);
        self
    }

    /// Whether the entity and its children are visible.
    ///
    /// This property is propagated down the entity hierarchy until another child entity
    /// sets `visible_recursive` to a different value at which point propagation continues with that value instead.
    ///
    /// `visible_recursive` is ignored on any individual entity that has the `visible` field set.
    /// (But this does not affect tree propagation of the property)
    ///
    /// Defaults to true.
    #[inline]
    pub fn with_visible_recursive(
        mut self,
        visible_recursive: impl Into<crate::components::VisibleRecursive>,
    ) -> Self {
        self.visible_recursive =
            try_serialize_field(Self::descriptor_visible_recursive(), [visible_recursive]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::VisibleRecursive`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_visible_recursive`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_visible_recursive(
        mut self,
        visible_recursive: impl IntoIterator<Item = impl Into<crate::components::VisibleRecursive>>,
    ) -> Self {
        self.visible_recursive =
            try_serialize_field(Self::descriptor_visible_recursive(), visible_recursive);
        self
    }
}

impl ::re_byte_size::SizeBytes for EntityProperties {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.interactive.heap_size_bytes()
            + self.visible.heap_size_bytes()
            + self.visible_recursive.heap_size_bytes()
    }
}
