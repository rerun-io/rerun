// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_types/definitions/rerun/datatypes/video.fbs".

#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]

use ::re_types_core::external::arrow2;
use ::re_types_core::ComponentName;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch, MaybeOwnedComponentBatch};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Datatype**: Minimal video container format.
#[derive(Clone, Debug)]
pub struct Video {
    /// The config for this video.
    pub config: crate::datatypes::VideoConfig,

    /// Number of time units per second.
    pub timescale: crate::datatypes::UInt64,

    /// Duration of the video in time units.
    pub duration: crate::datatypes::UInt64,

    /// Video is split into segments.
    ///
    /// Each segment begins with a key frame, followed by any number of delta frames.
    pub segments: Vec<crate::datatypes::VideoSegment>,

    /// Raw video data.
    pub data: ::re_types_core::ArrowBuffer<u8>,
}

impl ::re_types_core::SizeBytes for Video {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.config.heap_size_bytes()
            + self.timescale.heap_size_bytes()
            + self.duration.heap_size_bytes()
            + self.segments.heap_size_bytes()
            + self.data.heap_size_bytes()
    }

    #[inline]
    fn is_pod() -> bool {
        <crate::datatypes::VideoConfig>::is_pod()
            && <crate::datatypes::UInt64>::is_pod()
            && <crate::datatypes::UInt64>::is_pod()
            && <Vec<crate::datatypes::VideoSegment>>::is_pod()
            && <::re_types_core::ArrowBuffer<u8>>::is_pod()
    }
}

::re_types_core::macros::impl_into_cow!(Video);

impl ::re_types_core::Loggable for Video {
    type Name = ::re_types_core::DatatypeName;

    #[inline]
    fn name() -> Self::Name {
        "rerun.datatypes.Video".into()
    }

    #[inline]
    fn arrow_datatype() -> arrow2::datatypes::DataType {
        #![allow(clippy::wildcard_imports)]
        use arrow2::datatypes::*;
        DataType::Struct(std::sync::Arc::new(vec![
            Field::new(
                "config",
                <crate::datatypes::VideoConfig>::arrow_datatype(),
                false,
            ),
            Field::new(
                "timescale",
                <crate::datatypes::UInt64>::arrow_datatype(),
                false,
            ),
            Field::new(
                "duration",
                <crate::datatypes::UInt64>::arrow_datatype(),
                false,
            ),
            Field::new(
                "segments",
                DataType::List(std::sync::Arc::new(Field::new(
                    "item",
                    <crate::datatypes::VideoSegment>::arrow_datatype(),
                    false,
                ))),
                false,
            ),
            Field::new(
                "data",
                DataType::List(std::sync::Arc::new(Field::new(
                    "item",
                    DataType::UInt8,
                    false,
                ))),
                false,
            ),
        ]))
    }

    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<::std::borrow::Cow<'a, Self>>>>,
    ) -> SerializationResult<Box<dyn arrow2::array::Array>>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::wildcard_imports)]
        use ::re_types_core::{Loggable as _, ResultExt as _};
        use arrow2::{array::*, datatypes::*};
        Ok({
            let (somes, data): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<::std::borrow::Cow<'a, Self>> = datum.map(Into::into);
                    (datum.is_some(), datum)
                })
                .unzip();
            let bitmap: Option<arrow2::bitmap::Bitmap> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            StructArray::new(
                Self::arrow_datatype(),
                vec![
                    {
                        let (somes, config): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.config.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let config_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            _ = config_bitmap;
                            crate::datatypes::VideoConfig::to_arrow_opt(config)?
                        }
                    },
                    {
                        let (somes, timescale): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.timescale.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let timescale_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        PrimitiveArray::new(
                            DataType::UInt64,
                            timescale
                                .into_iter()
                                .map(|datum| datum.map(|datum| datum.0).unwrap_or_default())
                                .collect(),
                            timescale_bitmap,
                        )
                        .boxed()
                    },
                    {
                        let (somes, duration): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.duration.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let duration_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        PrimitiveArray::new(
                            DataType::UInt64,
                            duration
                                .into_iter()
                                .map(|datum| datum.map(|datum| datum.0).unwrap_or_default())
                                .collect(),
                            duration_bitmap,
                        )
                        .boxed()
                    },
                    {
                        let (somes, segments): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.segments.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let segments_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            use arrow2::{buffer::Buffer, offset::OffsetsBuffer};
                            let offsets = arrow2::offset::Offsets::<i32>::try_from_lengths(
                                segments
                                    .iter()
                                    .map(|opt| opt.as_ref().map_or(0, |datum| datum.len())),
                            )?
                            .into();
                            let segments_inner_data: Vec<_> =
                                segments.into_iter().flatten().flatten().collect();
                            let segments_inner_bitmap: Option<arrow2::bitmap::Bitmap> = None;
                            ListArray::try_new(
                                DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    <crate::datatypes::VideoSegment>::arrow_datatype(),
                                    false,
                                ))),
                                offsets,
                                {
                                    _ = segments_inner_bitmap;
                                    crate::datatypes::VideoSegment::to_arrow_opt(
                                        segments_inner_data.into_iter().map(Some),
                                    )?
                                },
                                segments_bitmap,
                            )?
                            .boxed()
                        }
                    },
                    {
                        let (somes, data): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.data.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let data_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            use arrow2::{buffer::Buffer, offset::OffsetsBuffer};
                            let offsets =
                                arrow2::offset::Offsets::<i32>::try_from_lengths(data.iter().map(
                                    |opt| opt.as_ref().map_or(0, |datum| datum.num_instances()),
                                ))?
                                .into();
                            let data_inner_data: Buffer<_> = data
                                .iter()
                                .flatten()
                                .map(|b| b.as_slice())
                                .collect::<Vec<_>>()
                                .concat()
                                .into();
                            let data_inner_bitmap: Option<arrow2::bitmap::Bitmap> = None;
                            ListArray::try_new(
                                DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    DataType::UInt8,
                                    false,
                                ))),
                                offsets,
                                PrimitiveArray::new(
                                    DataType::UInt8,
                                    data_inner_data,
                                    data_inner_bitmap,
                                )
                                .boxed(),
                                data_bitmap,
                            )?
                            .boxed()
                        }
                    },
                ],
                bitmap,
            )
            .boxed()
        })
    }

    fn from_arrow_opt(
        arrow_data: &dyn arrow2::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>>
    where
        Self: Sized,
    {
        #![allow(clippy::wildcard_imports)]
        use ::re_types_core::{Loggable as _, ResultExt as _};
        use arrow2::{array::*, buffer::*, datatypes::*};
        Ok({
            let arrow_data = arrow_data
                .as_any()
                .downcast_ref::<arrow2::array::StructArray>()
                .ok_or_else(|| {
                    let expected = Self::arrow_datatype();
                    let actual = arrow_data.data_type().clone();
                    DeserializationError::datatype_mismatch(expected, actual)
                })
                .with_context("rerun.datatypes.Video")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let (arrow_data_fields, arrow_data_arrays) =
                    (arrow_data.fields(), arrow_data.values());
                let arrays_by_name: ::std::collections::HashMap<_, _> = arrow_data_fields
                    .iter()
                    .map(|field| field.name.as_str())
                    .zip(arrow_data_arrays)
                    .collect();
                let config = {
                    if !arrays_by_name.contains_key("config") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "config",
                        ))
                        .with_context("rerun.datatypes.Video");
                    }
                    let arrow_data = &**arrays_by_name["config"];
                    crate::datatypes::VideoConfig::from_arrow_opt(arrow_data)
                        .with_context("rerun.datatypes.Video#config")?
                        .into_iter()
                };
                let timescale = {
                    if !arrays_by_name.contains_key("timescale") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "timescale",
                        ))
                        .with_context("rerun.datatypes.Video");
                    }
                    let arrow_data = &**arrays_by_name["timescale"];
                    arrow_data
                        .as_any()
                        .downcast_ref::<UInt64Array>()
                        .ok_or_else(|| {
                            let expected = DataType::UInt64;
                            let actual = arrow_data.data_type().clone();
                            DeserializationError::datatype_mismatch(expected, actual)
                        })
                        .with_context("rerun.datatypes.Video#timescale")?
                        .into_iter()
                        .map(|opt| opt.copied())
                        .map(|res_or_opt| res_or_opt.map(crate::datatypes::UInt64))
                };
                let duration = {
                    if !arrays_by_name.contains_key("duration") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "duration",
                        ))
                        .with_context("rerun.datatypes.Video");
                    }
                    let arrow_data = &**arrays_by_name["duration"];
                    arrow_data
                        .as_any()
                        .downcast_ref::<UInt64Array>()
                        .ok_or_else(|| {
                            let expected = DataType::UInt64;
                            let actual = arrow_data.data_type().clone();
                            DeserializationError::datatype_mismatch(expected, actual)
                        })
                        .with_context("rerun.datatypes.Video#duration")?
                        .into_iter()
                        .map(|opt| opt.copied())
                        .map(|res_or_opt| res_or_opt.map(crate::datatypes::UInt64))
                };
                let segments = {
                    if !arrays_by_name.contains_key("segments") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "segments",
                        ))
                        .with_context("rerun.datatypes.Video");
                    }
                    let arrow_data = &**arrays_by_name["segments"];
                    {
                        let arrow_data = arrow_data
                            .as_any()
                            .downcast_ref::<arrow2::array::ListArray<i32>>()
                            .ok_or_else(|| {
                                let expected = DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    <crate::datatypes::VideoSegment>::arrow_datatype(),
                                    false,
                                )));
                                let actual = arrow_data.data_type().clone();
                                DeserializationError::datatype_mismatch(expected, actual)
                            })
                            .with_context("rerun.datatypes.Video#segments")?;
                        if arrow_data.is_empty() {
                            Vec::new()
                        } else {
                            let arrow_data_inner = {
                                let arrow_data_inner = &**arrow_data.values();
                                crate::datatypes::VideoSegment::from_arrow_opt(arrow_data_inner)
                                    .with_context("rerun.datatypes.Video#segments")?
                                    .into_iter()
                                    .collect::<Vec<_>>()
                            };
                            let offsets = arrow_data.offsets();
                            arrow2::bitmap::utils::ZipValidity::new_with_validity(
                                offsets.iter().zip(offsets.lengths()),
                                arrow_data.validity(),
                            )
                            .map(|elem| {
                                elem.map(|(start, len)| {
                                    let start = *start as usize;
                                    let end = start + len;
                                    if end > arrow_data_inner.len() {
                                        return Err(DeserializationError::offset_slice_oob(
                                            (start, end),
                                            arrow_data_inner.len(),
                                        ));
                                    }

                                    #[allow(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                    let data =
                                        unsafe { arrow_data_inner.get_unchecked(start..end) };
                                    let data = data
                                        .iter()
                                        .cloned()
                                        .map(Option::unwrap_or_default)
                                        .collect();
                                    Ok(data)
                                })
                                .transpose()
                            })
                            .collect::<DeserializationResult<Vec<Option<_>>>>()?
                        }
                        .into_iter()
                    }
                };
                let data = {
                    if !arrays_by_name.contains_key("data") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "data",
                        ))
                        .with_context("rerun.datatypes.Video");
                    }
                    let arrow_data = &**arrays_by_name["data"];
                    {
                        let arrow_data = arrow_data
                            .as_any()
                            .downcast_ref::<arrow2::array::ListArray<i32>>()
                            .ok_or_else(|| {
                                let expected = DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    DataType::UInt8,
                                    false,
                                )));
                                let actual = arrow_data.data_type().clone();
                                DeserializationError::datatype_mismatch(expected, actual)
                            })
                            .with_context("rerun.datatypes.Video#data")?;
                        if arrow_data.is_empty() {
                            Vec::new()
                        } else {
                            let arrow_data_inner = {
                                let arrow_data_inner = &**arrow_data.values();
                                arrow_data_inner
                                    .as_any()
                                    .downcast_ref::<UInt8Array>()
                                    .ok_or_else(|| {
                                        let expected = DataType::UInt8;
                                        let actual = arrow_data_inner.data_type().clone();
                                        DeserializationError::datatype_mismatch(expected, actual)
                                    })
                                    .with_context("rerun.datatypes.Video#data")?
                                    .values()
                            };
                            let offsets = arrow_data.offsets();
                            arrow2::bitmap::utils::ZipValidity::new_with_validity(
                                offsets.iter().zip(offsets.lengths()),
                                arrow_data.validity(),
                            )
                            .map(|elem| {
                                elem.map(|(start, len)| {
                                    let start = *start as usize;
                                    let end = start + len;
                                    if end > arrow_data_inner.len() {
                                        return Err(DeserializationError::offset_slice_oob(
                                            (start, end),
                                            arrow_data_inner.len(),
                                        ));
                                    }

                                    #[allow(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                    let data = unsafe {
                                        arrow_data_inner
                                            .clone()
                                            .sliced_unchecked(start, end - start)
                                    };
                                    let data = ::re_types_core::ArrowBuffer::from(data);
                                    Ok(data)
                                })
                                .transpose()
                            })
                            .collect::<DeserializationResult<Vec<Option<_>>>>()?
                        }
                        .into_iter()
                    }
                };
                arrow2::bitmap::utils::ZipValidity::new_with_validity(
                    ::itertools::izip!(config, timescale, duration, segments, data),
                    arrow_data.validity(),
                )
                .map(|opt| {
                    opt.map(|(config, timescale, duration, segments, data)| {
                        Ok(Self {
                            config: config
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.Video#config")?,
                            timescale: timescale
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.Video#timescale")?,
                            duration: duration
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.Video#duration")?,
                            segments: segments
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.Video#segments")?,
                            data: data
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.Video#data")?,
                        })
                    })
                    .transpose()
                })
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.datatypes.Video")?
            }
        })
    }
}
