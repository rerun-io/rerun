// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_types/definitions/rerun/datatypes/video.fbs".

#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]

use ::re_types_core::external::arrow2;
use ::re_types_core::ComponentName;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch, MaybeOwnedComponentBatch};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Datatype**: Video configuration.
#[derive(Clone, Debug)]
pub struct VideoConfig {
    /// String used to identify the codec and some of its configuration.
    pub codec: crate::datatypes::Utf8,

    /// Natural height of the video.
    pub coded_height: crate::datatypes::UInt16,

    /// Natural width of the video.
    pub coded_width: crate::datatypes::UInt16,

    /// Codec-specific configuration.
    pub description: ::re_types_core::ArrowBuffer<u8>,
}

impl ::re_types_core::SizeBytes for VideoConfig {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.codec.heap_size_bytes()
            + self.coded_height.heap_size_bytes()
            + self.coded_width.heap_size_bytes()
            + self.description.heap_size_bytes()
    }

    #[inline]
    fn is_pod() -> bool {
        <crate::datatypes::Utf8>::is_pod()
            && <crate::datatypes::UInt16>::is_pod()
            && <crate::datatypes::UInt16>::is_pod()
            && <::re_types_core::ArrowBuffer<u8>>::is_pod()
    }
}

::re_types_core::macros::impl_into_cow!(VideoConfig);

impl ::re_types_core::Loggable for VideoConfig {
    type Name = ::re_types_core::DatatypeName;

    #[inline]
    fn name() -> Self::Name {
        "rerun.datatypes.VideoConfig".into()
    }

    #[inline]
    fn arrow_datatype() -> arrow2::datatypes::DataType {
        #![allow(clippy::wildcard_imports)]
        use arrow2::datatypes::*;
        DataType::Struct(std::sync::Arc::new(vec![
            Field::new("codec", <crate::datatypes::Utf8>::arrow_datatype(), false),
            Field::new(
                "coded_height",
                <crate::datatypes::UInt16>::arrow_datatype(),
                false,
            ),
            Field::new(
                "coded_width",
                <crate::datatypes::UInt16>::arrow_datatype(),
                false,
            ),
            Field::new(
                "description",
                DataType::List(std::sync::Arc::new(Field::new(
                    "item",
                    DataType::UInt8,
                    false,
                ))),
                false,
            ),
        ]))
    }

    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<::std::borrow::Cow<'a, Self>>>>,
    ) -> SerializationResult<Box<dyn arrow2::array::Array>>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::wildcard_imports)]
        use ::re_types_core::{Loggable as _, ResultExt as _};
        use arrow2::{array::*, datatypes::*};
        Ok({
            let (somes, data): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<::std::borrow::Cow<'a, Self>> = datum.map(Into::into);
                    (datum.is_some(), datum)
                })
                .unzip();
            let bitmap: Option<arrow2::bitmap::Bitmap> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            StructArray::new(
                Self::arrow_datatype(),
                vec![
                    {
                        let (somes, codec): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.codec.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let codec_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            let offsets = arrow2::offset::Offsets::<i32>::try_from_lengths(
                                codec.iter().map(|opt| {
                                    opt.as_ref().map(|datum| datum.0.len()).unwrap_or_default()
                                }),
                            )?
                            .into();
                            let inner_data: arrow2::buffer::Buffer<u8> = codec
                                .into_iter()
                                .flatten()
                                .flat_map(|datum| datum.0 .0)
                                .collect();

                            #[allow(unsafe_code, clippy::undocumented_unsafe_blocks)]
                            unsafe {
                                Utf8Array::<i32>::new_unchecked(
                                    DataType::Utf8,
                                    offsets,
                                    inner_data,
                                    codec_bitmap,
                                )
                            }
                            .boxed()
                        }
                    },
                    {
                        let (somes, coded_height): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.coded_height.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let coded_height_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        PrimitiveArray::new(
                            DataType::UInt16,
                            coded_height
                                .into_iter()
                                .map(|datum| datum.map(|datum| datum.0).unwrap_or_default())
                                .collect(),
                            coded_height_bitmap,
                        )
                        .boxed()
                    },
                    {
                        let (somes, coded_width): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.coded_width.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let coded_width_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        PrimitiveArray::new(
                            DataType::UInt16,
                            coded_width
                                .into_iter()
                                .map(|datum| datum.map(|datum| datum.0).unwrap_or_default())
                                .collect(),
                            coded_width_bitmap,
                        )
                        .boxed()
                    },
                    {
                        let (somes, description): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.description.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let description_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            use arrow2::{buffer::Buffer, offset::OffsetsBuffer};
                            let offsets = arrow2::offset::Offsets::<i32>::try_from_lengths(
                                description.iter().map(|opt| {
                                    opt.as_ref().map_or(0, |datum| datum.num_instances())
                                }),
                            )?
                            .into();
                            let description_inner_data: Buffer<_> = description
                                .iter()
                                .flatten()
                                .map(|b| b.as_slice())
                                .collect::<Vec<_>>()
                                .concat()
                                .into();
                            let description_inner_bitmap: Option<arrow2::bitmap::Bitmap> = None;
                            ListArray::try_new(
                                DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    DataType::UInt8,
                                    false,
                                ))),
                                offsets,
                                PrimitiveArray::new(
                                    DataType::UInt8,
                                    description_inner_data,
                                    description_inner_bitmap,
                                )
                                .boxed(),
                                description_bitmap,
                            )?
                            .boxed()
                        }
                    },
                ],
                bitmap,
            )
            .boxed()
        })
    }

    fn from_arrow_opt(
        arrow_data: &dyn arrow2::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>>
    where
        Self: Sized,
    {
        #![allow(clippy::wildcard_imports)]
        use ::re_types_core::{Loggable as _, ResultExt as _};
        use arrow2::{array::*, buffer::*, datatypes::*};
        Ok({
            let arrow_data = arrow_data
                .as_any()
                .downcast_ref::<arrow2::array::StructArray>()
                .ok_or_else(|| {
                    let expected = Self::arrow_datatype();
                    let actual = arrow_data.data_type().clone();
                    DeserializationError::datatype_mismatch(expected, actual)
                })
                .with_context("rerun.datatypes.VideoConfig")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let (arrow_data_fields, arrow_data_arrays) =
                    (arrow_data.fields(), arrow_data.values());
                let arrays_by_name: ::std::collections::HashMap<_, _> = arrow_data_fields
                    .iter()
                    .map(|field| field.name.as_str())
                    .zip(arrow_data_arrays)
                    .collect();
                let codec = {
                    if !arrays_by_name.contains_key("codec") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "codec",
                        ))
                        .with_context("rerun.datatypes.VideoConfig");
                    }
                    let arrow_data = &**arrays_by_name["codec"];
                    {
                        let arrow_data = arrow_data
                            .as_any()
                            .downcast_ref::<arrow2::array::Utf8Array<i32>>()
                            .ok_or_else(|| {
                                let expected = DataType::Utf8;
                                let actual = arrow_data.data_type().clone();
                                DeserializationError::datatype_mismatch(expected, actual)
                            })
                            .with_context("rerun.datatypes.VideoConfig#codec")?;
                        let arrow_data_buf = arrow_data.values();
                        let offsets = arrow_data.offsets();
                        arrow2::bitmap::utils::ZipValidity::new_with_validity(
                            offsets.iter().zip(offsets.lengths()),
                            arrow_data.validity(),
                        )
                        .map(|elem| {
                            elem.map(|(start, len)| {
                                let start = *start as usize;
                                let end = start + len;
                                if end > arrow_data_buf.len() {
                                    return Err(DeserializationError::offset_slice_oob(
                                        (start, end),
                                        arrow_data_buf.len(),
                                    ));
                                }

                                #[allow(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                let data =
                                    unsafe { arrow_data_buf.clone().sliced_unchecked(start, len) };
                                Ok(data)
                            })
                            .transpose()
                        })
                        .map(|res_or_opt| {
                            res_or_opt.map(|res_or_opt| {
                                res_or_opt.map(|v| {
                                    crate::datatypes::Utf8(::re_types_core::ArrowString(v))
                                })
                            })
                        })
                        .collect::<DeserializationResult<Vec<Option<_>>>>()
                        .with_context("rerun.datatypes.VideoConfig#codec")?
                        .into_iter()
                    }
                };
                let coded_height = {
                    if !arrays_by_name.contains_key("coded_height") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "coded_height",
                        ))
                        .with_context("rerun.datatypes.VideoConfig");
                    }
                    let arrow_data = &**arrays_by_name["coded_height"];
                    arrow_data
                        .as_any()
                        .downcast_ref::<UInt16Array>()
                        .ok_or_else(|| {
                            let expected = DataType::UInt16;
                            let actual = arrow_data.data_type().clone();
                            DeserializationError::datatype_mismatch(expected, actual)
                        })
                        .with_context("rerun.datatypes.VideoConfig#coded_height")?
                        .into_iter()
                        .map(|opt| opt.copied())
                        .map(|res_or_opt| res_or_opt.map(crate::datatypes::UInt16))
                };
                let coded_width = {
                    if !arrays_by_name.contains_key("coded_width") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "coded_width",
                        ))
                        .with_context("rerun.datatypes.VideoConfig");
                    }
                    let arrow_data = &**arrays_by_name["coded_width"];
                    arrow_data
                        .as_any()
                        .downcast_ref::<UInt16Array>()
                        .ok_or_else(|| {
                            let expected = DataType::UInt16;
                            let actual = arrow_data.data_type().clone();
                            DeserializationError::datatype_mismatch(expected, actual)
                        })
                        .with_context("rerun.datatypes.VideoConfig#coded_width")?
                        .into_iter()
                        .map(|opt| opt.copied())
                        .map(|res_or_opt| res_or_opt.map(crate::datatypes::UInt16))
                };
                let description = {
                    if !arrays_by_name.contains_key("description") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "description",
                        ))
                        .with_context("rerun.datatypes.VideoConfig");
                    }
                    let arrow_data = &**arrays_by_name["description"];
                    {
                        let arrow_data = arrow_data
                            .as_any()
                            .downcast_ref::<arrow2::array::ListArray<i32>>()
                            .ok_or_else(|| {
                                let expected = DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    DataType::UInt8,
                                    false,
                                )));
                                let actual = arrow_data.data_type().clone();
                                DeserializationError::datatype_mismatch(expected, actual)
                            })
                            .with_context("rerun.datatypes.VideoConfig#description")?;
                        if arrow_data.is_empty() {
                            Vec::new()
                        } else {
                            let arrow_data_inner = {
                                let arrow_data_inner = &**arrow_data.values();
                                arrow_data_inner
                                    .as_any()
                                    .downcast_ref::<UInt8Array>()
                                    .ok_or_else(|| {
                                        let expected = DataType::UInt8;
                                        let actual = arrow_data_inner.data_type().clone();
                                        DeserializationError::datatype_mismatch(expected, actual)
                                    })
                                    .with_context("rerun.datatypes.VideoConfig#description")?
                                    .values()
                            };
                            let offsets = arrow_data.offsets();
                            arrow2::bitmap::utils::ZipValidity::new_with_validity(
                                offsets.iter().zip(offsets.lengths()),
                                arrow_data.validity(),
                            )
                            .map(|elem| {
                                elem.map(|(start, len)| {
                                    let start = *start as usize;
                                    let end = start + len;
                                    if end > arrow_data_inner.len() {
                                        return Err(DeserializationError::offset_slice_oob(
                                            (start, end),
                                            arrow_data_inner.len(),
                                        ));
                                    }

                                    #[allow(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                    let data = unsafe {
                                        arrow_data_inner
                                            .clone()
                                            .sliced_unchecked(start, end - start)
                                    };
                                    let data = ::re_types_core::ArrowBuffer::from(data);
                                    Ok(data)
                                })
                                .transpose()
                            })
                            .collect::<DeserializationResult<Vec<Option<_>>>>()?
                        }
                        .into_iter()
                    }
                };
                arrow2::bitmap::utils::ZipValidity::new_with_validity(
                    ::itertools::izip!(codec, coded_height, coded_width, description),
                    arrow_data.validity(),
                )
                .map(|opt| {
                    opt.map(|(codec, coded_height, coded_width, description)| {
                        Ok(Self {
                            codec: codec
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.VideoConfig#codec")?,
                            coded_height: coded_height
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.VideoConfig#coded_height")?,
                            coded_width: coded_width
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.VideoConfig#coded_width")?,
                            description: description
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.datatypes.VideoConfig#description")?,
                        })
                    })
                    .transpose()
                })
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.datatypes.VideoConfig")?
            }
        })
    }
}
