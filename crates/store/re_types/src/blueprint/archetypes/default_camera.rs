// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_types/definitions/rerun/blueprint/archetypes/default_camera.fbs".

#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]

use ::re_types_core::external::arrow2;
use ::re_types_core::ComponentName;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch, MaybeOwnedComponentBatch};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: Defines a default camera view.
#[derive(Clone, Debug, Copy, PartialEq)]
pub struct DefaultCamera {
    /// Origin of the camera view.
    pub origin: Option<crate::blueprint::components::CameraOrigin>,

    /// Target of the camera view.
    pub target: Option<crate::blueprint::components::CameraTarget>,
}

impl ::re_types_core::SizeBytes for DefaultCamera {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.origin.heap_size_bytes() + self.target.heap_size_bytes()
    }

    #[inline]
    fn is_pod() -> bool {
        <Option<crate::blueprint::components::CameraOrigin>>::is_pod()
            && <Option<crate::blueprint::components::CameraTarget>>::is_pod()
    }
}

static REQUIRED_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 0usize]> =
    once_cell::sync::Lazy::new(|| []);

static RECOMMENDED_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 1usize]> =
    once_cell::sync::Lazy::new(|| ["rerun.blueprint.components.DefaultCameraIndicator".into()]);

static OPTIONAL_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 2usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            "rerun.blueprint.components.CameraOrigin".into(),
            "rerun.blueprint.components.CameraTarget".into(),
        ]
    });

static ALL_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 3usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            "rerun.blueprint.components.DefaultCameraIndicator".into(),
            "rerun.blueprint.components.CameraOrigin".into(),
            "rerun.blueprint.components.CameraTarget".into(),
        ]
    });

impl DefaultCamera {
    /// The total number of components in the archetype: 0 required, 1 recommended, 2 optional
    pub const NUM_COMPONENTS: usize = 3usize;
}

/// Indicator component for the [`DefaultCamera`] [`::re_types_core::Archetype`]
pub type DefaultCameraIndicator = ::re_types_core::GenericIndicatorComponent<DefaultCamera>;

impl ::re_types_core::Archetype for DefaultCamera {
    type Indicator = DefaultCameraIndicator;

    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        "rerun.blueprint.archetypes.DefaultCamera".into()
    }

    #[inline]
    fn display_name() -> &'static str {
        "Default camera"
    }

    #[inline]
    fn indicator() -> MaybeOwnedComponentBatch<'static> {
        static INDICATOR: DefaultCameraIndicator = DefaultCameraIndicator::DEFAULT;
        MaybeOwnedComponentBatch::Ref(&INDICATOR)
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentName, Box<dyn arrow2::array::Array>)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_name: ::std::collections::HashMap<_, _> = arrow_data
            .into_iter()
            .map(|(name, array)| (name.full_name(), array))
            .collect();
        let origin =
            if let Some(array) = arrays_by_name.get("rerun.blueprint.components.CameraOrigin") {
                <crate::blueprint::components::CameraOrigin>::from_arrow_opt(&**array)
                    .with_context("rerun.blueprint.archetypes.DefaultCamera#origin")?
                    .into_iter()
                    .next()
                    .flatten()
            } else {
                None
            };
        let target =
            if let Some(array) = arrays_by_name.get("rerun.blueprint.components.CameraTarget") {
                <crate::blueprint::components::CameraTarget>::from_arrow_opt(&**array)
                    .with_context("rerun.blueprint.archetypes.DefaultCamera#target")?
                    .into_iter()
                    .next()
                    .flatten()
            } else {
                None
            };
        Ok(Self { origin, target })
    }
}

impl ::re_types_core::AsComponents for DefaultCamera {
    fn as_component_batches(&self) -> Vec<MaybeOwnedComponentBatch<'_>> {
        re_tracing::profile_function!();
        use ::re_types_core::Archetype as _;
        [
            Some(Self::indicator()),
            self.origin
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
            self.target
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for DefaultCamera {}

impl DefaultCamera {
    /// Create a new `DefaultCamera`.
    #[inline]
    pub fn new() -> Self {
        Self {
            origin: None,
            target: None,
        }
    }

    /// Origin of the camera view.
    #[inline]
    pub fn with_origin(
        mut self,
        origin: impl Into<crate::blueprint::components::CameraOrigin>,
    ) -> Self {
        self.origin = Some(origin.into());
        self
    }

    /// Target of the camera view.
    #[inline]
    pub fn with_target(
        mut self,
        target: impl Into<crate::blueprint::components::CameraTarget>,
    ) -> Self {
        self.target = Some(target.into());
        self
    }
}
