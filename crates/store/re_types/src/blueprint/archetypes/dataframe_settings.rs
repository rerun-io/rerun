// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_types/definitions/rerun/blueprint/archetypes/dataframe_settings.fbs".

#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]

use ::re_types_core::external::arrow2;
use ::re_types_core::ComponentName;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch, MaybeOwnedComponentBatch};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: Configuration for the dataframe view
#[derive(Clone, Debug, Copy)]
pub struct DataframeSettings {
    /// The kind of table to display
    pub mode: Option<crate::blueprint::components::DataframeMode>,

    /// The primary sort key (time range mode only)
    pub sort_key: Option<crate::blueprint::components::SortKey>,

    /// The sort order (time range mode only)
    pub sort_order: Option<crate::blueprint::components::SortOrder>,
}

impl ::re_types_core::SizeBytes for DataframeSettings {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.mode.heap_size_bytes()
            + self.sort_key.heap_size_bytes()
            + self.sort_order.heap_size_bytes()
    }

    #[inline]
    fn is_pod() -> bool {
        <Option<crate::blueprint::components::DataframeMode>>::is_pod()
            && <Option<crate::blueprint::components::SortKey>>::is_pod()
            && <Option<crate::blueprint::components::SortOrder>>::is_pod()
    }
}

static REQUIRED_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 0usize]> =
    once_cell::sync::Lazy::new(|| []);

static RECOMMENDED_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 1usize]> =
    once_cell::sync::Lazy::new(|| ["rerun.blueprint.components.DataframeSettingsIndicator".into()]);

static OPTIONAL_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 3usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            "rerun.blueprint.components.DataframeMode".into(),
            "rerun.blueprint.components.SortKey".into(),
            "rerun.blueprint.components.SortOrder".into(),
        ]
    });

static ALL_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 4usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            "rerun.blueprint.components.DataframeSettingsIndicator".into(),
            "rerun.blueprint.components.DataframeMode".into(),
            "rerun.blueprint.components.SortKey".into(),
            "rerun.blueprint.components.SortOrder".into(),
        ]
    });

impl DataframeSettings {
    /// The total number of components in the archetype: 0 required, 1 recommended, 3 optional
    pub const NUM_COMPONENTS: usize = 4usize;
}

/// Indicator component for the [`DataframeSettings`] [`::re_types_core::Archetype`]
pub type DataframeSettingsIndicator = ::re_types_core::GenericIndicatorComponent<DataframeSettings>;

impl ::re_types_core::Archetype for DataframeSettings {
    type Indicator = DataframeSettingsIndicator;

    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        "rerun.blueprint.archetypes.DataframeSettings".into()
    }

    #[inline]
    fn display_name() -> &'static str {
        "Dataframe settings"
    }

    #[inline]
    fn indicator() -> MaybeOwnedComponentBatch<'static> {
        static INDICATOR: DataframeSettingsIndicator = DataframeSettingsIndicator::DEFAULT;
        MaybeOwnedComponentBatch::Ref(&INDICATOR)
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentName, Box<dyn arrow2::array::Array>)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_name: ::std::collections::HashMap<_, _> = arrow_data
            .into_iter()
            .map(|(name, array)| (name.full_name(), array))
            .collect();
        let mode =
            if let Some(array) = arrays_by_name.get("rerun.blueprint.components.DataframeMode") {
                <crate::blueprint::components::DataframeMode>::from_arrow_opt(&**array)
                    .with_context("rerun.blueprint.archetypes.DataframeSettings#mode")?
                    .into_iter()
                    .next()
                    .flatten()
            } else {
                None
            };
        let sort_key = if let Some(array) = arrays_by_name.get("rerun.blueprint.components.SortKey")
        {
            <crate::blueprint::components::SortKey>::from_arrow_opt(&**array)
                .with_context("rerun.blueprint.archetypes.DataframeSettings#sort_key")?
                .into_iter()
                .next()
                .flatten()
        } else {
            None
        };
        let sort_order =
            if let Some(array) = arrays_by_name.get("rerun.blueprint.components.SortOrder") {
                <crate::blueprint::components::SortOrder>::from_arrow_opt(&**array)
                    .with_context("rerun.blueprint.archetypes.DataframeSettings#sort_order")?
                    .into_iter()
                    .next()
                    .flatten()
            } else {
                None
            };
        Ok(Self {
            mode,
            sort_key,
            sort_order,
        })
    }
}

impl ::re_types_core::AsComponents for DataframeSettings {
    fn as_component_batches(&self) -> Vec<MaybeOwnedComponentBatch<'_>> {
        re_tracing::profile_function!();
        use ::re_types_core::Archetype as _;
        [
            Some(Self::indicator()),
            self.mode
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
            self.sort_key
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
            self.sort_order
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for DataframeSettings {}

impl DataframeSettings {
    /// Create a new `DataframeSettings`.
    #[inline]
    pub fn new() -> Self {
        Self {
            mode: None,
            sort_key: None,
            sort_order: None,
        }
    }

    /// The kind of table to display
    #[inline]
    pub fn with_mode(
        mut self,
        mode: impl Into<crate::blueprint::components::DataframeMode>,
    ) -> Self {
        self.mode = Some(mode.into());
        self
    }

    /// The primary sort key (time range mode only)
    #[inline]
    pub fn with_sort_key(
        mut self,
        sort_key: impl Into<crate::blueprint::components::SortKey>,
    ) -> Self {
        self.sort_key = Some(sort_key.into());
        self
    }

    /// The sort order (time range mode only)
    #[inline]
    pub fn with_sort_order(
        mut self,
        sort_order: impl Into<crate::blueprint::components::SortOrder>,
    ) -> Self {
        self.sort_order = Some(sort_order.into());
        self
    }
}
