namespace rerun.blueprint.components;

struct ForcePositionY (
    "attr.rerun.scope": "blueprint",
    "attr.rust.derive": "Default, Clone, PartialEq, Eq",
) {
    position_y: rerun.datatypes.ForcePositionY (order: 100);
}
