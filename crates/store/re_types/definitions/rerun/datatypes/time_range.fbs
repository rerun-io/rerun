namespace rerun.datatypes;

/// Two [datatypes.TimeInt] describing a range of time.
struct AbsoluteTimeRange (
  "attr.rust.derive": "Copy, PartialEq, Eq, PartialOrd, Ord",
  "attr.rust.override_crate": "re_types_core"
) {
  /// Start of the range.
  min: TimeInt (order: 100);

  /// End of the range (inclusive).
  max: TimeInt (order: 200);
}

