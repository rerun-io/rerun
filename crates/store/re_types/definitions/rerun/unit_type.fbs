namespace rerun.builtins;

/// Special placeholder type used for null variants of enums.
///
/// This type itself should not generate any code.
/// It is always serialized as `true` in all languages and deserialized as `()` in Rust.
struct UnitType {
  void: bool (order: 0);
}
