// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/testing/encodings/enum_test.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

#[derive(Clone, Debug, Default, PartialEq, Eq, ::re_byte_size::SizeBytes)]
pub struct MultiEnum {
    /// The first value.
    pub value1: crate::testing::encodings::EnumTest,

    /// The second value.
    pub value2: Option<crate::testing::encodings::ValuedEnum>,
}

::re_types_core::macros::impl_into_cow!(MultiEnum);

impl ::re_types_core::ArrowDatatype for MultiEnum {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::Struct(Fields::from(vec![
            Field::new(
                "value1",
                <crate::testing::encodings::EnumTest>::arrow_datatype(),
                false,
            ),
            Field::new(
                "value2",
                <crate::testing::encodings::ValuedEnum>::arrow_datatype(),
                true,
            ),
        ]))
    }
}

impl ::re_types_core::ToArrowOpt for MultiEnum {
    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<Cow<'a, Self>>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let fields = Fields::from(vec![
                Field::new(
                    "value1",
                    <crate::testing::encodings::EnumTest>::arrow_datatype(),
                    false,
                ),
                Field::new(
                    "value2",
                    <crate::testing::encodings::ValuedEnum>::arrow_datatype(),
                    true,
                ),
            ]);
            let (somes, data): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<Cow<'a, Self>> = datum.map(Into::into);
                    (datum.is_some(), datum)
                })
                .unzip();
            let validity: Option<arrow::buffer::NullBuffer> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            as_array_ref(StructArray::new(
                fields,
                vec![
                    {
                        let (somes, value1): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.value1.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let value1_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            let _ = value1_validity;
                            crate::testing::encodings::EnumTest::to_arrow_opt(value1)?
                        }
                    },
                    {
                        let (somes, value2): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum =
                                    datum.as_ref().map(|datum| datum.value2.clone()).flatten();
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let value2_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            let _ = value2_validity;
                            crate::testing::encodings::ValuedEnum::to_arrow_opt(value2)?
                        }
                    },
                ],
                validity,
            ))
        })
    }
}

::re_types_core::macros::impl_to_arrow_via_to_arrow_opt!(MultiEnum);

impl ::re_types_core::FromArrowOpt for MultiEnum {
    fn from_arrow_opt(
        arrow_data: &dyn arrow::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let arrow_data = arrow_data
                .try_cast::<arrow::array::StructArray>(|| Self::arrow_datatype())
                .with_context("rerun.testing.encodings.MultiEnum")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let (arrow_data_fields, arrow_data_arrays) =
                    (arrow_data.fields(), arrow_data.columns());
                let arrays_by_name: ::std::collections::HashMap<_, _> = ::std::iter::zip(
                    arrow_data_fields.iter().map(|field| field.name().as_str()),
                    arrow_data_arrays,
                )
                .collect();
                let value1 = {
                    if !arrays_by_name.contains_key("value1") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "value1",
                        ))
                        .with_context("rerun.testing.encodings.MultiEnum");
                    }
                    let arrow_data = &**arrays_by_name["value1"];
                    crate::testing::encodings::EnumTest::from_arrow_opt(arrow_data)
                        .with_context("rerun.testing.encodings.MultiEnum#value1")?
                        .into_iter()
                };
                let value2 = {
                    if !arrays_by_name.contains_key("value2") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "value2",
                        ))
                        .with_context("rerun.testing.encodings.MultiEnum");
                    }
                    let arrow_data = &**arrays_by_name["value2"];
                    crate::testing::encodings::ValuedEnum::from_arrow_opt(arrow_data)
                        .with_context("rerun.testing.encodings.MultiEnum#value2")?
                        .into_iter()
                };
                ZipValidity::new_with_validity(
                    ::itertools::izip!(value1, value2),
                    arrow_data.nulls(),
                )
                .map(|opt| {
                    opt.map(|(value1, value2)| {
                        Ok(Self {
                            value1: value1
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.testing.encodings.MultiEnum#value1")?,
                            value2,
                        })
                    })
                    .transpose()
                })
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.testing.encodings.MultiEnum")?
            }
        })
    }
}

::re_types_core::macros::impl_from_arrow_via_from_arrow_opt!(MultiEnum);
