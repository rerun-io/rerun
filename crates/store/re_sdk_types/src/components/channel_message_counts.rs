// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/components/channel_message_counts.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Component**: A mapping of channel IDs to their respective message counts.
///
/// Used in MCAP statistics to track how many messages were recorded per channel.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Debug, Default, PartialEq, Eq, ::re_byte_size::SizeBytes)]
pub struct ChannelMessageCounts(
    /// The channel ID to message count pairs.
    pub Vec<crate::encodings::ChannelCountPair>,
);

impl ::re_types_core::Component for ChannelMessageCounts {
    #[inline]
    fn name() -> ComponentType {
        "rerun.components.ChannelMessageCounts".into()
    }
}

::re_types_core::macros::impl_into_cow!(ChannelMessageCounts);

impl ::re_types_core::ArrowDatatype for ChannelMessageCounts {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::List(std::sync::Arc::new(Field::new(
            "item",
            <crate::encodings::ChannelCountPair>::arrow_datatype(),
            false,
        )))
    }
}

impl ::re_types_core::ToArrow for ChannelMessageCounts {
    fn to_arrow<'a>(
        data: impl IntoIterator<Item = impl Into<Cow<'a, Self>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let data0: Vec<_> = data
                .into_iter()
                .map(|datum| {
                    let datum: Cow<'a, Self> = datum.into();
                    datum.into_owned().0
                })
                .collect();
            let data0_validity = None;
            {
                let offsets = arrow::buffer::OffsetBuffer::<i32>::from_lengths(
                    data0.iter().map(|datum| datum.len()),
                );
                let data0_inner_data: Vec<_> = data0.into_iter().flatten().collect();
                let data0_inner_validity = None;
                as_array_ref(ListArray::try_new(
                    std::sync::Arc::new(Field::new(
                        "item",
                        <crate::encodings::ChannelCountPair>::arrow_datatype(),
                        false,
                    )),
                    offsets,
                    {
                        let _: Option<arrow::buffer::NullBuffer> = data0_inner_validity;
                        crate::encodings::ChannelCountPair::to_arrow(data0_inner_data)?
                    },
                    data0_validity,
                )?)
            }
        })
    }
}

impl ::re_types_core::FromArrow for ChannelMessageCounts {
    fn from_arrow(arrow_data: &dyn arrow::array::Array) -> DeserializationResult<Vec<Self>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        err_on_nulls(arrow_data, "rerun.components.ChannelMessageCounts")?;
        Ok({
            let arrow_data = arrow_data
                .try_cast::<arrow::array::ListArray>(|| Self::arrow_datatype())
                .with_context("rerun.components.ChannelMessageCounts#counts")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let arrow_data_inner = {
                    let arrow_data_inner = &**arrow_data.values();
                    crate::encodings::ChannelCountPair::from_arrow_opt(arrow_data_inner)
                        .with_context("rerun.components.ChannelMessageCounts#counts")?
                        .into_iter()
                        .collect::<Vec<_>>()
                };
                let offsets = arrow_data.offsets();
                ZipValidity::new_with_validity(offsets.array_windows(), arrow_data.nulls())
                    .map(|elem| {
                        elem.map(|&[start, end]| {
                            let start = start as usize;
                            let end = end as usize;
                            if arrow_data_inner.len() < end {
                                return Err(DeserializationError::offset_slice_oob(
                                    (start, end),
                                    arrow_data_inner.len(),
                                ));
                            }

                            #[expect(unsafe_code, clippy::undocumented_unsafe_blocks)]
                            let data = unsafe { arrow_data_inner.get_unchecked(start..end) };
                            let data = data
                                .iter()
                                .cloned()
                                .map(Option::unwrap_or_default)
                                .collect();
                            Ok(data)
                        })
                        .transpose()
                    })
                    .collect::<DeserializationResult<Vec<Option<_>>>>()?
            }
            .into_iter()
        }
        .map(|v| v.ok_or_else(DeserializationError::missing_data))
        .map(|res| res.map(Self))
        .collect::<DeserializationResult<Vec<_>>>()
        .with_context("rerun.components.ChannelMessageCounts#counts")
        .with_context("rerun.components.ChannelMessageCounts")?)
    }
}

impl<I: Into<crate::encodings::ChannelCountPair>, T: IntoIterator<Item = I>> From<T>
    for ChannelMessageCounts
{
    fn from(v: T) -> Self {
        Self(v.into_iter().map(|v| v.into()).collect())
    }
}
