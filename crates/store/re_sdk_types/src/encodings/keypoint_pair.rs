// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/keypoint_pair.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Encoding**: A connection between two [`encodings::KeypointId`][crate::encodings::KeypointId]s.
#[derive(Clone, Debug, Default, Eq, PartialEq, ::re_byte_size::SizeBytes)]
pub struct KeypointPair {
    /// The first point of the pair.
    pub keypoint0: crate::encodings::KeypointId,

    /// The second point of the pair.
    pub keypoint1: crate::encodings::KeypointId,
}

::re_types_core::macros::impl_into_cow!(KeypointPair);

impl ::re_types_core::ArrowDatatype for KeypointPair {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::Struct(Fields::from(vec![
            Field::new(
                "keypoint0",
                <crate::encodings::KeypointId>::arrow_datatype(),
                false,
            ),
            Field::new(
                "keypoint1",
                <crate::encodings::KeypointId>::arrow_datatype(),
                false,
            ),
        ]))
    }
}

impl ::re_types_core::ToArrowOpt for KeypointPair {
    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<Cow<'a, Self>>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let fields = Fields::from(vec![
                Field::new(
                    "keypoint0",
                    <crate::encodings::KeypointId>::arrow_datatype(),
                    false,
                ),
                Field::new(
                    "keypoint1",
                    <crate::encodings::KeypointId>::arrow_datatype(),
                    false,
                ),
            ]);
            let (somes, data): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<Cow<'a, Self>> = datum.map(Into::into);
                    (datum.is_some(), datum)
                })
                .unzip();
            let validity: Option<arrow::buffer::NullBuffer> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            as_array_ref(StructArray::new(
                fields,
                vec![
                    {
                        let (somes, keypoint0): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.keypoint0.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let keypoint0_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        as_array_ref(PrimitiveArray::<UInt16Type>::new(
                            keypoint0
                                .into_iter()
                                .map(|datum| datum.map(|datum| datum.0).unwrap_or_default())
                                .collect(),
                            keypoint0_validity,
                        ))
                    },
                    {
                        let (somes, keypoint1): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.keypoint1.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let keypoint1_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        as_array_ref(PrimitiveArray::<UInt16Type>::new(
                            keypoint1
                                .into_iter()
                                .map(|datum| datum.map(|datum| datum.0).unwrap_or_default())
                                .collect(),
                            keypoint1_validity,
                        ))
                    },
                ],
                validity,
            ))
        })
    }
}

::re_types_core::macros::impl_to_arrow_via_to_arrow_opt!(KeypointPair);

impl ::re_types_core::FromArrowOpt for KeypointPair {
    fn from_arrow_opt(
        arrow_data: &dyn arrow::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let arrow_data = arrow_data
                .try_cast::<arrow::array::StructArray>(|| Self::arrow_datatype())
                .with_context("rerun.encodings.KeypointPair")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let (arrow_data_fields, arrow_data_arrays) =
                    (arrow_data.fields(), arrow_data.columns());
                let arrays_by_name: ::std::collections::HashMap<_, _> = ::std::iter::zip(
                    arrow_data_fields.iter().map(|field| field.name().as_str()),
                    arrow_data_arrays,
                )
                .collect();
                let keypoint0 = {
                    if !arrays_by_name.contains_key("keypoint0") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "keypoint0",
                        ))
                        .with_context("rerun.encodings.KeypointPair");
                    }
                    let arrow_data = &**arrays_by_name["keypoint0"];
                    arrow_data
                        .try_cast::<UInt16Array>(|| DataType::UInt16)
                        .with_context("rerun.encodings.KeypointPair#keypoint0")?
                        .into_iter()
                        .map(|res_or_opt| res_or_opt.map(crate::encodings::KeypointId))
                };
                let keypoint1 = {
                    if !arrays_by_name.contains_key("keypoint1") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "keypoint1",
                        ))
                        .with_context("rerun.encodings.KeypointPair");
                    }
                    let arrow_data = &**arrays_by_name["keypoint1"];
                    arrow_data
                        .try_cast::<UInt16Array>(|| DataType::UInt16)
                        .with_context("rerun.encodings.KeypointPair#keypoint1")?
                        .into_iter()
                        .map(|res_or_opt| res_or_opt.map(crate::encodings::KeypointId))
                };
                ZipValidity::new_with_validity(
                    ::itertools::izip!(keypoint0, keypoint1),
                    arrow_data.nulls(),
                )
                .map(|opt| {
                    opt.map(|(keypoint0, keypoint1)| {
                        Ok(Self {
                            keypoint0: keypoint0
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.encodings.KeypointPair#keypoint0")?,
                            keypoint1: keypoint1
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.encodings.KeypointPair#keypoint1")?,
                        })
                    })
                    .transpose()
                })
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.encodings.KeypointPair")?
            }
        })
    }
}

::re_types_core::macros::impl_from_arrow_via_from_arrow_opt!(KeypointPair);
