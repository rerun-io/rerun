// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/uuid.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Encoding**: A 16-byte UUID.
#[derive(
    Clone, Debug, Default, Copy, PartialEq, Eq, PartialOrd, Ord, Hash, ::re_byte_size::SizeBytes,
)]
#[repr(transparent)]
pub struct Uuid {
    /// The raw bytes representing the UUID.
    pub bytes: [u8; 16usize],
}

::re_types_core::macros::impl_into_cow!(Uuid);

impl ::re_types_core::ArrowDatatype for Uuid {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::FixedSizeList(
            std::sync::Arc::new(Field::new("item", DataType::UInt8, false)),
            16,
        )
    }
}

impl ::re_types_core::ToArrow for Uuid {
    fn to_arrow<'a>(
        data: impl IntoIterator<Item = impl Into<Cow<'a, Self>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let bytes: Vec<_> = data
                .into_iter()
                .map(|datum| {
                    let datum: Cow<'a, Self> = datum.into();
                    datum.into_owned().bytes
                })
                .collect();
            let bytes_validity = None;
            {
                let bytes_inner_data: Vec<_> = bytes.into_iter().flatten().collect();
                let bytes_inner_validity = None;
                as_array_ref(FixedSizeListArray::new(
                    std::sync::Arc::new(Field::new("item", DataType::UInt8, false)),
                    16,
                    as_array_ref(PrimitiveArray::<UInt8Type>::new(
                        bytes_inner_data.into_iter().collect(),
                        bytes_inner_validity,
                    )),
                    bytes_validity,
                ))
            }
        })
    }
}

impl ::re_types_core::FromArrow for Uuid {
    #[inline]
    fn from_arrow(arrow_data: &dyn arrow::array::Array) -> DeserializationResult<Vec<Self>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        err_on_nulls(arrow_data, "rerun.encodings.Uuid")?;
        Ok({
            let slice = {
                let arrow_data = arrow_data
                    .try_cast::<arrow::array::FixedSizeListArray>(|| {
                        DataType::FixedSizeList(
                            std::sync::Arc::new(Field::new("item", DataType::UInt8, false)),
                            16,
                        )
                    })
                    .with_context("rerun.encodings.Uuid#bytes")?;
                if arrow_data.value_length() != 16i32 {
                    return Err(DeserializationError::datatype_mismatch(
                        DataType::FixedSizeList(
                            std::sync::Arc::new(Field::new("item", DataType::UInt8, false)),
                            16,
                        ),
                        arrow_data.data_type().clone(),
                    ))
                    .with_context("rerun.encodings.Uuid#bytes");
                }
                let arrow_data_inner = &**arrow_data.values();
                bytemuck::try_cast_slice::<_, [u8; 16usize]>(
                    arrow_data_inner
                        .try_cast::<UInt8Array>(|| DataType::UInt8)
                        .with_context("rerun.encodings.Uuid#bytes")?
                        .values()
                        .as_ref(),
                )
                .map_err(|err| DeserializationError::ValidationError(err.to_string()))
                .with_context("rerun.encodings.Uuid#bytes")?
            };
            {
                slice
                    .iter()
                    .copied()
                    .map(|bytes| Self { bytes })
                    .collect::<Vec<_>>()
            }
        })
    }
}

impl From<[u8; 16usize]> for Uuid {
    #[inline]
    fn from(bytes: [u8; 16usize]) -> Self {
        Self { bytes }
    }
}

impl From<Uuid> for [u8; 16usize] {
    #[inline]
    fn from(value: Uuid) -> Self {
        value.bytes
    }
}
