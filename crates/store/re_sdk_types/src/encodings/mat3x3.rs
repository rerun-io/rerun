// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/mat3x3.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Encoding**: A 3x3 Matrix.
///
/// Matrices in Rerun are stored as flat list of coefficients in column-major order:
/// ```text
///             column 0       column 1       column 2
///        -------------------------------------------------
/// row 0 | flat_columns[0] flat_columns[3] flat_columns[6]
/// row 1 | flat_columns[1] flat_columns[4] flat_columns[7]
/// row 2 | flat_columns[2] flat_columns[5] flat_columns[8]
/// ```
#[derive(
    Clone,
    Debug,
    Copy,
    PartialEq,
    PartialOrd,
    bytemuck::Pod,
    bytemuck::Zeroable,
    ::re_byte_size::SizeBytes,
)]
#[repr(transparent)]
pub struct Mat3x3(
    /// Flat list of matrix coefficients in column-major order.
    pub [f32; 9usize],
);

::re_types_core::macros::impl_into_cow!(Mat3x3);

impl ::re_types_core::ArrowDatatype for Mat3x3 {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::FixedSizeList(
            std::sync::Arc::new(Field::new("item", DataType::Float32, false)),
            9,
        )
    }
}

impl ::re_types_core::ToArrow for Mat3x3 {
    fn to_arrow<'a>(
        data: impl IntoIterator<Item = impl Into<Cow<'a, Self>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let data0: Vec<_> = data
                .into_iter()
                .map(|datum| {
                    let datum: Cow<'a, Self> = datum.into();
                    datum.into_owned().0
                })
                .collect();
            let data0_validity = None;
            {
                let data0_inner_data: Vec<_> = data0.into_iter().flatten().collect();
                let data0_inner_validity = None;
                as_array_ref(FixedSizeListArray::new(
                    std::sync::Arc::new(Field::new("item", DataType::Float32, false)),
                    9,
                    as_array_ref(PrimitiveArray::<Float32Type>::new(
                        data0_inner_data.into_iter().collect(),
                        data0_inner_validity,
                    )),
                    data0_validity,
                ))
            }
        })
    }
}

impl ::re_types_core::FromArrow for Mat3x3 {
    #[inline]
    fn from_arrow(arrow_data: &dyn arrow::array::Array) -> DeserializationResult<Vec<Self>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        err_on_nulls(arrow_data, "rerun.encodings.Mat3x3")?;
        Ok({
            let slice = {
                let arrow_data = arrow_data
                    .try_cast::<arrow::array::FixedSizeListArray>(|| {
                        DataType::FixedSizeList(
                            std::sync::Arc::new(Field::new("item", DataType::Float32, false)),
                            9,
                        )
                    })
                    .with_context("rerun.encodings.Mat3x3#flat_columns")?;
                if arrow_data.value_length() != 9i32 {
                    return Err(DeserializationError::datatype_mismatch(
                        DataType::FixedSizeList(
                            std::sync::Arc::new(Field::new("item", DataType::Float32, false)),
                            9,
                        ),
                        arrow_data.data_type().clone(),
                    ))
                    .with_context("rerun.encodings.Mat3x3#flat_columns");
                }
                let arrow_data_inner = &**arrow_data.values();
                bytemuck::try_cast_slice::<_, [f32; 9usize]>(
                    arrow_data_inner
                        .try_cast::<Float32Array>(|| DataType::Float32)
                        .with_context("rerun.encodings.Mat3x3#flat_columns")?
                        .values()
                        .as_ref(),
                )
                .map_err(|err| DeserializationError::ValidationError(err.to_string()))
                .with_context("rerun.encodings.Mat3x3#flat_columns")?
            };
            { slice.iter().copied().map(Self).collect::<Vec<_>>() }
        })
    }
}

impl From<[f32; 9usize]> for Mat3x3 {
    #[inline]
    fn from(flat_columns: [f32; 9usize]) -> Self {
        Self(flat_columns)
    }
}

impl From<Mat3x3> for [f32; 9usize] {
    #[inline]
    fn from(value: Mat3x3) -> Self {
        value.0
    }
}
