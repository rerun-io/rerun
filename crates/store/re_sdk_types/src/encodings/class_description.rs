// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/class_description.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Encoding**: The description of a semantic Class.
///
/// If an entity is annotated with a corresponding [`components::ClassId`][crate::components::ClassId], Rerun will use
/// the attached [`encodings::AnnotationInfo`][crate::encodings::AnnotationInfo] to derive labels and colors.
///
/// Keypoints within an annotation class can similarly be annotated with a
/// [`components::KeypointId`][crate::components::KeypointId] in which case we should defer to the label and color for the
/// [`encodings::AnnotationInfo`][crate::encodings::AnnotationInfo] specifically associated with the Keypoint.
///
/// Keypoints within the class can also be decorated with skeletal edges.
/// Keypoint-connections are pairs of [`components::KeypointId`][crate::components::KeypointId]s. If an edge is
/// defined, and both keypoints exist within the instance of the class, then the
/// keypoints should be connected with an edge. The edge should be labeled and
/// colored as described by the class's [`encodings::AnnotationInfo`][crate::encodings::AnnotationInfo].
#[derive(Clone, Debug, Default, Eq, PartialEq, ::re_byte_size::SizeBytes)]
pub struct ClassDescription {
    /// The [`encodings::AnnotationInfo`][crate::encodings::AnnotationInfo] for the class.
    pub info: crate::encodings::AnnotationInfo,

    /// The [`encodings::AnnotationInfo`][crate::encodings::AnnotationInfo] for all of the keypoints.
    pub keypoint_annotations: Vec<crate::encodings::AnnotationInfo>,

    /// The connections between keypoints.
    pub keypoint_connections: Vec<crate::encodings::KeypointPair>,
}

::re_types_core::macros::impl_into_cow!(ClassDescription);

impl ::re_types_core::ArrowDatatype for ClassDescription {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::Struct(Fields::from(vec![
            Field::new(
                "info",
                <crate::encodings::AnnotationInfo>::arrow_datatype(),
                false,
            ),
            Field::new(
                "keypoint_annotations",
                DataType::List(std::sync::Arc::new(Field::new(
                    "item",
                    <crate::encodings::AnnotationInfo>::arrow_datatype(),
                    false,
                ))),
                false,
            ),
            Field::new(
                "keypoint_connections",
                DataType::List(std::sync::Arc::new(Field::new(
                    "item",
                    <crate::encodings::KeypointPair>::arrow_datatype(),
                    false,
                ))),
                false,
            ),
        ]))
    }
}

impl ::re_types_core::ToArrowOpt for ClassDescription {
    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<Cow<'a, Self>>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let fields = Fields::from(vec![
                Field::new(
                    "info",
                    <crate::encodings::AnnotationInfo>::arrow_datatype(),
                    false,
                ),
                Field::new(
                    "keypoint_annotations",
                    DataType::List(std::sync::Arc::new(Field::new(
                        "item",
                        <crate::encodings::AnnotationInfo>::arrow_datatype(),
                        false,
                    ))),
                    false,
                ),
                Field::new(
                    "keypoint_connections",
                    DataType::List(std::sync::Arc::new(Field::new(
                        "item",
                        <crate::encodings::KeypointPair>::arrow_datatype(),
                        false,
                    ))),
                    false,
                ),
            ]);
            let (somes, data): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<Cow<'a, Self>> = datum.map(Into::into);
                    (datum.is_some(), datum)
                })
                .unzip();
            let validity: Option<arrow::buffer::NullBuffer> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            as_array_ref(StructArray::new(
                fields,
                vec![
                    {
                        let (somes, info): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| datum.info.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let info_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            let _ = info_validity;
                            crate::encodings::AnnotationInfo::to_arrow_opt(info)?
                        }
                    },
                    {
                        let (somes, keypoint_annotations): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum
                                    .as_ref()
                                    .map(|datum| datum.keypoint_annotations.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let keypoint_annotations_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            let offsets = arrow::buffer::OffsetBuffer::<i32>::from_lengths(
                                keypoint_annotations
                                    .iter()
                                    .map(|opt| opt.as_ref().map_or(0, |datum| datum.len())),
                            );
                            let keypoint_annotations_inner_data: Vec<_> = keypoint_annotations
                                .into_iter()
                                .flatten()
                                .flatten()
                                .collect();
                            let keypoint_annotations_inner_validity = None;
                            as_array_ref(ListArray::try_new(
                                std::sync::Arc::new(Field::new(
                                    "item",
                                    <crate::encodings::AnnotationInfo>::arrow_datatype(),
                                    false,
                                )),
                                offsets,
                                {
                                    let _: Option<arrow::buffer::NullBuffer> =
                                        keypoint_annotations_inner_validity;
                                    crate::encodings::AnnotationInfo::to_arrow(
                                        keypoint_annotations_inner_data,
                                    )?
                                },
                                keypoint_annotations_validity,
                            )?)
                        }
                    },
                    {
                        let (somes, keypoint_connections): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum
                                    .as_ref()
                                    .map(|datum| datum.keypoint_connections.clone());
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let keypoint_connections_validity: Option<arrow::buffer::NullBuffer> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            let offsets = arrow::buffer::OffsetBuffer::<i32>::from_lengths(
                                keypoint_connections
                                    .iter()
                                    .map(|opt| opt.as_ref().map_or(0, |datum| datum.len())),
                            );
                            let keypoint_connections_inner_data: Vec<_> = keypoint_connections
                                .into_iter()
                                .flatten()
                                .flatten()
                                .collect();
                            let keypoint_connections_inner_validity = None;
                            as_array_ref(ListArray::try_new(
                                std::sync::Arc::new(Field::new(
                                    "item",
                                    <crate::encodings::KeypointPair>::arrow_datatype(),
                                    false,
                                )),
                                offsets,
                                {
                                    let _: Option<arrow::buffer::NullBuffer> =
                                        keypoint_connections_inner_validity;
                                    crate::encodings::KeypointPair::to_arrow(
                                        keypoint_connections_inner_data,
                                    )?
                                },
                                keypoint_connections_validity,
                            )?)
                        }
                    },
                ],
                validity,
            ))
        })
    }
}

::re_types_core::macros::impl_to_arrow_via_to_arrow_opt!(ClassDescription);

impl ::re_types_core::FromArrowOpt for ClassDescription {
    fn from_arrow_opt(
        arrow_data: &dyn arrow::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let arrow_data = arrow_data
                .try_cast::<arrow::array::StructArray>(|| Self::arrow_datatype())
                .with_context("rerun.encodings.ClassDescription")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let (arrow_data_fields, arrow_data_arrays) =
                    (arrow_data.fields(), arrow_data.columns());
                let arrays_by_name: ::std::collections::HashMap<_, _> = ::std::iter::zip(
                    arrow_data_fields.iter().map(|field| field.name().as_str()),
                    arrow_data_arrays,
                )
                .collect();
                let info = {
                    if !arrays_by_name.contains_key("info") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "info",
                        ))
                        .with_context("rerun.encodings.ClassDescription");
                    }
                    let arrow_data = &**arrays_by_name["info"];
                    crate::encodings::AnnotationInfo::from_arrow_opt(arrow_data)
                        .with_context("rerun.encodings.ClassDescription#info")?
                        .into_iter()
                };
                let keypoint_annotations = {
                    if !arrays_by_name.contains_key("keypoint_annotations") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "keypoint_annotations",
                        ))
                        .with_context("rerun.encodings.ClassDescription");
                    }
                    let arrow_data = &**arrays_by_name["keypoint_annotations"];
                    {
                        let arrow_data = arrow_data
                            .try_cast::<arrow::array::ListArray>(|| {
                                DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    <crate::encodings::AnnotationInfo>::arrow_datatype(),
                                    false,
                                )))
                            })
                            .with_context(
                                "rerun.encodings.ClassDescription#keypoint_annotations",
                            )?;
                        if arrow_data.is_empty() {
                            Vec::new()
                        } else {
                            let arrow_data_inner = {
                                let arrow_data_inner = &**arrow_data.values();
                                crate::encodings::AnnotationInfo::from_arrow_opt(arrow_data_inner)
                                    .with_context(
                                        "rerun.encodings.ClassDescription#keypoint_annotations",
                                    )?
                                    .into_iter()
                                    .collect::<Vec<_>>()
                            };
                            let offsets = arrow_data.offsets();
                            ZipValidity::new_with_validity(
                                offsets.array_windows(),
                                arrow_data.nulls(),
                            )
                            .map(|elem| {
                                elem.map(|&[start, end]| {
                                    let start = start as usize;
                                    let end = end as usize;
                                    if arrow_data_inner.len() < end {
                                        return Err(DeserializationError::offset_slice_oob(
                                            (start, end),
                                            arrow_data_inner.len(),
                                        ));
                                    }

                                    #[expect(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                    let data =
                                        unsafe { arrow_data_inner.get_unchecked(start..end) };
                                    let data = data
                                        .iter()
                                        .cloned()
                                        .map(Option::unwrap_or_default)
                                        .collect();
                                    Ok(data)
                                })
                                .transpose()
                            })
                            .collect::<DeserializationResult<Vec<Option<_>>>>()?
                        }
                        .into_iter()
                    }
                };
                let keypoint_connections = {
                    if !arrays_by_name.contains_key("keypoint_connections") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "keypoint_connections",
                        ))
                        .with_context("rerun.encodings.ClassDescription");
                    }
                    let arrow_data = &**arrays_by_name["keypoint_connections"];
                    {
                        let arrow_data = arrow_data
                            .try_cast::<arrow::array::ListArray>(|| {
                                DataType::List(std::sync::Arc::new(Field::new(
                                    "item",
                                    <crate::encodings::KeypointPair>::arrow_datatype(),
                                    false,
                                )))
                            })
                            .with_context(
                                "rerun.encodings.ClassDescription#keypoint_connections",
                            )?;
                        if arrow_data.is_empty() {
                            Vec::new()
                        } else {
                            let arrow_data_inner = {
                                let arrow_data_inner = &**arrow_data.values();
                                crate::encodings::KeypointPair::from_arrow_opt(arrow_data_inner)
                                    .with_context(
                                        "rerun.encodings.ClassDescription#keypoint_connections",
                                    )?
                                    .into_iter()
                                    .collect::<Vec<_>>()
                            };
                            let offsets = arrow_data.offsets();
                            ZipValidity::new_with_validity(
                                offsets.array_windows(),
                                arrow_data.nulls(),
                            )
                            .map(|elem| {
                                elem.map(|&[start, end]| {
                                    let start = start as usize;
                                    let end = end as usize;
                                    if arrow_data_inner.len() < end {
                                        return Err(DeserializationError::offset_slice_oob(
                                            (start, end),
                                            arrow_data_inner.len(),
                                        ));
                                    }

                                    #[expect(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                    let data =
                                        unsafe { arrow_data_inner.get_unchecked(start..end) };
                                    let data = data
                                        .iter()
                                        .cloned()
                                        .map(Option::unwrap_or_default)
                                        .collect();
                                    Ok(data)
                                })
                                .transpose()
                            })
                            .collect::<DeserializationResult<Vec<Option<_>>>>()?
                        }
                        .into_iter()
                    }
                };
                ZipValidity::new_with_validity(
                    ::itertools::izip!(info, keypoint_annotations, keypoint_connections),
                    arrow_data.nulls(),
                )
                .map(|opt| {
                    opt.map(|(info, keypoint_annotations, keypoint_connections)| {
                        Ok(Self {
                            info: info
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context("rerun.encodings.ClassDescription#info")?,
                            keypoint_annotations: keypoint_annotations
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context(
                                    "rerun.encodings.ClassDescription#keypoint_annotations",
                                )?,
                            keypoint_connections: keypoint_connections
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context(
                                    "rerun.encodings.ClassDescription#keypoint_connections",
                                )?,
                        })
                    })
                    .transpose()
                })
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.encodings.ClassDescription")?
            }
        })
    }
}

::re_types_core::macros::impl_from_arrow_via_from_arrow_opt!(ClassDescription);
