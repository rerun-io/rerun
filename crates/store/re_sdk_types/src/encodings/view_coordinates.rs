// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/encodings/view_coordinates.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Encoding**: An orientation convention for three-dimensional coordinates.
///
/// The three directions are always ordered as [x, y, z] and specify where each positive axis points.
/// For example, [Right, Down, Forward] means that +X points right, +Y points down, and +Z points forward.
///
/// ⚠ [Rerun does not yet support left-handed coordinate systems](https://github.com/rerun-io/rerun/issues/5032).
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Debug, Copy, PartialEq, Eq, ::re_byte_size::SizeBytes)]
#[repr(transparent)]
pub struct ViewCoordinates(
    /// The directions of the [x, y, z] axes.
    pub [crate::encodings::ViewDir; 3usize],
);

::re_types_core::macros::impl_into_cow!(ViewCoordinates);

impl ::re_types_core::ArrowDatatype for ViewCoordinates {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::FixedSizeList(
            std::sync::Arc::new(Field::new(
                "item",
                <crate::encodings::ViewDir>::arrow_datatype(),
                false,
            )),
            3,
        )
    }
}

impl ::re_types_core::ToArrow for ViewCoordinates {
    fn to_arrow<'a>(
        data: impl IntoIterator<Item = impl Into<Cow<'a, Self>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let data0: Vec<_> = data
                .into_iter()
                .map(|datum| {
                    let datum: Cow<'a, Self> = datum.into();
                    datum.into_owned().0
                })
                .collect();
            let data0_validity = None;
            {
                let data0_inner_data: Vec<_> = data0.into_iter().flatten().collect();
                let data0_inner_validity = None;
                as_array_ref(FixedSizeListArray::new(
                    std::sync::Arc::new(Field::new(
                        "item",
                        <crate::encodings::ViewDir>::arrow_datatype(),
                        false,
                    )),
                    3,
                    {
                        let _: Option<arrow::buffer::NullBuffer> = data0_inner_validity;
                        crate::encodings::ViewDir::to_arrow(data0_inner_data)?
                    },
                    data0_validity,
                ))
            }
        })
    }
}

impl ::re_types_core::FromArrow for ViewCoordinates {
    fn from_arrow(arrow_data: &dyn arrow::array::Array) -> DeserializationResult<Vec<Self>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        err_on_nulls(arrow_data, "rerun.encodings.ViewCoordinates")?;
        Ok(
            {
                let arrow_data = arrow_data
                    .try_cast::<
                        arrow::array::FixedSizeListArray,
                    >(|| Self::arrow_datatype())
                    .with_context("rerun.encodings.ViewCoordinates#coordinates")?;
                if arrow_data.is_empty() {
                    Vec::new()
                } else {
                    let offsets = ::std::iter::zip(
                        (0..).step_by(3usize),
                        (3usize..).step_by(3usize).take(arrow_data.len()),
                    );
                    let arrow_data_inner = {
                        let arrow_data_inner = &**arrow_data.values();
                        crate::encodings::ViewDir::from_arrow_opt(arrow_data_inner)
                            .with_context("rerun.encodings.ViewCoordinates#coordinates")?
                            .into_iter()
                            .collect::<Vec<_>>()
                    };
                    ZipValidity::new_with_validity(offsets, arrow_data.nulls())
                        .map(|elem| {
                            elem
                                .map(|(start, end): (usize, usize)| {
                                    re_log::debug_assert!(end - start == 3usize);
                                    if arrow_data_inner.len() < end {
                                        return Err(
                                            DeserializationError::offset_slice_oob(
                                                (start, end),
                                                arrow_data_inner.len(),
                                            ),
                                        );
                                    }

                                    #[expect(unsafe_code, clippy::undocumented_unsafe_blocks)]
                                    let data = unsafe {
                                        arrow_data_inner.get_unchecked(start..end)
                                    };
                                    if data.iter().any(Option::is_none) {
                                        return Err(DeserializationError::missing_data());
                                    }
                                    let data = data
                                        .iter()
                                        .cloned()
                                        .map(|opt| {
                                            opt
                                                .unwrap_or_else(|| {
                                                    <crate::encodings::ViewDir as ::re_types_core::reflection::Enum>::variants()[0]
                                                })
                                        });

                                    // NOTE: Unwrapping cannot fail: the length must be correct.
                                    #[expect(clippy::unwrap_used)]
                                    Ok(array_init::from_iter(data).unwrap())
                                })
                                .transpose()
                        })
                        .collect::<DeserializationResult<Vec<Option<_>>>>()?
                }
                    .into_iter()
            }
                .map(|v| v.ok_or_else(DeserializationError::missing_data))
                .map(|res| res.map(Self))
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.encodings.ViewCoordinates#coordinates")
                .with_context("rerun.encodings.ViewCoordinates")?,
        )
    }
}

impl<T: Into<[crate::encodings::ViewDir; 3usize]>> From<T> for ViewCoordinates {
    fn from(v: T) -> Self {
        Self(v.into())
    }
}

impl std::borrow::Borrow<[crate::encodings::ViewDir; 3usize]> for ViewCoordinates {
    #[inline]
    fn borrow(&self) -> &[crate::encodings::ViewDir; 3usize] {
        &self.0
    }
}

impl std::ops::Deref for ViewCoordinates {
    type Target = [crate::encodings::ViewDir; 3usize];

    #[inline]
    fn deref(&self) -> &[crate::encodings::ViewDir; 3usize] {
        &self.0
    }
}

impl std::ops::DerefMut for ViewCoordinates {
    #[inline]
    fn deref_mut(&mut self) -> &mut [crate::encodings::ViewDir; 3usize] {
        &mut self.0
    }
}
