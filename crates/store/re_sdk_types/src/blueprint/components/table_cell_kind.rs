// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/components/table_cell_kind.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]
#![allow(non_camel_case_types)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Component**: How a table column value is rendered.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Copy, Debug, Hash, PartialEq, Eq, Default, ::re_byte_size::SizeBytes)]
#[repr(u8)]
pub enum TableCellKind {
    /// Infer the renderer from the component or Arrow datatype.
    #[default]
    Auto = 1,

    /// Render a Rerun URI as an interactive link.
    Link = 2,

    /// Render an image blob as a thumbnail.
    Thumbnail = 3,

    /// Render a boolean as a flag.
    ///
    /// Card layouts place the first flag field in the special flag position and
    /// currently support at most one flag field.
    Flag = 4,

    /// Render a recording reference as embedded views.
    ///
    /// The column must also have [`archetypes::TableColumnPreview`][crate::blueprint::archetypes::TableColumnPreview] configuration with at least one view.
    /// All preview columns use the shared [`archetypes::PreviewsConfig`][crate::blueprint::archetypes::PreviewsConfig].
    Preview = 5,

    /// Interpret scalar `i32` values as `rerun.cloud.v1alpha1.EntryKind` enum values and render their human-readable names.
    ///
    /// Unknown numeric values are rendered as `Unknown EntryKind`.
    EntryKind = 6,
}

impl ::re_types_core::Component for TableCellKind {
    #[inline]
    fn name() -> ComponentType {
        "rerun.blueprint.components.TableCellKind".into()
    }
}

::re_types_core::macros::impl_into_cow!(TableCellKind);

impl ::re_types_core::ArrowDatatype for TableCellKind {
    #[inline]
    fn arrow_datatype() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::UInt8
    }
}

impl ::re_types_core::ToArrow for TableCellKind {
    fn to_arrow<'a>(
        data: impl IntoIterator<Item = impl Into<Cow<'a, Self>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDatatype as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let data0 = data.into_iter().map(|datum| {
                let datum: Cow<'a, Self> = datum.into();
                *datum as u8
            });
            let data0_validity = None;
            as_array_ref(PrimitiveArray::<UInt8Type>::new(
                data0.into_iter().collect(),
                data0_validity,
            ))
        })
    }
}

impl ::re_types_core::FromArrow for TableCellKind {
    fn from_arrow(arrow_data: &dyn arrow::array::Array) -> DeserializationResult<Vec<Self>> {
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        err_on_nulls(arrow_data, "rerun.blueprint.components.TableCellKind")?;
        Ok(arrow_data
            .try_cast::<UInt8Array>(|| Self::arrow_datatype())
            .with_context("rerun.blueprint.components.TableCellKind#enum")?
            .values()
            .iter()
            .copied()
            .map(|val| {
                <Self as ::re_types_core::reflection::Enum>::try_from_integer(val).ok_or_else(
                    || {
                        DeserializationError::missing_union_arm(
                            Self::arrow_datatype(),
                            "<invalid>",
                            val as _,
                        )
                    },
                )
            })
            .collect::<DeserializationResult<Vec<_>>>()
            .with_context("rerun.blueprint.components.TableCellKind")?)
    }
}

impl std::fmt::Display for TableCellKind {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Auto => write!(f, "Auto"),
            Self::Link => write!(f, "Link"),
            Self::Thumbnail => write!(f, "Thumbnail"),
            Self::Flag => write!(f, "Flag"),
            Self::Preview => write!(f, "Preview"),
            Self::EntryKind => write!(f, "EntryKind"),
        }
    }
}

impl ::re_types_core::reflection::Enum for TableCellKind {
    type Repr = u8;

    #[inline]
    fn variants() -> &'static [Self] {
        &[
            Self::Auto,
            Self::Link,
            Self::Thumbnail,
            Self::Flag,
            Self::Preview,
            Self::EntryKind,
        ]
    }

    #[inline]
    fn docstring_md(self) -> &'static str {
        match self {
            Self::Auto => "Infer the renderer from the component or Arrow datatype.",
            Self::Link => "Render a Rerun URI as an interactive link.",
            Self::Thumbnail => "Render an image blob as a thumbnail.",
            Self::Flag => {
                "Render a boolean as a flag.\n\nCard layouts place the first flag field in the special flag position and\ncurrently support at most one flag field."
            }
            Self::Preview => {
                "Render a recording reference as embedded views.\n\nThe column must also have archetypes.TableColumnPreview configuration with at least one view.\nAll preview columns use the shared archetypes.PreviewsConfig."
            }
            Self::EntryKind => {
                "Interpret scalar `i32` values as `rerun.cloud.v1alpha1.EntryKind` enum values and render their human-readable names.\n\nUnknown numeric values are rendered as `Unknown EntryKind`."
            }
        }
    }

    #[inline]
    fn try_from_integer(value: u8) -> Option<Self> {
        Self::variants()
            .get((value as usize).wrapping_sub(1))
            .copied()
    }
}
