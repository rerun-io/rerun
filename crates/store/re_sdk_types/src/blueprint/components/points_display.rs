// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/components/points_display.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]
#![allow(non_camel_case_types)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Component**: Controls when data point markers are displayed on line series.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Copy, Debug, Hash, PartialEq, Eq, Default, ::re_byte_size::SizeBytes)]
#[repr(u8)]
pub enum PointsDisplay {
    /// Show data point markers only when hovering near them.
    #[default]
    OnHover = 1,

    /// Always show data point markers on non-aggregated line series.
    Always = 2,
}

impl ::re_types_core::Component for PointsDisplay {
    #[inline]
    fn name() -> ComponentType {
        "rerun.blueprint.components.PointsDisplay".into()
    }
}

::re_types_core::macros::impl_into_cow!(PointsDisplay);

impl ::re_types_core::ArrowDataType for PointsDisplay {
    #[inline]
    fn arrow_data_type() -> arrow::datatypes::DataType {
        use arrow::datatypes::*;
        DataType::UInt8
    }
}

impl ::re_types_core::ToArrow for PointsDisplay {
    fn to_arrow<'a>(
        data: impl IntoIterator<Item = impl Into<Cow<'a, Self>>>,
    ) -> SerializationResult<ArrayRef>
    where
        Self: Clone + 'a,
    {
        #![allow(clippy::manual_is_variant_and)]
        use ::re_types_core::{
            ArrowDataType as _, ResultExt as _, ToArrow as _, ToArrowOpt as _,
            arrow_helpers::as_array_ref,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        Ok({
            let data0 = data.into_iter().map(|datum| {
                let datum: Cow<'a, Self> = datum.into();
                *datum as u8
            });
            let data0_validity = None;
            as_array_ref(PrimitiveArray::<UInt8Type>::new(
                data0.into_iter().collect(),
                data0_validity,
            ))
        })
    }
}

impl ::re_types_core::FromArrow for PointsDisplay {
    fn from_arrow(arrow_data: &dyn arrow::array::Array) -> DeserializationResult<Vec<Self>> {
        use ::re_types_core::{
            ArrowDataType as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
            arrow_helpers::*, arrow_zip_validity::ZipValidity,
        };
        use arrow::{array::*, buffer::*, datatypes::*};
        err_on_nulls(arrow_data, "rerun.blueprint.components.PointsDisplay")?;
        Ok(arrow_data
            .try_cast::<UInt8Array>(|| Self::arrow_data_type())
            .with_context("rerun.blueprint.components.PointsDisplay#enum")?
            .values()
            .iter()
            .copied()
            .map(|val| {
                <Self as ::re_types_core::reflection::Enum>::try_from_integer(val).ok_or_else(
                    || {
                        DeserializationError::missing_union_arm(
                            Self::arrow_data_type(),
                            "<invalid>",
                            val as _,
                        )
                    },
                )
            })
            .collect::<DeserializationResult<Vec<_>>>()
            .with_context("rerun.blueprint.components.PointsDisplay")?)
    }
}

impl std::fmt::Display for PointsDisplay {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::OnHover => write!(f, "OnHover"),
            Self::Always => write!(f, "Always"),
        }
    }
}

impl ::re_types_core::reflection::Enum for PointsDisplay {
    type Repr = u8;

    #[inline]
    fn variants() -> &'static [Self] {
        &[Self::OnHover, Self::Always]
    }

    #[inline]
    fn docstring_md(self) -> &'static str {
        match self {
            Self::OnHover => "Show data point markers only when hovering near them.",
            Self::Always => "Always show data point markers on non-aggregated line series.",
        }
    }

    #[inline]
    fn try_from_integer(value: u8) -> Option<Self> {
        Self::variants()
            .get((value as usize).wrapping_sub(1))
            .copied()
    }
}
