// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/table_column.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Archetype**: Blueprint for a column used by a table or card layout.
///
/// This archetype is stored at the layout-specific path for its source column.
/// The source is the final [`components::ColumnName`][crate::blueprint::components::ColumnName] part of that path.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Debug, Default, ::re_byte_size::SizeBytes)]
pub struct TableColumn {
    /// The name shown for the column.
    ///
    /// If unset, the name is inferred from the source column.
    pub name: Option<SerializedComponentBatch>,

    /// Whether the column's values can be edited.
    ///
    /// If unset, editing is disabled.
    /// Edits requires a remote table with a column marked by `rerun:is_table_index` metadata and write permission.
    /// ⚠ Currently only boolean values are supported.
    pub editable: Option<SerializedComponentBatch>,

    /// Whether the column is visible in this layout.
    ///
    /// If unset, the enclosing layout determines visibility.
    /// Table layouts use the viewer default for the source column.
    /// Card layouts show sources listed in `field_order` and hide unlisted sources.
    pub visible: Option<SerializedComponentBatch>,

    /// How to render the column's values.
    ///
    /// If unset or `Auto`, the viewer infers the renderer from the component or Arrow datatype.
    pub cell_kind: Option<SerializedComponentBatch>,
}

impl TableColumn {
    /// Returns the [`ComponentDescriptor`] for [`Self::name`].
    ///
    /// The corresponding component is [`crate::components::Name`].
    #[inline]
    pub fn descriptor_name() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.TableColumn".into()),
                component: "TableColumn:name".into(),
                component_type: Some("rerun.components.Name".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::editable`].
    ///
    /// The corresponding component is [`crate::blueprint::components::Editable`].
    #[inline]
    pub fn descriptor_editable() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.TableColumn".into()),
                component: "TableColumn:editable".into(),
                component_type: Some("rerun.blueprint.components.Editable".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::visible`].
    ///
    /// The corresponding component is [`crate::components::Visible`].
    #[inline]
    pub fn descriptor_visible() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.TableColumn".into()),
                component: "TableColumn:visible".into(),
                component_type: Some("rerun.components.Visible".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::cell_kind`].
    ///
    /// The corresponding component is [`crate::blueprint::components::TableCellKind`].
    #[inline]
    pub fn descriptor_cell_kind() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.TableColumn".into()),
                component: "TableColumn:cell_kind".into(),
                component_type: Some("rerun.blueprint.components.TableCellKind".into()),
            });
        (*DESCRIPTOR).clone()
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 4usize]> =
    std::sync::LazyLock::new(|| {
        [
            TableColumn::descriptor_name(),
            TableColumn::descriptor_editable(),
            TableColumn::descriptor_visible(),
            TableColumn::descriptor_cell_kind(),
        ]
    });

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 4usize]> =
    std::sync::LazyLock::new(|| {
        [
            TableColumn::descriptor_name(),
            TableColumn::descriptor_editable(),
            TableColumn::descriptor_visible(),
            TableColumn::descriptor_cell_kind(),
        ]
    });

impl TableColumn {
    /// The total number of components in the archetype: 0 required, 0 recommended, 4 optional
    pub const NUM_COMPONENTS: usize = 4usize;
}

impl ::re_types_core::Archetype for TableColumn {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        ::re_types_core::external::re_string_interner::intern_static_nonempty!(
            ::re_types_core::ArchetypeName,
            "rerun.blueprint.archetypes.TableColumn"
        )
    }

    #[inline]
    fn display_name() -> &'static str {
        "Table column"
    }

    #[inline]
    fn required_components() -> Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
        };
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let name = arrays_by_descr
            .get(&Self::descriptor_name())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_name()));
        let editable = arrays_by_descr
            .get(&Self::descriptor_editable())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_editable()));
        let visible = arrays_by_descr
            .get(&Self::descriptor_visible())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_visible()));
        let cell_kind = arrays_by_descr
            .get(&Self::descriptor_cell_kind())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_cell_kind())
            });
        Ok(Self {
            name,
            editable,
            visible,
            cell_kind,
        })
    }
}

impl ::re_types_core::AsComponents for TableColumn {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        [
            self.name.clone(),
            self.editable.clone(),
            self.visible.clone(),
            self.cell_kind.clone(),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for TableColumn {}

impl TableColumn {
    /// Create a new `TableColumn`.
    #[inline]
    pub fn new() -> Self {
        Self {
            name: None,
            editable: None,
            visible: None,
            cell_kind: None,
        }
    }

    /// Update only some specific fields of a `TableColumn`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `TableColumn`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::ArrowDatatype as _;
        Self {
            name: Some(SerializedComponentBatch::new(
                crate::components::Name::arrow_empty(),
                Self::descriptor_name(),
            )),
            editable: Some(SerializedComponentBatch::new(
                crate::blueprint::components::Editable::arrow_empty(),
                Self::descriptor_editable(),
            )),
            visible: Some(SerializedComponentBatch::new(
                crate::components::Visible::arrow_empty(),
                Self::descriptor_visible(),
            )),
            cell_kind: Some(SerializedComponentBatch::new(
                crate::blueprint::components::TableCellKind::arrow_empty(),
                Self::descriptor_cell_kind(),
            )),
        }
    }

    /// The name shown for the column.
    ///
    /// If unset, the name is inferred from the source column.
    #[inline]
    pub fn with_name(mut self, name: impl Into<crate::components::Name>) -> Self {
        self.name = try_serialize_field(Self::descriptor_name(), [name]);
        self
    }

    /// Whether the column's values can be edited.
    ///
    /// If unset, editing is disabled.
    /// Edits requires a remote table with a column marked by `rerun:is_table_index` metadata and write permission.
    /// ⚠ Currently only boolean values are supported.
    #[inline]
    pub fn with_editable(
        mut self,
        editable: impl Into<crate::blueprint::components::Editable>,
    ) -> Self {
        self.editable = try_serialize_field(Self::descriptor_editable(), [editable]);
        self
    }

    /// Whether the column is visible in this layout.
    ///
    /// If unset, the enclosing layout determines visibility.
    /// Table layouts use the viewer default for the source column.
    /// Card layouts show sources listed in `field_order` and hide unlisted sources.
    #[inline]
    pub fn with_visible(mut self, visible: impl Into<crate::components::Visible>) -> Self {
        self.visible = try_serialize_field(Self::descriptor_visible(), [visible]);
        self
    }

    /// How to render the column's values.
    ///
    /// If unset or `Auto`, the viewer infers the renderer from the component or Arrow datatype.
    #[inline]
    pub fn with_cell_kind(
        mut self,
        cell_kind: impl Into<crate::blueprint::components::TableCellKind>,
    ) -> Self {
        self.cell_kind = try_serialize_field(Self::descriptor_cell_kind(), [cell_kind]);
        self
    }
}
