// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/blueprint/archetypes/card_layout.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Archetype**: Blueprint for displaying table records as cards.
///
/// This archetype is stored at the entity `/table/layouts/cards`.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
#[derive(Clone, Debug, Default, ::re_byte_size::SizeBytes)]
pub struct CardLayout {
    /// The source column used for card titles.
    ///
    /// If unset, the first visible string column is used as the title.
    pub title: Option<SerializedComponentBatch>,

    /// The source column containing the target opened when a card is activated.
    ///
    /// If unset, the first configured preview field is used, then the first inferred URL column.
    pub link: Option<SerializedComponentBatch>,

    /// Source columns visible by default in each card, in display order.
    ///
    /// Unlisted fields are hidden unless their [`archetypes::TableColumn`][crate::blueprint::archetypes::TableColumn] visibility overrides the default.
    /// Each source column may appear at most once.
    ///
    /// Fields with `TableCellKind::Flag` are omitted from the labeled-field list and the first one is shown in the card header.
    /// Card layouts currently support at most one flag field.
    pub field_order: Option<SerializedComponentBatch>,
}

impl CardLayout {
    /// Returns the [`ComponentDescriptor`] for [`Self::title`].
    ///
    /// The corresponding component is [`crate::blueprint::components::ColumnName`].
    #[inline]
    pub fn descriptor_title() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.CardLayout".into()),
                component: "CardLayout:title".into(),
                component_type: Some("rerun.blueprint.components.ColumnName".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::link`].
    ///
    /// The corresponding component is [`crate::blueprint::components::ColumnName`].
    #[inline]
    pub fn descriptor_link() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.CardLayout".into()),
                component: "CardLayout:link".into(),
                component_type: Some("rerun.blueprint.components.ColumnName".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::field_order`].
    ///
    /// The corresponding component is [`crate::blueprint::components::ColumnName`].
    #[inline]
    pub fn descriptor_field_order() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.blueprint.archetypes.CardLayout".into()),
                component: "CardLayout:field_order".into(),
                component_type: Some("rerun.blueprint.components.ColumnName".into()),
            });
        (*DESCRIPTOR).clone()
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [CardLayout::descriptor_field_order()]);

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 2usize]> =
    std::sync::LazyLock::new(|| {
        [
            CardLayout::descriptor_title(),
            CardLayout::descriptor_link(),
        ]
    });

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 3usize]> =
    std::sync::LazyLock::new(|| {
        [
            CardLayout::descriptor_field_order(),
            CardLayout::descriptor_title(),
            CardLayout::descriptor_link(),
        ]
    });

impl CardLayout {
    /// The total number of components in the archetype: 1 required, 0 recommended, 2 optional
    pub const NUM_COMPONENTS: usize = 3usize;
}

impl ::re_types_core::Archetype for CardLayout {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        ::re_types_core::external::re_string_interner::intern_static_nonempty!(
            ::re_types_core::ArchetypeName,
            "rerun.blueprint.archetypes.CardLayout"
        )
    }

    #[inline]
    fn display_name() -> &'static str {
        "Card layout"
    }

    #[inline]
    fn required_components() -> Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{
            ArrowDatatype as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
        };
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let title = arrays_by_descr
            .get(&Self::descriptor_title())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_title()));
        let link = arrays_by_descr
            .get(&Self::descriptor_link())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_link()));
        let field_order = arrays_by_descr
            .get(&Self::descriptor_field_order())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_field_order())
            });
        Ok(Self {
            title,
            link,
            field_order,
        })
    }
}

impl ::re_types_core::AsComponents for CardLayout {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        [
            self.title.clone(),
            self.link.clone(),
            self.field_order.clone(),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for CardLayout {}

impl CardLayout {
    /// Create a new `CardLayout`.
    #[inline]
    pub fn new(
        field_order: impl IntoIterator<Item = impl Into<crate::blueprint::components::ColumnName>>,
    ) -> Self {
        Self {
            field_order: try_serialize_field(Self::descriptor_field_order(), field_order),
            title: None,
            link: None,
        }
    }

    /// Update only some specific fields of a `CardLayout`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `CardLayout`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::ArrowDatatype as _;
        Self {
            field_order: Some(SerializedComponentBatch::new(
                crate::blueprint::components::ColumnName::arrow_empty(),
                Self::descriptor_field_order(),
            )),
            title: Some(SerializedComponentBatch::new(
                crate::blueprint::components::ColumnName::arrow_empty(),
                Self::descriptor_title(),
            )),
            link: Some(SerializedComponentBatch::new(
                crate::blueprint::components::ColumnName::arrow_empty(),
                Self::descriptor_link(),
            )),
        }
    }

    /// Source columns visible by default in each card, in display order.
    ///
    /// Unlisted fields are hidden unless their [`archetypes::TableColumn`][crate::blueprint::archetypes::TableColumn] visibility overrides the default.
    /// Each source column may appear at most once.
    ///
    /// Fields with `TableCellKind::Flag` are omitted from the labeled-field list and the first one is shown in the card header.
    /// Card layouts currently support at most one flag field.
    #[inline]
    pub fn with_field_order(
        mut self,
        field_order: impl IntoIterator<Item = impl Into<crate::blueprint::components::ColumnName>>,
    ) -> Self {
        self.field_order = try_serialize_field(Self::descriptor_field_order(), field_order);
        self
    }

    /// The source column used for card titles.
    ///
    /// If unset, the first visible string column is used as the title.
    #[inline]
    pub fn with_title(
        mut self,
        title: impl Into<crate::blueprint::components::ColumnName>,
    ) -> Self {
        self.title = try_serialize_field(Self::descriptor_title(), [title]);
        self
    }

    /// The source column containing the target opened when a card is activated.
    ///
    /// If unset, the first configured preview field is used, then the first inferred URL column.
    #[inline]
    pub fn with_link(mut self, link: impl Into<crate::blueprint::components::ColumnName>) -> Self {
        self.link = try_serialize_field(Self::descriptor_link(), [link]);
        self
    }
}
