// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/build/re_type_definitions/rerun/archetypes/measurements.def.rs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::arrow::array::ArrayRef;
use ::re_types_core::SerializationResult;
use ::re_types_core::SerializedComponentBatch;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};
use ::std::borrow::Cow;

/// **Archetype**: One or more scalar measurements, each with a variance and a unit.
///
/// Use this for sensors that report a value together with its uncertainty:
/// pressure, temperature, illuminance, relative humidity, range, and so on.
///
/// In a [`views::TimeSeriesView`][crate::blueprint::views::TimeSeriesView] each series is drawn as a line with a translucent
/// band around it, one standard deviation (the square root of the variance) wide in each direction.
/// Leave `variances` unset if the uncertainty is unknown.
///
/// The current timeline value is used for the time/X-axis, so measurements should not be
/// static. Number of values per timestamp is expected to be the same over time.
///
/// Unlike [`archetypes::Scalars`][crate::archetypes::Scalars], this archetype carries its own styling, so values and style
/// are logged together.
/// Changes over time are supported for most but not all styling fields (see respective fields for details).
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
///
/// ## Example
///
/// ### Pressure with variance
/// ```ignore
/// fn main() -> Result<(), Box<dyn std::error::Error>> {
///     let rec =
///         rerun::RecordingStreamBuilder::new("rerun_example_measurements_simple")
///             .spawn()?;
///
///     // Two parallel pressure sensors (in Pa), each with slowly drifting variance.
///     for step in 0..64 {
///         rec.set_time_sequence("step", step);
///         let pressures = [
///             101_325.0 + 50.0 * (step as f64 / 10.0).sin(),
///             101_300.0 + 30.0 * (step as f64 / 8.0).cos(),
///         ];
///         let variances = [
///             100.0 + 25.0 * (step as f64 / 7.0).sin(),
///             80.0 + 15.0 * (step as f64 / 11.0).cos(),
///         ];
///         rec.log(
///             "pressure",
///             &rerun::Measurements::new(pressures)
///                 .with_variances(variances)
///                 .with_units(["Pa"]),
///         )?;
///     }
///
///     Ok(())
/// }
/// ```
/// <center>
/// <picture>
///   <source media="(max-width: 480px)" srcset="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/480w.png">
///   <source media="(max-width: 768px)" srcset="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/768w.png">
///   <source media="(max-width: 1024px)" srcset="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/1024w.png">
///   <img src="https://static.rerun.io/measurements/2388490ab2b487bb6c47a2be3e7d5e7aa17c08f3/full.png" width="640">
/// </picture>
/// </center>
#[derive(Clone, Debug, PartialEq, Default, ::re_byte_size::SizeBytes)]
pub struct Measurements {
    /// The measured scalar values.
    pub values: Option<SerializedComponentBatch>,

    /// Variances of the measurements (σ², in the units of `values` squared).
    ///
    /// When set, length is expected to match `values`.
    pub variances: Option<SerializedComponentBatch>,

    /// Units of the measurements, shown in the legend and in tooltips.
    ///
    /// When set, length is expected to match `values`.
    /// Expected to be unchanging over time.
    pub units: Option<SerializedComponentBatch>,

    /// Color for the corresponding series.
    ///
    /// May change over time, but can cause discontinuities in the line.
    pub colors: Option<SerializedComponentBatch>,

    /// Stroke width for the corresponding series.
    ///
    /// May change over time, but can cause discontinuities in the line.
    pub widths: Option<SerializedComponentBatch>,

    /// Display name of the series.
    ///
    /// Used in the legend. Expected to be unchanging over time.
    pub names: Option<SerializedComponentBatch>,

    /// Which series are visible.
    ///
    /// If not set, all series on this entity are visible.
    /// Unlike with the regular visibility property of the entire entity, any series that is hidden
    /// via this property will still be visible in the legend.
    ///
    /// May change over time, but can cause discontinuities in the line.
    pub visible_series: Option<SerializedComponentBatch>,

    /// Configures the zoom-dependent scalar aggregation.
    ///
    /// This is done only if steps on the X axis go below a single pixel,
    /// i.e. a single pixel covers more than one tick worth of data. It can greatly improve performance
    /// (and readability) in such situations as it prevents overdraw.
    ///
    /// Expected to be unchanging over time.
    pub aggregation_policy: Option<SerializedComponentBatch>,

    /// Specifies how values between data points are interpolated.
    ///
    /// Defaults to linear interpolation. Use one of the `Step*` variants for a stepped (staircase) line.
    ///
    /// Expected to be unchanging over time.
    pub interpolation_mode: Option<SerializedComponentBatch>,
}

impl Measurements {
    /// Returns the [`ComponentDescriptor`] for [`Self::values`].
    ///
    /// The corresponding component is [`crate::components::Scalar`].
    #[inline]
    pub fn descriptor_values() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:values".into(),
                component_type: Some("rerun.components.Scalar".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::variances`].
    ///
    /// The corresponding component is [`crate::components::Variance`].
    #[inline]
    pub fn descriptor_variances() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:variances".into(),
                component_type: Some("rerun.components.Variance".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::units`].
    ///
    /// The corresponding component is [`crate::components::Unit`].
    #[inline]
    pub fn descriptor_units() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:units".into(),
                component_type: Some("rerun.components.Unit".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::colors`].
    ///
    /// The corresponding component is [`crate::components::Color`].
    #[inline]
    pub fn descriptor_colors() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:colors".into(),
                component_type: Some("rerun.components.Color".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::widths`].
    ///
    /// The corresponding component is [`crate::components::StrokeWidth`].
    #[inline]
    pub fn descriptor_widths() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:widths".into(),
                component_type: Some("rerun.components.StrokeWidth".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::names`].
    ///
    /// The corresponding component is [`crate::components::Name`].
    #[inline]
    pub fn descriptor_names() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:names".into(),
                component_type: Some("rerun.components.Name".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::visible_series`].
    ///
    /// The corresponding component is [`crate::components::Visible`].
    #[inline]
    pub fn descriptor_visible_series() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:visible_series".into(),
                component_type: Some("rerun.components.Visible".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::aggregation_policy`].
    ///
    /// The corresponding component is [`crate::components::AggregationPolicy`].
    #[inline]
    pub fn descriptor_aggregation_policy() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:aggregation_policy".into(),
                component_type: Some("rerun.components.AggregationPolicy".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::interpolation_mode`].
    ///
    /// The corresponding component is [`crate::components::InterpolationMode`].
    #[inline]
    pub fn descriptor_interpolation_mode() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.Measurements".into()),
                component: "Measurements:interpolation_mode".into(),
                component_type: Some("rerun.components.InterpolationMode".into()),
            });
        (*DESCRIPTOR).clone()
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [Measurements::descriptor_values()]);

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [Measurements::descriptor_variances()]);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 7usize]> =
    std::sync::LazyLock::new(|| {
        [
            Measurements::descriptor_units(),
            Measurements::descriptor_colors(),
            Measurements::descriptor_widths(),
            Measurements::descriptor_names(),
            Measurements::descriptor_visible_series(),
            Measurements::descriptor_aggregation_policy(),
            Measurements::descriptor_interpolation_mode(),
        ]
    });

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 9usize]> =
    std::sync::LazyLock::new(|| {
        [
            Measurements::descriptor_values(),
            Measurements::descriptor_variances(),
            Measurements::descriptor_units(),
            Measurements::descriptor_colors(),
            Measurements::descriptor_widths(),
            Measurements::descriptor_names(),
            Measurements::descriptor_visible_series(),
            Measurements::descriptor_aggregation_policy(),
            Measurements::descriptor_interpolation_mode(),
        ]
    });

impl Measurements {
    /// The total number of components in the archetype: 1 required, 1 recommended, 7 optional
    pub const NUM_COMPONENTS: usize = 9usize;
}

impl ::re_types_core::Archetype for Measurements {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        ::re_types_core::external::re_string_interner::intern_static_nonempty!(
            ::re_types_core::ArchetypeName,
            "rerun.archetypes.Measurements"
        )
    }

    #[inline]
    fn display_name() -> &'static str {
        "Measurements"
    }

    #[inline]
    fn required_components() -> Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{
            ArrowDataType as _, FromArrow as _, FromArrowOpt as _, ResultExt as _,
        };
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let values = arrays_by_descr
            .get(&Self::descriptor_values())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_values()));
        let variances = arrays_by_descr
            .get(&Self::descriptor_variances())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_variances())
            });
        let units = arrays_by_descr
            .get(&Self::descriptor_units())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_units()));
        let colors = arrays_by_descr
            .get(&Self::descriptor_colors())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_colors()));
        let widths = arrays_by_descr
            .get(&Self::descriptor_widths())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_widths()));
        let names = arrays_by_descr
            .get(&Self::descriptor_names())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_names()));
        let visible_series = arrays_by_descr
            .get(&Self::descriptor_visible_series())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_visible_series())
            });
        let aggregation_policy = arrays_by_descr
            .get(&Self::descriptor_aggregation_policy())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_aggregation_policy())
            });
        let interpolation_mode = arrays_by_descr
            .get(&Self::descriptor_interpolation_mode())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_interpolation_mode())
            });
        Ok(Self {
            values,
            variances,
            units,
            colors,
            widths,
            names,
            visible_series,
            aggregation_policy,
            interpolation_mode,
        })
    }
}

impl ::re_types_core::AsComponents for Measurements {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        [
            self.values.clone(),
            self.variances.clone(),
            self.units.clone(),
            self.colors.clone(),
            self.widths.clone(),
            self.names.clone(),
            self.visible_series.clone(),
            self.aggregation_policy.clone(),
            self.interpolation_mode.clone(),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for Measurements {}

impl crate::VisualizableArchetype for Measurements {
    #[inline]
    fn visualizer(&self) -> crate::Visualizer {
        crate::Visualizer::new("MeasurementsSeries").with_overrides(self)
    }
}

impl Measurements {
    /// Create a new `Measurements`.
    #[inline]
    pub fn new(values: impl IntoIterator<Item = impl Into<crate::components::Scalar>>) -> Self {
        Self {
            values: try_serialize_field(Self::descriptor_values(), values),
            variances: None,
            units: None,
            colors: None,
            widths: None,
            names: None,
            visible_series: None,
            aggregation_policy: None,
            interpolation_mode: None,
        }
    }

    /// Update only some specific fields of a `Measurements`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `Measurements`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::ArrowDataType as _;
        Self {
            values: Some(SerializedComponentBatch::new(
                crate::components::Scalar::arrow_empty(),
                Self::descriptor_values(),
            )),
            variances: Some(SerializedComponentBatch::new(
                crate::components::Variance::arrow_empty(),
                Self::descriptor_variances(),
            )),
            units: Some(SerializedComponentBatch::new(
                crate::components::Unit::arrow_empty(),
                Self::descriptor_units(),
            )),
            colors: Some(SerializedComponentBatch::new(
                crate::components::Color::arrow_empty(),
                Self::descriptor_colors(),
            )),
            widths: Some(SerializedComponentBatch::new(
                crate::components::StrokeWidth::arrow_empty(),
                Self::descriptor_widths(),
            )),
            names: Some(SerializedComponentBatch::new(
                crate::components::Name::arrow_empty(),
                Self::descriptor_names(),
            )),
            visible_series: Some(SerializedComponentBatch::new(
                crate::components::Visible::arrow_empty(),
                Self::descriptor_visible_series(),
            )),
            aggregation_policy: Some(SerializedComponentBatch::new(
                crate::components::AggregationPolicy::arrow_empty(),
                Self::descriptor_aggregation_policy(),
            )),
            interpolation_mode: Some(SerializedComponentBatch::new(
                crate::components::InterpolationMode::arrow_empty(),
                Self::descriptor_interpolation_mode(),
            )),
        }
    }

    /// Partitions the component data into multiple sub-batches.
    ///
    /// Specifically, this transforms the existing [`SerializedComponentBatch`]es data into [`SerializedComponentColumn`]s
    /// instead, via [`SerializedComponentBatch::partitioned`].
    ///
    /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
    ///
    /// The specified `lengths` must sum to the total length of the component batch.
    ///
    /// [`SerializedComponentColumn`]: [::re_types_core::SerializedComponentColumn]
    #[inline]
    pub fn columns<I>(
        self,
        _lengths: I,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>>
    where
        I: IntoIterator<Item = usize> + Clone,
    {
        let columns = [
            self.values
                .map(|values| values.partitioned(_lengths.clone()))
                .transpose()?,
            self.variances
                .map(|variances| variances.partitioned(_lengths.clone()))
                .transpose()?,
            self.units
                .map(|units| units.partitioned(_lengths.clone()))
                .transpose()?,
            self.colors
                .map(|colors| colors.partitioned(_lengths.clone()))
                .transpose()?,
            self.widths
                .map(|widths| widths.partitioned(_lengths.clone()))
                .transpose()?,
            self.names
                .map(|names| names.partitioned(_lengths.clone()))
                .transpose()?,
            self.visible_series
                .map(|visible_series| visible_series.partitioned(_lengths.clone()))
                .transpose()?,
            self.aggregation_policy
                .map(|aggregation_policy| aggregation_policy.partitioned(_lengths.clone()))
                .transpose()?,
            self.interpolation_mode
                .map(|interpolation_mode| interpolation_mode.partitioned(_lengths.clone()))
                .transpose()?,
        ];
        Ok(columns.into_iter().flatten())
    }

    /// Helper to partition the component data into unit-length sub-batches.
    ///
    /// This is semantically similar to calling [`Self::columns`] with `std::iter::take(1).repeat(n)`,
    /// where `n` is automatically guessed.
    #[inline]
    pub fn columns_of_unit_batches(
        self,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>> {
        let len_values = self.values.as_ref().map(|b| b.array.len());
        let len_variances = self.variances.as_ref().map(|b| b.array.len());
        let len_units = self.units.as_ref().map(|b| b.array.len());
        let len_colors = self.colors.as_ref().map(|b| b.array.len());
        let len_widths = self.widths.as_ref().map(|b| b.array.len());
        let len_names = self.names.as_ref().map(|b| b.array.len());
        let len_visible_series = self.visible_series.as_ref().map(|b| b.array.len());
        let len_aggregation_policy = self.aggregation_policy.as_ref().map(|b| b.array.len());
        let len_interpolation_mode = self.interpolation_mode.as_ref().map(|b| b.array.len());
        let len = None
            .or(len_values)
            .or(len_variances)
            .or(len_units)
            .or(len_colors)
            .or(len_widths)
            .or(len_names)
            .or(len_visible_series)
            .or(len_aggregation_policy)
            .or(len_interpolation_mode)
            .unwrap_or(0);
        self.columns(std::iter::repeat_n(1, len))
    }

    /// The measured scalar values.
    #[inline]
    pub fn with_values(
        mut self,
        values: impl IntoIterator<Item = impl Into<crate::components::Scalar>>,
    ) -> Self {
        self.values = try_serialize_field(Self::descriptor_values(), values);
        self
    }

    /// Variances of the measurements (σ², in the units of `values` squared).
    ///
    /// When set, length is expected to match `values`.
    #[inline]
    pub fn with_variances(
        mut self,
        variances: impl IntoIterator<Item = impl Into<crate::components::Variance>>,
    ) -> Self {
        self.variances = try_serialize_field(Self::descriptor_variances(), variances);
        self
    }

    /// Units of the measurements, shown in the legend and in tooltips.
    ///
    /// When set, length is expected to match `values`.
    /// Expected to be unchanging over time.
    #[inline]
    pub fn with_units(
        mut self,
        units: impl IntoIterator<Item = impl Into<crate::components::Unit>>,
    ) -> Self {
        self.units = try_serialize_field(Self::descriptor_units(), units);
        self
    }

    /// Color for the corresponding series.
    ///
    /// May change over time, but can cause discontinuities in the line.
    #[inline]
    pub fn with_colors(
        mut self,
        colors: impl IntoIterator<Item = impl Into<crate::components::Color>>,
    ) -> Self {
        self.colors = try_serialize_field(Self::descriptor_colors(), colors);
        self
    }

    /// Stroke width for the corresponding series.
    ///
    /// May change over time, but can cause discontinuities in the line.
    #[inline]
    pub fn with_widths(
        mut self,
        widths: impl IntoIterator<Item = impl Into<crate::components::StrokeWidth>>,
    ) -> Self {
        self.widths = try_serialize_field(Self::descriptor_widths(), widths);
        self
    }

    /// Display name of the series.
    ///
    /// Used in the legend. Expected to be unchanging over time.
    #[inline]
    pub fn with_names(
        mut self,
        names: impl IntoIterator<Item = impl Into<crate::components::Name>>,
    ) -> Self {
        self.names = try_serialize_field(Self::descriptor_names(), names);
        self
    }

    /// Which series are visible.
    ///
    /// If not set, all series on this entity are visible.
    /// Unlike with the regular visibility property of the entire entity, any series that is hidden
    /// via this property will still be visible in the legend.
    ///
    /// May change over time, but can cause discontinuities in the line.
    #[inline]
    pub fn with_visible_series(
        mut self,
        visible_series: impl IntoIterator<Item = impl Into<crate::components::Visible>>,
    ) -> Self {
        self.visible_series =
            try_serialize_field(Self::descriptor_visible_series(), visible_series);
        self
    }

    /// Configures the zoom-dependent scalar aggregation.
    ///
    /// This is done only if steps on the X axis go below a single pixel,
    /// i.e. a single pixel covers more than one tick worth of data. It can greatly improve performance
    /// (and readability) in such situations as it prevents overdraw.
    ///
    /// Expected to be unchanging over time.
    #[inline]
    pub fn with_aggregation_policy(
        mut self,
        aggregation_policy: impl Into<crate::components::AggregationPolicy>,
    ) -> Self {
        self.aggregation_policy =
            try_serialize_field(Self::descriptor_aggregation_policy(), [aggregation_policy]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::AggregationPolicy`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_aggregation_policy`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_aggregation_policy(
        mut self,
        aggregation_policy: impl IntoIterator<Item = impl Into<crate::components::AggregationPolicy>>,
    ) -> Self {
        self.aggregation_policy =
            try_serialize_field(Self::descriptor_aggregation_policy(), aggregation_policy);
        self
    }

    /// Specifies how values between data points are interpolated.
    ///
    /// Defaults to linear interpolation. Use one of the `Step*` variants for a stepped (staircase) line.
    ///
    /// Expected to be unchanging over time.
    #[inline]
    pub fn with_interpolation_mode(
        mut self,
        interpolation_mode: impl Into<crate::components::InterpolationMode>,
    ) -> Self {
        self.interpolation_mode =
            try_serialize_field(Self::descriptor_interpolation_mode(), [interpolation_mode]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::InterpolationMode`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_interpolation_mode`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_interpolation_mode(
        mut self,
        interpolation_mode: impl IntoIterator<Item = impl Into<crate::components::InterpolationMode>>,
    ) -> Self {
        self.interpolation_mode =
            try_serialize_field(Self::descriptor_interpolation_mode(), interpolation_mode);
        self
    }
}
