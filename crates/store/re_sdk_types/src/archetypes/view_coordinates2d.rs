// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/view_coordinates2d.fbs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::re_types_core::SerializationResult;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentBatch as _, SerializedComponentBatch};
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: How we interpret the 2D coordinate system of an entity/space.
///
/// For instance: Does the Y axis point up or down?
///
/// The two coordinates are always ordered as [x, y].
///
/// For example [Right, Down] means that the X axis points to the right, and the Y axis points
/// down. This is the default 2D coordinate system and common image/screen convention.
///
/// Only the 4 axis-flip combinations are supported:
///  * **RD** (Right, Down) - default, image/screen convention
///  * **RU** (Right, Up) - math/plot convention
///  * **LD** (Left, Down) - horizontally mirrored image
///  * **LU** (Left, Up) - both axes flipped
///
/// Make sure that this archetype is logged at or above the origin entity path of your 2D views.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
///
/// ## Example
///
/// ### View coordinates for adjusting the 2D coordinate system
/// ```ignore
/// fn main() -> Result<(), Box<dyn std::error::Error>> {
///     let rec = rerun::RecordingStreamBuilder::new("rerun_example_view_coordinates2d").spawn()?;
///
///     rec.log_static("world", &rerun::ViewCoordinates2D::RU())?; // Set Y-Up
///
///     rec.log(
///         "world/points",
///         &rerun::Points2D::new([(0.0, 0.0), (1.0, 1.0), (3.0, 2.0)]),
///     )?;
///
///     Ok(())
/// }
/// ```
#[derive(Clone, Debug, PartialEq, Default)]
#[repr(transparent)]
pub struct ViewCoordinates2D {
    /// The directions of the [x, y] axes.
    pub xy: Option<SerializedComponentBatch>,
}

impl ViewCoordinates2D {
    /// Returns the [`ComponentDescriptor`] for [`Self::xy`].
    ///
    /// The corresponding component is [`crate::components::ViewCoordinates2D`].
    #[inline]
    pub fn descriptor_xy() -> ComponentDescriptor {
        ComponentDescriptor {
            archetype: Some("rerun.archetypes.ViewCoordinates2D".into()),
            component: "ViewCoordinates2D:xy".into(),
            component_type: Some("rerun.components.ViewCoordinates2D".into()),
        }
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [ViewCoordinates2D::descriptor_xy()]);

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [ViewCoordinates2D::descriptor_xy()]);

impl ViewCoordinates2D {
    /// The total number of components in the archetype: 1 required, 0 recommended, 0 optional
    pub const NUM_COMPONENTS: usize = 1usize;
}

impl ::re_types_core::Archetype for ViewCoordinates2D {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        "rerun.archetypes.ViewCoordinates2D".into()
    }

    #[inline]
    fn display_name() -> &'static str {
        "View coordinates 2D"
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, arrow::array::ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let xy = arrays_by_descr
            .get(&Self::descriptor_xy())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_xy()));
        Ok(Self { xy })
    }
}

impl ::re_types_core::AsComponents for ViewCoordinates2D {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        std::iter::once(self.xy.clone()).flatten().collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for ViewCoordinates2D {}

impl ViewCoordinates2D {
    /// Create a new `ViewCoordinates2D`.
    #[inline]
    pub fn new(xy: impl Into<crate::components::ViewCoordinates2D>) -> Self {
        Self {
            xy: try_serialize_field(Self::descriptor_xy(), [xy]),
        }
    }

    /// Update only some specific fields of a `ViewCoordinates2D`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `ViewCoordinates2D`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::Loggable as _;
        Self {
            xy: Some(SerializedComponentBatch::new(
                crate::components::ViewCoordinates2D::arrow_empty(),
                Self::descriptor_xy(),
            )),
        }
    }

    /// Partitions the component data into multiple sub-batches.
    ///
    /// Specifically, this transforms the existing [`SerializedComponentBatch`]es data into [`SerializedComponentColumn`]s
    /// instead, via [`SerializedComponentBatch::partitioned`].
    ///
    /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
    ///
    /// The specified `lengths` must sum to the total length of the component batch.
    ///
    /// [`SerializedComponentColumn`]: [::re_types_core::SerializedComponentColumn]
    #[inline]
    pub fn columns<I>(
        self,
        _lengths: I,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>>
    where
        I: IntoIterator<Item = usize> + Clone,
    {
        let columns = [self
            .xy
            .map(|xy| xy.partitioned(_lengths.clone()))
            .transpose()?];
        Ok(columns.into_iter().flatten())
    }

    /// Helper to partition the component data into unit-length sub-batches.
    ///
    /// This is semantically similar to calling [`Self::columns`] with `std::iter::take(1).repeat(n)`,
    /// where `n` is automatically guessed.
    #[inline]
    pub fn columns_of_unit_batches(
        self,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>> {
        let len_xy = self.xy.as_ref().map(|b| b.array.len());
        let len = None.or(len_xy).unwrap_or(0);
        self.columns(std::iter::repeat_n(1, len))
    }

    /// The directions of the [x, y] axes.
    #[inline]
    pub fn with_xy(mut self, xy: impl Into<crate::components::ViewCoordinates2D>) -> Self {
        self.xy = try_serialize_field(Self::descriptor_xy(), [xy]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::ViewCoordinates2D`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_xy`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_xy(
        mut self,
        xy: impl IntoIterator<Item = impl Into<crate::components::ViewCoordinates2D>>,
    ) -> Self {
        self.xy = try_serialize_field(Self::descriptor_xy(), xy);
        self
    }
}

impl ::re_byte_size::SizeBytes for ViewCoordinates2D {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.xy.heap_size_bytes()
    }
}
