// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/status.fbs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::re_types_core::SerializationResult;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentBatch as _, SerializedComponentBatch};
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: A status update, representing a change in the status of an entity.
///
/// Useful for representing discrete state machines, mode transitions, or
/// status changes over time. Each logged [`archetypes::Status`][crate::archetypes::Status] marks a new status
/// at the given time. A `null` status is ignored by the Status view.
///
/// The Status view displays these as horizontal colored lanes over time.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
///
/// ## Example
///
/// ### Status changes over time
/// ```ignore
/// fn main() -> Result<(), Box<dyn std::error::Error>> {
///     let rec = rerun::RecordingStreamBuilder::new("rerun_example_status").spawn()?;
///
///     rec.set_time_sequence("step", 0);
///     rec.log("door", &rerun::Status::new().with_status("open"))?;
///
///     rec.set_time_sequence("step", 1);
///     rec.log("door", &rerun::Status::new().with_status("closed"))?;
///
///     rec.set_time_sequence("step", 2);
///     rec.log("door", &rerun::Status::new().with_status("open"))?;
///
///     Ok(())
/// }
/// ```
/// <center>
/// <picture>
///   <source media="(max-width: 480px)" srcset="https://static.rerun.io/status/8f224c6e4a9cbbb4b1e279c56a426ec4c6bfca50/480w.png">
///   <source media="(max-width: 768px)" srcset="https://static.rerun.io/status/8f224c6e4a9cbbb4b1e279c56a426ec4c6bfca50/768w.png">
///   <source media="(max-width: 1024px)" srcset="https://static.rerun.io/status/8f224c6e4a9cbbb4b1e279c56a426ec4c6bfca50/1024w.png">
///   <source media="(max-width: 1200px)" srcset="https://static.rerun.io/status/8f224c6e4a9cbbb4b1e279c56a426ec4c6bfca50/1200w.png">
///   <img src="https://static.rerun.io/status/8f224c6e4a9cbbb4b1e279c56a426ec4c6bfca50/full.png" width="640">
/// </picture>
/// </center>
#[derive(Clone, Debug, PartialEq, Default)]
pub struct Status {
    /// The new status value. A `null` status is ignored, it can be used to partially update a multi-instance status array.
    pub status: Option<SerializedComponentBatch>,
}

impl Status {
    /// Returns the [`ComponentDescriptor`] for [`Self::status`].
    ///
    /// The corresponding component is [`crate::components::Text`].
    #[inline]
    pub fn descriptor_status() -> ComponentDescriptor {
        ComponentDescriptor {
            archetype: Some("rerun.archetypes.Status".into()),
            component: "Status:status".into(),
            component_type: Some("rerun.components.Text".into()),
        }
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [Status::descriptor_status()]);

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [Status::descriptor_status()]);

impl Status {
    /// The total number of components in the archetype: 1 required, 0 recommended, 0 optional
    pub const NUM_COMPONENTS: usize = 1usize;
}

impl ::re_types_core::Archetype for Status {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        "rerun.archetypes.Status".into()
    }

    #[inline]
    fn display_name() -> &'static str {
        "Status"
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, arrow::array::ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let status = arrays_by_descr
            .get(&Self::descriptor_status())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_status()));
        Ok(Self { status })
    }
}

impl ::re_types_core::AsComponents for Status {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        std::iter::once(self.status.clone()).flatten().collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for Status {}

impl crate::VisualizableArchetype for Status {
    #[inline]
    fn visualizer(&self) -> crate::Visualizer {
        crate::Visualizer::new("StatusVisualizer").with_overrides(self)
    }
}

impl Status {
    /// Create a new `Status`.
    #[inline]
    pub fn new() -> Self {
        Self { status: None }
    }

    /// Update only some specific fields of a `Status`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `Status`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::Loggable as _;
        Self {
            status: Some(SerializedComponentBatch::new(
                crate::components::Text::arrow_empty(),
                Self::descriptor_status(),
            )),
        }
    }

    /// Partitions the component data into multiple sub-batches.
    ///
    /// Specifically, this transforms the existing [`SerializedComponentBatch`]es data into [`SerializedComponentColumn`]s
    /// instead, via [`SerializedComponentBatch::partitioned`].
    ///
    /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
    ///
    /// The specified `lengths` must sum to the total length of the component batch.
    ///
    /// [`SerializedComponentColumn`]: [::re_types_core::SerializedComponentColumn]
    #[inline]
    pub fn columns<I>(
        self,
        _lengths: I,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>>
    where
        I: IntoIterator<Item = usize> + Clone,
    {
        let columns = [self
            .status
            .map(|status| status.partitioned(_lengths.clone()))
            .transpose()?];
        Ok(columns.into_iter().flatten())
    }

    /// Helper to partition the component data into unit-length sub-batches.
    ///
    /// This is semantically similar to calling [`Self::columns`] with `std::iter::take(1).repeat(n)`,
    /// where `n` is automatically guessed.
    #[inline]
    pub fn columns_of_unit_batches(
        self,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>> {
        let len_status = self.status.as_ref().map(|b| b.array.len());
        let len = None.or(len_status).unwrap_or(0);
        self.columns(std::iter::repeat_n(1, len))
    }

    /// The new status value. A `null` status is ignored, it can be used to partially update a multi-instance status array.
    #[inline]
    pub fn with_status(mut self, status: impl Into<crate::components::Text>) -> Self {
        self.status = try_serialize_field(Self::descriptor_status(), [status]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::Text`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_status`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_status(
        mut self,
        status: impl IntoIterator<Item = impl Into<crate::components::Text>>,
    ) -> Self {
        self.status = try_serialize_field(Self::descriptor_status(), status);
        self
    }
}

impl ::re_byte_size::SizeBytes for Status {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.status.heap_size_bytes()
    }
}
