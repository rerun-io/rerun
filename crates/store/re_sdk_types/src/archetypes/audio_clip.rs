// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_clip.fbs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::re_types_core::SerializationResult;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentBatch as _, SerializedComponentBatch};
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: A chunk of PCM audio samples.
///
/// The samples are stored as a numeric tensor. 1D tensors are interpreted as mono audio.
/// 2D tensors are interpreted as `[sample, channel]` audio, and may include dimension
/// names such as `"sample"` and `"channel"`.
///
/// Log audio chunks on a time timeline at the time where the first sample starts. The
/// [`components::SampleRate`][crate::components::SampleRate] determines how sample indices map to time, allowing the
/// viewer to display waveforms and keep audio-aligned views in sync with the global
/// timeline.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
///
/// ## Example
///
/// ### Log a multi-channel audio clip.
/// ```ignore
/// use ndarray::{Array2, ShapeBuilder as _};
///
/// fn main() -> Result<(), Box<dyn std::error::Error>> {
///     let rec =
///         rerun::RecordingStreamBuilder::new("rerun_example_audio_clip")
///             .spawn()?;
///
///     let sample_rate = 16_000.0;
///     let seconds = 2.0;
///     let sample_count = (sample_rate * seconds) as usize;
///
///     let samples = Array2::from_shape_fn((sample_count, 2).f(), |(i, channel)| {
///         let t = i as f32 / sample_rate as f32;
///         let frequency = if channel == 0 { 220.0 } else { 330.0 };
///         0.4 * (std::f32::consts::TAU * frequency * t).sin()
///     });
///
///     rec.set_time("time", rerun::Duration::from_secs(0.0));
///     let samples =
///         rerun::datatypes::TensorData::try_from(samples)?.with_dim_names(["sample", "channel"]);
///
///     rec.log(
///         "audio",
///         &rerun::AudioClip::new(samples, sample_rate).with_channel_names(["left", "right"]),
///     )?;
///
///     Ok(())
/// }
/// ```
#[derive(Clone, Debug, PartialEq, Default, ::re_byte_size::SizeBytes)]
pub struct AudioClip {
    /// PCM audio samples.
    pub samples: Option<SerializedComponentBatch>,

    /// Sample rate in Hertz.
    pub sample_rate: Option<SerializedComponentBatch>,

    /// Optional display names for each audio channel.
    pub channel_names: Option<SerializedComponentBatch>,
}

impl AudioClip {
    /// Returns the [`ComponentDescriptor`] for [`Self::samples`].
    ///
    /// The corresponding component is [`crate::components::TensorData`].
    #[inline]
    pub fn descriptor_samples() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.AudioClip".into()),
                component: "AudioClip:samples".into(),
                component_type: Some("rerun.components.TensorData".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::sample_rate`].
    ///
    /// The corresponding component is [`crate::components::SampleRate`].
    #[inline]
    pub fn descriptor_sample_rate() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.AudioClip".into()),
                component: "AudioClip:sample_rate".into(),
                component_type: Some("rerun.components.SampleRate".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::channel_names`].
    ///
    /// The corresponding component is [`crate::components::AudioChannelName`].
    #[inline]
    pub fn descriptor_channel_names() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.AudioClip".into()),
                component: "AudioClip:channel_names".into(),
                component_type: Some("rerun.components.AudioChannelName".into()),
            });
        (*DESCRIPTOR).clone()
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 2usize]> =
    std::sync::LazyLock::new(|| {
        [
            AudioClip::descriptor_samples(),
            AudioClip::descriptor_sample_rate(),
        ]
    });

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [AudioClip::descriptor_channel_names()]);

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 3usize]> =
    std::sync::LazyLock::new(|| {
        [
            AudioClip::descriptor_samples(),
            AudioClip::descriptor_sample_rate(),
            AudioClip::descriptor_channel_names(),
        ]
    });

impl AudioClip {
    /// The total number of components in the archetype: 2 required, 0 recommended, 1 optional
    pub const NUM_COMPONENTS: usize = 3usize;
}

impl ::re_types_core::Archetype for AudioClip {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        ::re_types_core::external::re_string_interner::intern_static!(
            ::re_types_core::ArchetypeName,
            "rerun.archetypes.AudioClip"
        )
    }

    #[inline]
    fn display_name() -> &'static str {
        "Audio clip"
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, arrow::array::ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let samples = arrays_by_descr
            .get(&Self::descriptor_samples())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_samples()));
        let sample_rate = arrays_by_descr
            .get(&Self::descriptor_sample_rate())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_sample_rate())
            });
        let channel_names = arrays_by_descr
            .get(&Self::descriptor_channel_names())
            .map(|array| {
                SerializedComponentBatch::new(array.clone(), Self::descriptor_channel_names())
            });
        Ok(Self {
            samples,
            sample_rate,
            channel_names,
        })
    }
}

impl ::re_types_core::AsComponents for AudioClip {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        [
            self.samples.clone(),
            self.sample_rate.clone(),
            self.channel_names.clone(),
        ]
        .into_iter()
        .flatten()
        .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for AudioClip {}

impl crate::VisualizableArchetype for AudioClip {
    #[inline]
    fn visualizer(&self) -> crate::Visualizer {
        crate::Visualizer::new("Audio").with_overrides(self)
    }
}

impl AudioClip {
    /// Create a new `AudioClip`.
    #[inline]
    pub fn new(
        samples: impl Into<crate::components::TensorData>,
        sample_rate: impl Into<crate::components::SampleRate>,
    ) -> Self {
        Self {
            samples: try_serialize_field(Self::descriptor_samples(), [samples]),
            sample_rate: try_serialize_field(Self::descriptor_sample_rate(), [sample_rate]),
            channel_names: None,
        }
    }

    /// Update only some specific fields of a `AudioClip`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `AudioClip`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::Loggable as _;
        Self {
            samples: Some(SerializedComponentBatch::new(
                crate::components::TensorData::arrow_empty(),
                Self::descriptor_samples(),
            )),
            sample_rate: Some(SerializedComponentBatch::new(
                crate::components::SampleRate::arrow_empty(),
                Self::descriptor_sample_rate(),
            )),
            channel_names: Some(SerializedComponentBatch::new(
                crate::components::AudioChannelName::arrow_empty(),
                Self::descriptor_channel_names(),
            )),
        }
    }

    /// Partitions the component data into multiple sub-batches.
    ///
    /// Specifically, this transforms the existing [`SerializedComponentBatch`]es data into [`SerializedComponentColumn`]s
    /// instead, via [`SerializedComponentBatch::partitioned`].
    ///
    /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
    ///
    /// The specified `lengths` must sum to the total length of the component batch.
    ///
    /// [`SerializedComponentColumn`]: [::re_types_core::SerializedComponentColumn]
    #[inline]
    pub fn columns<I>(
        self,
        _lengths: I,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>>
    where
        I: IntoIterator<Item = usize> + Clone,
    {
        let columns = [
            self.samples
                .map(|samples| samples.partitioned(_lengths.clone()))
                .transpose()?,
            self.sample_rate
                .map(|sample_rate| sample_rate.partitioned(_lengths.clone()))
                .transpose()?,
            self.channel_names
                .map(|channel_names| channel_names.partitioned(_lengths.clone()))
                .transpose()?,
        ];
        Ok(columns.into_iter().flatten())
    }

    /// Helper to partition the component data into unit-length sub-batches.
    ///
    /// This is semantically similar to calling [`Self::columns`] with `std::iter::take(1).repeat(n)`,
    /// where `n` is automatically guessed.
    #[inline]
    pub fn columns_of_unit_batches(
        self,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>> {
        let len_samples = self.samples.as_ref().map(|b| b.array.len());
        let len_sample_rate = self.sample_rate.as_ref().map(|b| b.array.len());
        let len_channel_names = self.channel_names.as_ref().map(|b| b.array.len());
        let len = None
            .or(len_samples)
            .or(len_sample_rate)
            .or(len_channel_names)
            .unwrap_or(0);
        self.columns(std::iter::repeat_n(1, len))
    }

    /// PCM audio samples.
    #[inline]
    pub fn with_samples(mut self, samples: impl Into<crate::components::TensorData>) -> Self {
        self.samples = try_serialize_field(Self::descriptor_samples(), [samples]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::TensorData`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_samples`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_samples(
        mut self,
        samples: impl IntoIterator<Item = impl Into<crate::components::TensorData>>,
    ) -> Self {
        self.samples = try_serialize_field(Self::descriptor_samples(), samples);
        self
    }

    /// Sample rate in Hertz.
    #[inline]
    pub fn with_sample_rate(
        mut self,
        sample_rate: impl Into<crate::components::SampleRate>,
    ) -> Self {
        self.sample_rate = try_serialize_field(Self::descriptor_sample_rate(), [sample_rate]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::SampleRate`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_sample_rate`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_sample_rate(
        mut self,
        sample_rate: impl IntoIterator<Item = impl Into<crate::components::SampleRate>>,
    ) -> Self {
        self.sample_rate = try_serialize_field(Self::descriptor_sample_rate(), sample_rate);
        self
    }

    /// Optional display names for each audio channel.
    #[inline]
    pub fn with_channel_names(
        mut self,
        channel_names: impl IntoIterator<Item = impl Into<crate::components::AudioChannelName>>,
    ) -> Self {
        self.channel_names = try_serialize_field(Self::descriptor_channel_names(), channel_names);
        self
    }
}
