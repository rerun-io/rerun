// DO NOT EDIT! This file was auto-generated by crates/build/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/store/re_sdk_types/definitions/rerun/archetypes/audio_annotation.fbs".

#![allow(unused_braces)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::allow_attributes)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::cloned_instead_of_copied)]
#![allow(clippy::eq_op)]
#![allow(clippy::map_flatten)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::wildcard_imports)]

use ::re_types_core::SerializationResult;
use ::re_types_core::try_serialize_field;
use ::re_types_core::{ComponentBatch as _, SerializedComponentBatch};
use ::re_types_core::{ComponentDescriptor, ComponentType};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: A text annotation for a time window in an audio clip.
///
/// The `span` is measured in seconds relative to the row timestamp. For example,
/// log an ASR word at the start of the utterance with `span=[0.0, 0.42]`, or log
/// a complete transcript once at time zero with each word carrying its own
/// `span=[word_start, word_end]`.
///
/// ⚠️ **This type is _unstable_ and may change significantly in a way that the data won't be backwards compatible.**
///
/// ## Example
///
/// ### Log ASR word spans for audio.
/// ```ignore
/// fn main() -> Result<(), Box<dyn std::error::Error>> {
///     let rec =
///         rerun::RecordingStreamBuilder::new("rerun_example_audio_annotation")
///             .spawn()?;
///
///     rec.set_time("time", rerun::Duration::from_secs(0.0));
///     rec.log(
///         "audio/asr",
///         &rerun::AudioAnnotation::new("hello", [0.00, 0.32]),
///     )?;
///     rec.log(
///         "audio/asr",
///         &rerun::AudioAnnotation::new("world", [0.34, 0.72]),
///     )?;
///
///     Ok(())
/// }
/// ```
#[derive(Clone, Debug, PartialEq, Default, ::re_byte_size::SizeBytes)]
pub struct AudioAnnotation {
    /// Annotation text, such as an ASR word or phrase.
    pub text: Option<SerializedComponentBatch>,

    /// Time span in seconds relative to the row timestamp.
    pub span: Option<SerializedComponentBatch>,

    /// Optional color to use for the annotation span.
    pub color: Option<SerializedComponentBatch>,
}

impl AudioAnnotation {
    /// Returns the [`ComponentDescriptor`] for [`Self::text`].
    ///
    /// The corresponding component is [`crate::components::Text`].
    #[inline]
    pub fn descriptor_text() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.AudioAnnotation".into()),
                component: "AudioAnnotation:text".into(),
                component_type: Some("rerun.components.Text".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::span`].
    ///
    /// The corresponding component is [`crate::components::Range1D`].
    #[inline]
    pub fn descriptor_span() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.AudioAnnotation".into()),
                component: "AudioAnnotation:span".into(),
                component_type: Some("rerun.components.Range1D".into()),
            });
        (*DESCRIPTOR).clone()
    }

    /// Returns the [`ComponentDescriptor`] for [`Self::color`].
    ///
    /// The corresponding component is [`crate::components::Color`].
    #[inline]
    pub fn descriptor_color() -> ComponentDescriptor {
        static DESCRIPTOR: std::sync::LazyLock<ComponentDescriptor> =
            std::sync::LazyLock::new(|| ComponentDescriptor {
                archetype: Some("rerun.archetypes.AudioAnnotation".into()),
                component: "AudioAnnotation:color".into(),
                component_type: Some("rerun.components.Color".into()),
            });
        (*DESCRIPTOR).clone()
    }
}

static REQUIRED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 2usize]> =
    std::sync::LazyLock::new(|| {
        [
            AudioAnnotation::descriptor_text(),
            AudioAnnotation::descriptor_span(),
        ]
    });

static RECOMMENDED_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 0usize]> =
    std::sync::LazyLock::new(|| []);

static OPTIONAL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 1usize]> =
    std::sync::LazyLock::new(|| [AudioAnnotation::descriptor_color()]);

static ALL_COMPONENTS: std::sync::LazyLock<[ComponentDescriptor; 3usize]> =
    std::sync::LazyLock::new(|| {
        [
            AudioAnnotation::descriptor_text(),
            AudioAnnotation::descriptor_span(),
            AudioAnnotation::descriptor_color(),
        ]
    });

impl AudioAnnotation {
    /// The total number of components in the archetype: 2 required, 0 recommended, 1 optional
    pub const NUM_COMPONENTS: usize = 3usize;
}

impl ::re_types_core::Archetype for AudioAnnotation {
    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        ::re_types_core::external::re_string_interner::intern_static!(
            ::re_types_core::ArchetypeName,
            "rerun.archetypes.AudioAnnotation"
        )
    }

    #[inline]
    fn display_name() -> &'static str {
        "Audio annotation"
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentDescriptor]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentDescriptor, arrow::array::ArrayRef)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_descr: ::nohash_hasher::IntMap<_, _> = arrow_data.into_iter().collect();
        let text = arrays_by_descr
            .get(&Self::descriptor_text())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_text()));
        let span = arrays_by_descr
            .get(&Self::descriptor_span())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_span()));
        let color = arrays_by_descr
            .get(&Self::descriptor_color())
            .map(|array| SerializedComponentBatch::new(array.clone(), Self::descriptor_color()));
        Ok(Self { text, span, color })
    }
}

impl ::re_types_core::AsComponents for AudioAnnotation {
    #[inline]
    fn as_serialized_batches(&self) -> Vec<SerializedComponentBatch> {
        use ::re_types_core::Archetype as _;
        [self.text.clone(), self.span.clone(), self.color.clone()]
            .into_iter()
            .flatten()
            .collect()
    }
}

impl ::re_types_core::ArchetypeReflectionMarker for AudioAnnotation {}

impl crate::VisualizableArchetype for AudioAnnotation {
    #[inline]
    fn visualizer(&self) -> crate::Visualizer {
        crate::Visualizer::new("AudioAnnotation").with_overrides(self)
    }
}

impl AudioAnnotation {
    /// Create a new `AudioAnnotation`.
    #[inline]
    pub fn new(
        text: impl Into<crate::components::Text>,
        span: impl Into<crate::components::Range1D>,
    ) -> Self {
        Self {
            text: try_serialize_field(Self::descriptor_text(), [text]),
            span: try_serialize_field(Self::descriptor_span(), [span]),
            color: None,
        }
    }

    /// Update only some specific fields of a `AudioAnnotation`.
    #[inline]
    pub fn update_fields() -> Self {
        Self::default()
    }

    /// Clear all the fields of a `AudioAnnotation`.
    #[inline]
    pub fn clear_fields() -> Self {
        use ::re_types_core::Loggable as _;
        Self {
            text: Some(SerializedComponentBatch::new(
                crate::components::Text::arrow_empty(),
                Self::descriptor_text(),
            )),
            span: Some(SerializedComponentBatch::new(
                crate::components::Range1D::arrow_empty(),
                Self::descriptor_span(),
            )),
            color: Some(SerializedComponentBatch::new(
                crate::components::Color::arrow_empty(),
                Self::descriptor_color(),
            )),
        }
    }

    /// Partitions the component data into multiple sub-batches.
    ///
    /// Specifically, this transforms the existing [`SerializedComponentBatch`]es data into [`SerializedComponentColumn`]s
    /// instead, via [`SerializedComponentBatch::partitioned`].
    ///
    /// This makes it possible to use `RecordingStream::send_columns` to send columnar data directly into Rerun.
    ///
    /// The specified `lengths` must sum to the total length of the component batch.
    ///
    /// [`SerializedComponentColumn`]: [::re_types_core::SerializedComponentColumn]
    #[inline]
    pub fn columns<I>(
        self,
        _lengths: I,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>>
    where
        I: IntoIterator<Item = usize> + Clone,
    {
        let columns = [
            self.text
                .map(|text| text.partitioned(_lengths.clone()))
                .transpose()?,
            self.span
                .map(|span| span.partitioned(_lengths.clone()))
                .transpose()?,
            self.color
                .map(|color| color.partitioned(_lengths.clone()))
                .transpose()?,
        ];
        Ok(columns.into_iter().flatten())
    }

    /// Helper to partition the component data into unit-length sub-batches.
    ///
    /// This is semantically similar to calling [`Self::columns`] with `std::iter::take(1).repeat(n)`,
    /// where `n` is automatically guessed.
    #[inline]
    pub fn columns_of_unit_batches(
        self,
    ) -> SerializationResult<impl Iterator<Item = ::re_types_core::SerializedComponentColumn>> {
        let len_text = self.text.as_ref().map(|b| b.array.len());
        let len_span = self.span.as_ref().map(|b| b.array.len());
        let len_color = self.color.as_ref().map(|b| b.array.len());
        let len = None.or(len_text).or(len_span).or(len_color).unwrap_or(0);
        self.columns(std::iter::repeat_n(1, len))
    }

    /// Annotation text, such as an ASR word or phrase.
    #[inline]
    pub fn with_text(mut self, text: impl Into<crate::components::Text>) -> Self {
        self.text = try_serialize_field(Self::descriptor_text(), [text]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::Text`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_text`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_text(
        mut self,
        text: impl IntoIterator<Item = impl Into<crate::components::Text>>,
    ) -> Self {
        self.text = try_serialize_field(Self::descriptor_text(), text);
        self
    }

    /// Time span in seconds relative to the row timestamp.
    #[inline]
    pub fn with_span(mut self, span: impl Into<crate::components::Range1D>) -> Self {
        self.span = try_serialize_field(Self::descriptor_span(), [span]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::Range1D`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_span`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_span(
        mut self,
        span: impl IntoIterator<Item = impl Into<crate::components::Range1D>>,
    ) -> Self {
        self.span = try_serialize_field(Self::descriptor_span(), span);
        self
    }

    /// Optional color to use for the annotation span.
    #[inline]
    pub fn with_color(mut self, color: impl Into<crate::components::Color>) -> Self {
        self.color = try_serialize_field(Self::descriptor_color(), [color]);
        self
    }

    /// This method makes it possible to pack multiple [`crate::components::Color`] in a single component batch.
    ///
    /// This only makes sense when used in conjunction with [`Self::columns`]. [`Self::with_color`] should
    /// be used when logging a single row's worth of data.
    #[inline]
    pub fn with_many_color(
        mut self,
        color: impl IntoIterator<Item = impl Into<crate::components::Color>>,
    ) -> Self {
        self.color = try_serialize_field(Self::descriptor_color(), color);
        self
    }
}
