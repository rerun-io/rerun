[package]
name = "re_string_interner"
version = "0.1.0"
edition = "2021"
rust-version = "1.60"
license = "MIT OR Apache-2.0"
publish = false


[dependencies]
ahash = "0.7"
nohash-hasher = "0.2"
once_cell = "1.12"
parking_lot = "0.12"

# Optional:
serde = { version = "1", optional = true }
