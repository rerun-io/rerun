[package]
name = "re_sdk_comms"
version = "0.1.0"
edition = "2021"
rust-version = "1.63"
license = "MIT OR Apache-2.0"
publish = false


[features]
client = []
server = []


[dependencies]
re_log_types = { path = "../re_log_types", features = ["serde"] }

anyhow = "1.0.56"
bincode = "1.3"
tracing = "0.1"
