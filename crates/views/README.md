The views: the visualizations a user can put in the viewport.

Nothing else depends on a view — `re_viewer` in `../top` is what assembles them.
A sibling of `../panels`: neither folder may depend on the other.
