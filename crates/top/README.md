The top-level crates; the entry-points for our users.

`re_viewer` lives here too: it is the app that assembles the views in `../views` and the panels in `../viewer`, and only these top-level crates depend on it.
