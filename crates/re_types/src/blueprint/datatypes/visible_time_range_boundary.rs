// DO NOT EDIT! This file was auto-generated by crates/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/re_types/definitions/rerun/blueprint/datatypes/visible_time_range.fbs".

#![allow(trivial_numeric_casts)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::iter_on_single_items)]
#![allow(clippy::map_flatten)]
#![allow(clippy::match_wildcard_for_single_variants)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::unnecessary_cast)]

use ::re_types_core::external::arrow2;
use ::re_types_core::ComponentName;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch, MaybeOwnedComponentBatch};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Datatype**: Type of boundary for visible history.
#[derive(Clone, Debug)]
pub struct VisibleTimeRangeBoundary {
    /// Type of the boundary.
    pub kind: crate::blueprint::datatypes::VisibleTimeRangeBoundaryKind,

    /// Value of the boundary (ignored for `Infinite` type).
    pub time: crate::blueprint::datatypes::TimeInt,
}

impl ::re_types_core::SizeBytes for VisibleTimeRangeBoundary {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.kind.heap_size_bytes() + self.time.heap_size_bytes()
    }

    #[inline]
    fn is_pod() -> bool {
        <crate::blueprint::datatypes::VisibleTimeRangeBoundaryKind>::is_pod()
            && <crate::blueprint::datatypes::TimeInt>::is_pod()
    }
}

::re_types_core::macros::impl_into_cow!(VisibleTimeRangeBoundary);

impl ::re_types_core::Loggable for VisibleTimeRangeBoundary {
    type Name = ::re_types_core::DatatypeName;

    #[inline]
    fn name() -> Self::Name {
        "rerun.blueprint.datatypes.VisibleTimeRangeBoundary".into()
    }

    #[allow(clippy::wildcard_imports)]
    #[inline]
    fn arrow_datatype() -> arrow2::datatypes::DataType {
        use arrow2::datatypes::*;
        DataType::Struct(std::sync::Arc::new(vec![
            Field::new(
                "kind",
                <crate::blueprint::datatypes::VisibleTimeRangeBoundaryKind>::arrow_datatype(),
                false,
            ),
            Field::new(
                "time",
                <crate::blueprint::datatypes::TimeInt>::arrow_datatype(),
                false,
            ),
        ]))
    }

    #[allow(clippy::wildcard_imports)]
    fn to_arrow_opt<'a>(
        data: impl IntoIterator<Item = Option<impl Into<::std::borrow::Cow<'a, Self>>>>,
    ) -> SerializationResult<Box<dyn arrow2::array::Array>>
    where
        Self: Clone + 'a,
    {
        use ::re_types_core::{Loggable as _, ResultExt as _};
        use arrow2::{array::*, datatypes::*};
        Ok({
            let (somes, data): (Vec<_>, Vec<_>) = data
                .into_iter()
                .map(|datum| {
                    let datum: Option<::std::borrow::Cow<'a, Self>> = datum.map(Into::into);
                    (datum.is_some(), datum)
                })
                .unzip();
            let bitmap: Option<arrow2::bitmap::Bitmap> = {
                let any_nones = somes.iter().any(|some| !*some);
                any_nones.then(|| somes.into())
            };
            StructArray::new(
                <crate::blueprint::datatypes::VisibleTimeRangeBoundary>::arrow_datatype(),
                vec![
                    {
                        let (somes, kind): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| {
                                    let Self { kind, .. } = &**datum;
                                    kind.clone()
                                });
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let kind_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        {
                            _ = kind_bitmap;
                            crate::blueprint::datatypes::VisibleTimeRangeBoundaryKind::to_arrow_opt(
                                kind,
                            )?
                        }
                    },
                    {
                        let (somes, time): (Vec<_>, Vec<_>) = data
                            .iter()
                            .map(|datum| {
                                let datum = datum.as_ref().map(|datum| {
                                    let Self { time, .. } = &**datum;
                                    time.clone()
                                });
                                (datum.is_some(), datum)
                            })
                            .unzip();
                        let time_bitmap: Option<arrow2::bitmap::Bitmap> = {
                            let any_nones = somes.iter().any(|some| !*some);
                            any_nones.then(|| somes.into())
                        };
                        PrimitiveArray::new(
                            DataType::Int64,
                            time.into_iter()
                                .map(|datum| {
                                    datum
                                        .map(|datum| {
                                            let crate::blueprint::datatypes::TimeInt(data0) = datum;
                                            data0
                                        })
                                        .unwrap_or_default()
                                })
                                .collect(),
                            time_bitmap,
                        )
                        .boxed()
                    },
                ],
                bitmap,
            )
            .boxed()
        })
    }

    #[allow(clippy::wildcard_imports)]
    fn from_arrow_opt(
        arrow_data: &dyn arrow2::array::Array,
    ) -> DeserializationResult<Vec<Option<Self>>>
    where
        Self: Sized,
    {
        use ::re_types_core::{Loggable as _, ResultExt as _};
        use arrow2::{array::*, buffer::*, datatypes::*};
        Ok({
            let arrow_data = arrow_data
                .as_any()
                .downcast_ref::<arrow2::array::StructArray>()
                .ok_or_else(|| {
                    let expected = Self::arrow_datatype();
                    let actual = arrow_data.data_type().clone();
                    DeserializationError::datatype_mismatch(expected, actual)
                })
                .with_context("rerun.blueprint.datatypes.VisibleTimeRangeBoundary")?;
            if arrow_data.is_empty() {
                Vec::new()
            } else {
                let (arrow_data_fields, arrow_data_arrays) =
                    (arrow_data.fields(), arrow_data.values());
                let arrays_by_name: ::std::collections::HashMap<_, _> = arrow_data_fields
                    .iter()
                    .map(|field| field.name.as_str())
                    .zip(arrow_data_arrays)
                    .collect();
                let kind = {
                    if !arrays_by_name.contains_key("kind") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "kind",
                        ))
                        .with_context("rerun.blueprint.datatypes.VisibleTimeRangeBoundary");
                    }
                    let arrow_data = &**arrays_by_name["kind"];
                    crate::blueprint::datatypes::VisibleTimeRangeBoundaryKind::from_arrow_opt(
                        arrow_data,
                    )
                    .with_context("rerun.blueprint.datatypes.VisibleTimeRangeBoundary#kind")?
                    .into_iter()
                };
                let time = {
                    if !arrays_by_name.contains_key("time") {
                        return Err(DeserializationError::missing_struct_field(
                            Self::arrow_datatype(),
                            "time",
                        ))
                        .with_context("rerun.blueprint.datatypes.VisibleTimeRangeBoundary");
                    }
                    let arrow_data = &**arrays_by_name["time"];
                    arrow_data
                        .as_any()
                        .downcast_ref::<Int64Array>()
                        .ok_or_else(|| {
                            let expected = DataType::Int64;
                            let actual = arrow_data.data_type().clone();
                            DeserializationError::datatype_mismatch(expected, actual)
                        })
                        .with_context("rerun.blueprint.datatypes.VisibleTimeRangeBoundary#time")?
                        .into_iter()
                        .map(|opt| opt.copied())
                        .map(|res_or_opt| {
                            res_or_opt.map(|v| crate::blueprint::datatypes::TimeInt(v))
                        })
                };
                arrow2::bitmap::utils::ZipValidity::new_with_validity(
                    ::itertools::izip!(kind, time),
                    arrow_data.validity(),
                )
                .map(|opt| {
                    opt.map(|(kind, time)| {
                        Ok(Self {
                            kind: kind
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context(
                                    "rerun.blueprint.datatypes.VisibleTimeRangeBoundary#kind",
                                )?,
                            time: time
                                .ok_or_else(DeserializationError::missing_data)
                                .with_context(
                                    "rerun.blueprint.datatypes.VisibleTimeRangeBoundary#time",
                                )?,
                        })
                    })
                    .transpose()
                })
                .collect::<DeserializationResult<Vec<_>>>()
                .with_context("rerun.blueprint.datatypes.VisibleTimeRangeBoundary")?
            }
        })
    }
}
