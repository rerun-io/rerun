// DO NOT EDIT! This file was auto-generated by crates/re_types_builder/src/codegen/rust/api.rs
// Based on "crates/re_types/definitions/rerun/blueprint/archetypes/axis_y.fbs".

#![allow(trivial_numeric_casts)]
#![allow(unused_imports)]
#![allow(unused_parens)]
#![allow(clippy::clone_on_copy)]
#![allow(clippy::iter_on_single_items)]
#![allow(clippy::map_flatten)]
#![allow(clippy::match_wildcard_for_single_variants)]
#![allow(clippy::needless_question_mark)]
#![allow(clippy::new_without_default)]
#![allow(clippy::redundant_closure)]
#![allow(clippy::too_many_arguments)]
#![allow(clippy::too_many_lines)]
#![allow(clippy::unnecessary_cast)]

use ::re_types_core::external::arrow2;
use ::re_types_core::ComponentName;
use ::re_types_core::SerializationResult;
use ::re_types_core::{ComponentBatch, MaybeOwnedComponentBatch};
use ::re_types_core::{DeserializationError, DeserializationResult};

/// **Archetype**: Configuration for the legend of a plot.
#[derive(Clone, Debug, Default)]
pub struct AxisY {
    /// The default range of the y-axis of the plot.
    ///
    /// If unset, the range well be automatically determined based on the data.
    pub range: Option<crate::components::Range1D>,

    /// How the axis should scale as the plot is zoomed.
    pub zoom_behavior: Option<crate::blueprint::components::ZoomBehavior>,
}

impl ::re_types_core::SizeBytes for AxisY {
    #[inline]
    fn heap_size_bytes(&self) -> u64 {
        self.range.heap_size_bytes() + self.zoom_behavior.heap_size_bytes()
    }

    #[inline]
    fn is_pod() -> bool {
        <Option<crate::components::Range1D>>::is_pod()
            && <Option<crate::blueprint::components::ZoomBehavior>>::is_pod()
    }
}

static REQUIRED_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 0usize]> =
    once_cell::sync::Lazy::new(|| []);

static RECOMMENDED_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 1usize]> =
    once_cell::sync::Lazy::new(|| ["rerun.blueprint.components.AxisYIndicator".into()]);

static OPTIONAL_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 3usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            "rerun.blueprint.components.ZoomBehavior".into(),
            "rerun.components.InstanceKey".into(),
            "rerun.components.Range1D".into(),
        ]
    });

static ALL_COMPONENTS: once_cell::sync::Lazy<[ComponentName; 4usize]> =
    once_cell::sync::Lazy::new(|| {
        [
            "rerun.blueprint.components.AxisYIndicator".into(),
            "rerun.blueprint.components.ZoomBehavior".into(),
            "rerun.components.InstanceKey".into(),
            "rerun.components.Range1D".into(),
        ]
    });

impl AxisY {
    pub const NUM_COMPONENTS: usize = 4usize;
}

/// Indicator component for the [`AxisY`] [`::re_types_core::Archetype`]
pub type AxisYIndicator = ::re_types_core::GenericIndicatorComponent<AxisY>;

impl ::re_types_core::Archetype for AxisY {
    type Indicator = AxisYIndicator;

    #[inline]
    fn name() -> ::re_types_core::ArchetypeName {
        "rerun.blueprint.archetypes.AxisY".into()
    }

    #[inline]
    fn indicator() -> MaybeOwnedComponentBatch<'static> {
        static INDICATOR: AxisYIndicator = AxisYIndicator::DEFAULT;
        MaybeOwnedComponentBatch::Ref(&INDICATOR)
    }

    #[inline]
    fn required_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        REQUIRED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn recommended_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        RECOMMENDED_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn optional_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        OPTIONAL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn all_components() -> ::std::borrow::Cow<'static, [ComponentName]> {
        ALL_COMPONENTS.as_slice().into()
    }

    #[inline]
    fn from_arrow_components(
        arrow_data: impl IntoIterator<Item = (ComponentName, Box<dyn arrow2::array::Array>)>,
    ) -> DeserializationResult<Self> {
        re_tracing::profile_function!();
        use ::re_types_core::{Loggable as _, ResultExt as _};
        let arrays_by_name: ::std::collections::HashMap<_, _> = arrow_data
            .into_iter()
            .map(|(name, array)| (name.full_name(), array))
            .collect();
        let range = if let Some(array) = arrays_by_name.get("rerun.components.Range1D") {
            <crate::components::Range1D>::from_arrow_opt(&**array)
                .with_context("rerun.blueprint.archetypes.AxisY#range")?
                .into_iter()
                .next()
                .flatten()
        } else {
            None
        };
        let zoom_behavior =
            if let Some(array) = arrays_by_name.get("rerun.blueprint.components.ZoomBehavior") {
                <crate::blueprint::components::ZoomBehavior>::from_arrow_opt(&**array)
                    .with_context("rerun.blueprint.archetypes.AxisY#zoom_behavior")?
                    .into_iter()
                    .next()
                    .flatten()
            } else {
                None
            };
        Ok(Self {
            range,
            zoom_behavior,
        })
    }
}

impl ::re_types_core::AsComponents for AxisY {
    fn as_component_batches(&self) -> Vec<MaybeOwnedComponentBatch<'_>> {
        re_tracing::profile_function!();
        use ::re_types_core::Archetype as _;
        [
            Some(Self::indicator()),
            self.range
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
            self.zoom_behavior
                .as_ref()
                .map(|comp| (comp as &dyn ComponentBatch).into()),
        ]
        .into_iter()
        .flatten()
        .collect()
    }

    #[inline]
    fn num_instances(&self) -> usize {
        0
    }
}

impl AxisY {
    pub fn new() -> Self {
        Self {
            range: None,
            zoom_behavior: None,
        }
    }

    #[inline]
    pub fn with_range(mut self, range: impl Into<crate::components::Range1D>) -> Self {
        self.range = Some(range.into());
        self
    }

    #[inline]
    pub fn with_zoom_behavior(
        mut self,
        zoom_behavior: impl Into<crate::blueprint::components::ZoomBehavior>,
    ) -> Self {
        self.zoom_behavior = Some(zoom_behavior.into());
        self
    }
}
