include "arrow/attributes.fbs";
include "fbs/attributes.fbs";

namespace rerun.datatypes;

// ---

/// Storage for a `Tensor`
struct TensorId (
  order: 100,
  "attr.arrow.transparent",
  "attr.rust.derive": "Copy, Default, PartialEq"
) {
  id: [ubyte:16] (order: 100);
}
