The panels the app is assembled from, and the widgets they are built out of.

A sibling of `../views`: neither folder may depend on the other.
`re_viewer` — the app that hosts both — lives in `../top`, and the UI and rendering machinery in `../viewer_support`.
