# redap-server

Part of the [`dataplatform`](https://github.com/rerun-io/dataplatform) family of crates.

## Running locally

If you want to run Storage node locally, you can use the example config (or create your own):
``
cargo run -p redap-server -- --config-file redap-server/resources/example_config.ron
``
