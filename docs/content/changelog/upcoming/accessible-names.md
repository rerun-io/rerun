---
title: "Every panel, button and input has an accessible name"
hidden: true
type: misc
---

Every panel, icon button, menu and input in the Viewer now has a name in the accessibility tree, so screen readers, `egui_kittest` queries and the MCP UI tools can reach them by name.
