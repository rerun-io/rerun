---
title: "Links to datasets"
hidden: true
type: feature
---

### Links to datasets

A URI to a dataset is now `rerun://<origin>/dataset/<dataset_id>` instead of `rerun://<origin>/entry/<dataset_id>`. The older version
of the link still resolve correctly.
