[package]
name = "graph_view"
edition = "2021"
rust-version = "1.76"
license = "MIT OR Apache-2.0"
publish = false

[features]
default = []

# Turn on if you want to share analytics with Rerun (e.g. callstacks on crashes).
analytics = ["re_crash_handler/analytics", "re_viewer/analytics"]

[dependencies]
re_crash_handler = { path = "../../../crates/utils/re_crash_handler" }
re_format = { path = "../../../crates/utils/re_format" }
re_viewer = { path = "../../../crates/viewer/re_viewer", default-features = false }
re_log_types = { path = "../../../crates/store/re_log_types" }

# We need re_sdk_comms to receive log events from and SDK:
re_sdk_comms = { path = "../../../crates/store/re_sdk_comms", features = [
  "server",
] }

# mimalloc is a much faster allocator:
mimalloc = "0.1"

petgraph = "0.6"
bytemuck = "1.18"
thiserror = "1.0"

# Experiment with different layout algorithms.
layout-rs = "0.1"
fdg-sim = "0.9"
# `fdg` supersedes `fdg-sim` and is more featureful but not released to crates.io yet.
fdg = { git = "https://github.com/grantshandy/fdg", rev = "50755f1dea20249d753600e7e7e51ca33e87b5a1", default-features = false }
rand = "0.8"
