[package]
name = "custom_store_subscriber"
version = "0.14.0-rc.3"
edition = "2021"
rust-version = "1.74"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
rerun = { path = "../../../crates/rerun", features = ["native_viewer", "run"] }

[build-dependencies]
re_build_tools = { path = "../../../crates/re_build_tools" }
