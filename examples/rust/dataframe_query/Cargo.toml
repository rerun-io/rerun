[package]
name = "dataframe_query"
version = "0.29.0-alpha.8+dev"
edition = "2024"
rust-version = "1.90"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
rerun = { path = "../../../crates/top/rerun", default-features = false, features = ["dataframe"] }

itertools.workspace = true
unindent.workspace = true
