[package]
name = "chess_robby_fischer"
version = "0.1.0"
edition = "2024"
publish = false

# This is a documentation-only example with no actual code
# The README contains code snippets that reference an external project

[lib]
path = "lib.rs"
