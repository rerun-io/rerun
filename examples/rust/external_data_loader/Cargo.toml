[package]
# TODO: description, readme, screenshot, etc
name = "rerun-loader-rust-file"
version = "0.12.0-alpha.1+dev"
edition = "2021"
rust-version = "1.72"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
rerun = { path = "../../../crates/rerun" }

pico-args = { version = "0.5.0", features = ["eq-separator"] }
