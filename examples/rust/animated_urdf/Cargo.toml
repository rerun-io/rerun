[package]
name = "animated_urdf"
version = "0.30.0-alpha.5"
edition = "2024"
rust-version = "1.90"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
rerun = { path = "../../../crates/top/rerun", features = ["clap", "data_loaders"] }

anyhow.workspace = true
clap = { workspace = true, features = ["derive"] }
emath.workspace = true
