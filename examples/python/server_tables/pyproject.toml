[project]
name = "server_tables"
version = "0.1.1"
readme = "README.md"
dependencies = ["rerun-sdk", "datafusion==52.3.0"]

[project.scripts]
server_tables = "server_tables:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
