[project]
name = "server_tables"
version = "0.1.0"
# requires-python = "<3.12"
readme = "README.md"
dependencies = ["rerun-sdk", "datafusion==50.0.0"]

[project.scripts]
dataframe_query = "server_tables:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
