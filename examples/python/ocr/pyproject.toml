[project]
name = "ocr"
version = "0.1.0"
readme = "README.md"
dependencies = [
  "opencv-python",
  "paddleclas",
  "paddleocr",
  "paddlepaddle",
  "pandas",
  "requests",
  "rerun-sdk",
  "setuptools",
  "tabulate",
  "tqdm",
  "pdf2image",
]

[project.scripts]
ocr = "ocr:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
