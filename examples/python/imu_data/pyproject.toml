[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "imu_data"
version = "0.1.0"
requires-python = "<3.12"                                            # TODO(ab): relax when mediapipe supports 3.12
dependencies = ["numpy", "requests>=2.31,<3", "rerun-sdk", "pandas"]


[project.scripts]
imu_data = "imu_data:main"

[tool.rerun-example]
skip = false
