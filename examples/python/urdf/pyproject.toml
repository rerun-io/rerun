[project]
name = "urdf"
version = "0.1.0"
readme = "README.md"
dependencies = ["rerun-sdk"]

[project.scripts]
urdf = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
