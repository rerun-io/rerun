[project]
name = "graph_binary_tree"
version = "0.1.0"
readme = "README.md"
dependencies = ["rerun-sdk"]

[project.scripts]
graph_binary_tree = "graph_binary_tree:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
