[project]
name = "minimal"
version = "0.1.0"
readme = "README.md"
dependencies = ["numpy", "rerun-sdk"]

[project.scripts]
minimal = "minimal:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
