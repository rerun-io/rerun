[project]
name = "state_timeline"
version = "0.1.0"
readme = "README.md"
dependencies = ["rerun-sdk"]

[project.scripts]
state_timeline = "state_timeline:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
