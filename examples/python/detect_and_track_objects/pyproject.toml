[project]
name = "detect_and_track_objects"
version = "0.1.0"
# requires-python = "<3.12"
readme = "README.md"
dependencies = [
  "numpy",
  "opencv-python>4.9",
  "pillow",
  "requests>=2.31,<3",
  "rerun-sdk",
  "timm==1.0.19",
  "torch", # this will use the version defined in the uv workspace
  "transformers>=5.5.0", # >=5.5.0 clears three code-execution advisories in 4.x
]

[project.scripts]
detect_and_track_objects = "detect_and_track_objects:main"

[tool.rerun-example]
extra-args = "--max-frame=10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
